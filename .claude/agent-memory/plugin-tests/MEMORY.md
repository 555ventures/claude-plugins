# Memory index

- [Doctrine regex pins and line-wrap](doctrine-regex-linewrap.md) — use \s+ not a literal space between words in multi-word doctrine regex pins; markdown hard-wrap can split the phrase across lines in only one of several files.
- [Stale dispatch premise from a concurrent session](stale-dispatch-premise-concurrent-session.md) — re-derive current repo state (git log, live grep) before authoring a red pin from a dispatch prompt's incident narrative; another session can land the fix mid-task.
- [scope-reconcile degenerate stems](scope-reconcile-degenerate-stems.md) — gitRepo() pre-seeds a committed root .gitignore; mutation-proof one AC via cp-mutant + test-name-pattern + restore, never a stray scratch file.
- [replay.js: cwd not --root](replay-js-cwd-not-root.md) — spec 20260819/02's Decisions give exhaustive flag lists with no --root; replay.test.js uses runNode(..., {cwd: dir}) + explicit --dir, never --root.
- [Spec AC example vs shipped refusal](spec-ac-example-vs-shipped-refusal.md) — verify an AC's "e.g." worked-example literal against the actual current script before hardcoding it in a test; spec 20260819/02's own AC-4 example subject fails its own D5 regex.
- [Prove a regression pin against reconstructed old code](regression-pin-prove-against-reconstructed-old-code.md) — a fix-iteration "no test ever asserted X" dispatch requires empirically running the new assert against a scratch reconstruction of the old broken behavior, not just asserting it would fail.
- [Spec collision sweep can miss same-file collisions](spec-collision-sweep-can-miss-same-file-collisions.md) — grep the assigned file for every invocation of a retired flag, not just the Assumptions-named pins; also confirms the vacuous-rejection Gotcha (generic old-code fallback masks a new-behavior red pin) — prove it via the pre-image script before trusting a rejection AC as red.
- [Self-matching literal pin: fragment, don't exempt](self-matching-literal-pin-fragment-idiom.md) — a grep-for-string pin whose own tracked file would match assembles the forbidden string from runtime fragments (like tracked-text-purity's '\x00'), never a self-path exemption which fails silent.
- [Gate thresholds live in the roadmap brief](gate-thresholds-live-in-roadmap-brief.md) — a "brief-NN gate" Decision can give the formula but not the cutoff number; grep docs/roadmap/NN-*.md before treating it as an unlocked fork.
- [Review-finding pins with no AC-ID](review-finding-pins-no-ac.md) — name tests "review finding N", never fabricate an AC-ID; prove byte-threshold fixtures empirically and strip // comments before matching a literal like process.exit(0).
- [AC example points at an unreachable branch](ac-example-unreachable-branch.md) — trace the called lib function's actual throw/catch paths before trusting an AC's e.g.; a vacuous-but-correct exec pin is kept and logged, not forced red.
- [Driver reproducibility + worktree-CWD pins](driver-contract-reproducibility-and-worktree-cwd-pins.md) — "byte-equal to X.js's line" tests by lifting run-id/diff/iteration off the recorded row and stripping ts; CWD-guard tests vary spawn cwd, never the absolute specPath; cap tests hand-edit the sidecar counter with no matching manifest files.
- [Dispatch prompt overgeneralizes a refusal trigger](dispatch-prompt-overgeneralizes-refusal-trigger.md) — a "pin both directions" dispatch can broaden a conditional refusal (sidecar exists, no closeRunId) into an unconditional one (any done spec); verify the exact trigger against the live script, not the paraphrase.
- [Concurrent worker file collision + --select tie-break](concurrent-worker-file-collision-select-tiebreak.md) — Edit's "modified on disk" warning means stop and re-Read/diff before trusting your own draft; also replay.js --select self-selects the driver's own just-closed CLEAN+runId row, breaking naive "seed a separate eligible target" fixtures.
- [Tag anchoring: retag + prose regression](tag-anchoring-retag-and-prose-regression.md) — a [tag:] parser narrowed to 2 fixed positions needs every 3rd-position fixture moved to the declaration slot, plus a new regression quoting the real spec doc's own self-tagging wording, not a generic mid-sentence string.
- [PATH-stub binaries for CLI retry loops](path-stub-binaries-for-cli-retry-loops.md) — curl/gh PATH stubs with invocation-counter files for retry/poll ACs; a real child-process HTTP server (never in-process) for reachable-URL legs, spawnSync-safe.
- [Predicate widening: prove no-collision](predicate-widening-no-collision-proof.md) — hand-trace the amended regex/predicate against every pre-existing fixture string, then confirm with a full scoped test run, before trusting "no collision" over a prose claim.
- [git committer date Z round-trips](git-committer-date-iso-z-roundtrips.md) — GIT_COMMITTER_DATE="...Z" comes back verbatim from `git log -1 --format=%cI`; use it for exact-string date assertions in ttl/ordering fixtures.
- [New-spec ACs green pre-change](new-spec-ac-green-pre-change.md) — an AC pinning a not-yet-built mechanism's absence (overlay off/suppressed) is a sanctioned green-pre-change pin, not a stale-assumption block; also reconfirms replay.js's no-`--root`/cwd-based CLI pattern.
- [Pipe-truncation ground truth via file redirect](pipe-truncation-file-redirect-ground-truth.md) — pin a console.log()+process.exit() async-pipe-truncation fix with a synchronous file-redirect run as byte-count ground truth vs a real piped run; gate the vacuousness check on the ground truth, not the (always-truncated-under-the-bug) piped run.
- [Exec fixture can't reach branch: extract to lib](exec-fixture-cant-reach-branch-extract-to-lib.md) — when the real script a fixture drives structurally can't emit the AC's named input shape (only a superset), extract the parser to spec/scripts/lib/ and prove it with a direct test, retargeting the old exec test to what it actually proves.
- [render-rules ellipsis flags + var() discriminator](spec-20260824-04-render-rules-ellipsis-flags.md) — `--flag <x>…` in Contracts prose with no repo precedent resolves to repeated `--flag value` pairs per Worker Rules' own "hand-rolled --flag value" line; prove var()-resolution via absence of an "unresolvable" advisory line, not downstream pass/fail, when the AC's own token pair is non-discriminating.
- [Banned-literal loop dedup + blind-spot sweep](banned-literal-loop-dedup-and-blind-spot-sweep.md) — file-local `assertNoBannedLiterals(src,banned,msgFor)` for 3+ near-identical loops (never tests/helpers.js, single-consumer); wf-*.js exclusion retired 2026-08-25 (specs/20260825/05); keep enumerated sweeps as standing gates, stricter than doctrine siblings.
- [Prompt/step window isolation](spec-20260825-02-prompt-window-isolation.md) — when an AC scopes a literal check to "the prompt"/"the step" not "the file", isolate that substring with a bounded regex first; a whole-file check risks a vacuous pass off an unrelated later mention (e.g. a schema property key sharing the same word).
- [logTail byte-window falsification](logtail-byte-window-falsification.md) — three fixture shapes (giant single line via head/tr, many-short-lines via yes|head, fits-both-bounds control) for a "bound an excerpt by bytes not lines" fix; copy-one-function-back-in-place beats git-history reconstruction when a sibling-script resolution constraint (__dirname) applies.
- [collision-closure --spec resolves against cwd](collision-closure-spec-flag-resolves-against-cwd.md) — unlike scope-reconcile.js's path.join(root, specPath), collision-closure.js bare-reads --spec against process.cwd(); pass {cwd: dir} in runNode or it ENOENTs at exit 2 despite --root being correct.
- [sweepRetiredLiteral: waive only what must contain the literal](sweepretiredliteral-new-sweep-must-waive-own-siblings.md) — waivedPaths for your own new test files + a sibling sweep's test file only; edit out-of-batch stale mentions in place instead of waiving; never spell the retired literal in a new file, memory included.
- [genesis-driver: descriptor archetype vs status.archetype](driver-descriptor-archetype-vs-status-archetype.md) — deriveState() branches ONLY on status.archetype (brief `## Picks`); stack-descriptor.json's archetype field is cosmetic; a real r0-* winner dir needs the full internal tile funnel, no shortcut.
- [Red-proof must match persisted, not in-memory, order](red-proof-must-match-persisted-not-inmemory-order.md) — reverting "A now runs before B" needs saveSidecar() moved too, or a runGate() death-exit makes the revert a silent no-op.
- [AC-1/AC-2 banned literal collides with sibling sweep](ac1-ac2-banned-literal-collides-with-sibling-sweep.md) — a new banned-literal test can spell a string a DIFFERENT spec's repo-wide sweepRetiredLiteral already tracks; extend that sweep's existing per-literal waivedPaths, never touch its assertion; always run full npm test, not just assigned files.
- [Status-diet blocked-top + decide/hygiene fold gotchas](spec-20260903-05-status-diet.md) — rank-inverted fixture for "waiting on X" footer literal; pre-image's anomaly-section count can differ from total due to the old fold behavior, not a fixture bug.
- [mocks-driver fixture gotchas](mocks-driver-fixture-gotchas.md) — THEME needs >=3 journey labels for direction-composed; targets.json viewports must be non-empty even short of `approved`; serve's per-endpoint content-type can override the general MIME map; reconfirms top-level require-red and green-pre-change continuity pins.
