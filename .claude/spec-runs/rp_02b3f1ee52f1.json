{
  "ts": "2026-08-26T15:45:18.237Z",
  "stage": "replay",
  "spec": "specs/20260825/01-genesis-panel-collapse.md",
  "runId": "rp_02b3f1ee52f1",
  "reviewRunId": "rv_5c453029cee5",
  "class": "doc-contract-lie",
  "files": [
    "spec/bin/spec-paths"
  ],
  "legs": "baseline-red:reconcile",
  "outcome": "caught",
  "tokens": 96422,
  "patch": "diff --git a/spec/bin/spec-paths b/spec/bin/spec-paths\nindex a7972ac..8fd85b4 100755\n--- a/spec/bin/spec-paths\n+++ b/spec/bin/spec-paths\n@@ -75,7 +75,7 @@ case \"${1:-root}\" in\n       escape)  SECTIONS=\"Host Grounding|Feedback Loop|Incident Policy|Question Style|Console Output Style\" ;;\n       doctor)  SECTIONS=\"Host Grounding|Grounding Drift|Rule Enforcement|Runtime Verification|Feedback Loop|Tiers|State Machine|Question Style|Console Output Style\" ;;\n       replay)  SECTIONS=\"Host Grounding|Tiers|Model Placement|Decisions|Question Style|Console Output Style|Feedback Loop\" ;;\n-      *) cat \"$CORE\" \"$DESIGN\"; exit 0 ;;\n+      *) cat \"$CORE\"; exit 0 ;;\n     esac\n     echo \"<!-- shared invariants scoped to /spec:${CMD} — full doctrine: $CORE + $DESIGN -->\"\n     # Prefix match on the heading (some headings carry a parenthetical suffix). keep resets at\n",
  "reviewer": {
    "verdict": "CLEAN",
    "survivors": [
      {
        "severity": "hard",
        "claim": "spec/bin/spec-paths's `shared-for` default (unmapped-command) branch was changed from `cat \"$CORE\" \"$DESIGN\"; exit 0` to `cat \"$CORE\"; exit 0`, silently dropping all of design.md from the fail-open fallback that its own doc comment two lines above still promises.",
        "file": "spec/bin/spec-paths",
        "line": 78,
        "impact": "Any command name not in the explicit SECTIONS case list (a typo, or any future command added without updating this switch) now gets only core.md instead of core.md+design.md — the script contradicts its own documented contract in the same file, and the loss is silent (exit 0, no error).",
        "evidence": "Doc comment at spec/bin/spec-paths:55-57 (unchanged by this diff): 'an unknown command falls back to both full documents (fail-open, never less doctrine than intended)'. Executed repro: `./spec/bin/spec-paths shared-for unknown-cmd | wc -l` -> 310 lines (core.md only) vs `cat spec/doctrine/core.md spec/doctrine/design.md | wc -l` -> 467 lines. npm test (708/708 pass) does not catch this."
      }
    ],
    "killed": [],
    "reviewerCount": 1,
    "scope": "full",
    "tokens": 68000
  }
}
