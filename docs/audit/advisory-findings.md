# Advisory smell findings — accepted at review
<!-- appended by /spec:review (smell lens); ingested wholesale by the hotspot audit (roadmap brief 05) -->

- 2026-08-12 duplication spec/scripts/hotspot.js:91 duplicates spec/scripts/scope-reconcile.js:64 — hotspot.js defines a private loadConfig() (read .claude/spec.config.json, JSON.parse, catch → {}) byte-for-byte identical to scope-reconcile.js's, left behind by the D17 extraction that deduplicated the neighboring globMatch/BASELINE_GLOBS pair into lib/glob-match.js (spec specs/20260812/02-hotspot-audit.md, runId wf_c4af166d-86f) — RESOLVED 2026-08-12: both loadConfig blocks extracted into lib/glob-match.js `pipelineOwnedGlobs(root)`, sole resolver of the exclusion set; no open debt
- 2026-08-14 duplication spec/scripts/ci-query.js:56 duplicates spec/scripts/observe-ci.js:55 — both grew a byte-identical private readForge(dir) (read .claude/spec.config.json, JSON.parse, catch → undefined, return capabilities.forge) in the same diff; a third config read already exists at lib level in spec/scripts/lib/glob-match.js:45 `pipelineOwnedGlobs(root)`, so the correct paydown is ONE shared readConfig(root) serving all three call sites — a readForge-only lib would add a second lib-level config-read shape rather than remove one (Fable retainer brief, 2026-08-14). The spec's own Assumptions conditioned extraction on the two readers DIVERGING, which has not happened; the same class was found and resolved same-day once before (the 2026-08-12 loadConfig row above) (spec specs/20260813/10-host-capabilities.md, runId wf_aa92b4e0-575) — RESOLVED 2026-08-14: all three config reads extracted into lib/host-config.js `readConfig(root)`, with `declaredForge(root)` as the sole derivation of the declared forge capability; glob-match.js keeps the exclusion set and sheds the file access; no open debt
- 2026-08-14 duplication spec/scripts/ac-matrix.js:208 duplicates spec/scripts/verdict.js:103 — ac-matrix.js hand-rolled its own readManifestRows(p) (read the JSONL evidence manifest, JSON.parse per non-blank line, Map keyed by `leg`, last-row-wins, malformed lines skipped) doing the job verdict.js's legRows loop already does; the same spec extracted parseFilePlanRows into lib/file-plan.js for the File Plan side, so the manifest side was the one seam left hand-rolled. NOT identical: verdict.js tracks `manifestValid` (a malformed line poisons the verdict word) while ac-matrix.js deliberately ignores malformed lines and cites verdict.js as the well-formedness authority (ac-matrix.js:222-224), so a shared helper needs a strictness flag rather than a straight lift — extraction is warranted on the third copy, not this one. Third row in this log's private-helper-duplication class after the 2026-08-12 loadConfig and 2026-08-14 readForge rows, both of which resolved into lib/ (spec specs/20260814/01-ac-matrix-script.md, runId wf_e4b1f909-735)
- 2026-08-15 duplication spec/scripts/ci-gate-parity.js:40 duplicates spec/scripts/lib/host-config.js:25 — the new ci-gate-parity.js opens and JSON.parses `.claude/spec.config.json` inline (path.join(root, '.claude', 'spec.config.json') → readFileSync → JSON.parse in a try/catch that die()s) instead of calling `readConfig(root)` from lib/host-config.js, the helper this repo extracted ONE DAY EARLIER (2026-08-14, commit 009b5ad) with a header comment naming this exact recurrence as its reason for existing. Fourth row in this log's private-config-read class after the 2026-08-12 loadConfig and 2026-08-14 readForge rows (both resolved into lib/). NOT a straight lift: readConfig degrades an absent/unreadable/unparsable config to `{}` and never throws (host-config.js:15-17, 25-30), while ci-gate-parity's locked contract requires exit 2 on an unreadable config (ci-gate-parity.js:27, AC-20260814-02 Contracts block) — so the paydown is a raw-read variant in lib/host-config.js (or a strictness flag) serving both semantics, not a call-site swap. Same shape as the ac-matrix/verdict manifest-reader row above: the divergence is the ERROR POLICY, not the read (spec specs/20260814/02-doctor-mergeback-fidelity-mechanics.md, runId wf_84fc6287-e19)
- 2026-08-15 duplication spec/scripts/collision-closure.js:119 duplicates spec/scripts/ac-matrix.js:145 — the new collision-closure.js defines a private `walkAllFiles(dir, base, out = [])` (recursive `readdirSync(withFileTypes)`, try/catch → return-empty, recurse on directories, push `path.relative(base, full).split(path.sep).join('/')` for files) doing the same job as `ac-matrix.js`'s private `walkAll(dir, base, out = [])`, added ONE DAY EARLIER (2026-08-14, specs/20260814/01) with the same signature, the same structure, and the same relativization line. Fifth row in this log's private-helper-duplication class after the 2026-08-12 loadConfig, 2026-08-14 readForge, 2026-08-14 manifest-reader, and 2026-08-15 config-read rows — and this repo's own precedent, cited by this very spec's D5, is that the SECOND private copy is the extraction trigger (`lib/glob-match.js` was extracted "precisely because scope-reconcile.js and hotspot.js had grown two private matchers"). NOT identical: collision-closure.js's walker skips no directories and filters on `isFile()`, while ac-matrix.js's differs in its entry handling — so the paydown is one shared `walkFiles(dir, base, {skipDirs})` in `spec/scripts/lib/`, not a call-site swap. Notably the spec imports `lib/file-plan.js` and `lib/glob-match.js` correctly (D5 explicitly forbade a third private glob matcher) and reintroduced the duplication one seam over, at the walker (spec specs/20260814/05-collision-closure.md, runId wf_917cb3b3-98d)
