# Intake — canonical

- Every closed intake row declares its fix kind in the `Fix` column; a repeat Category may
  only close as `prose(<impossibility reason>)` — otherwise a mechanism path is required;
  enforced by tests/intake/intake-discipline.test.js on every suite run.
  (specs/20260805/04-intake-class-discipline.md)
