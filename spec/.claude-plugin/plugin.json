{
  "name": "spec",
  "version": "6.89.0",
  "description": "Full project lifecycle: greenfield genesis (/spec:genesis-architect → /spec:genesis-explore's fresh-UX-research candidate funnel and design pick → /spec:genesis-design ratifying the winner's tokens-as-code) → roadmap briefs with declared surfaces → per-brief /spec:sketch workbench (mock + brainstorm one brief, ratify before planning) → plan → mock-always design (local responsive HTML mocks under a render-critique harness covering the declared theme×viewport matrix; Claude Design demoted to escape hatch) → build → review → per-milestone /spec:release, plus /spec:atlas (the deterministic whole-product design view: journey graph, gap cards, annotation loop), with deterministic workflows, a hook-enforced state machine, per-repo grounding bootstrapped by /spec:init (activation-verified via its deliverable manifest; session grounding included — path-scoped rules, a generated permissions allowlist, run + verify skills), executed-boot and skipped-test gates behind every review verdict, deterministic rule enforcement generated by /spec:enforce, and a closed feedback loop: release flushes host findings into version-stamped briefs, the shipped intake ledger returns fixed-upstream signals through /spec:doctor, and /spec:status — the glanceable derived where-am-I view (roadmap/spec status + skip anomalies) behind one deterministic script shared with doctor and plan's dependency preflight (brief dependencies parsed item-wise, so spec references in a Depends-on header surface as anomalies instead of phantom blockers), and plan's lock step captures session-discovered follow-up work as roadmap briefs before context dies; /spec:review now reconciles the full unscoped diff against the File Plan via scope-reconcile.js — out-of-plan changes are a mechanical hard finding and get reviewed, planned-but-untouched files are a mechanical soft finding, and /spec:build's Final gate surfaces the same signal advisory (rename exemption is conditional — an unplanned file's rename stays a hard finding); the review/release verdict word is now a derived value, never model prose — verdict.js reads a fresh per-iteration evidence manifest (Phase 0 legs append rows, re-executed each fix-delta iteration) plus the workflow return and disposition counts to produce the one word and ledger row printed/appended verbatim, gating the close step on its exit code; CI status flows through the shared ci-query.js wrapper as one more manifest leg (red blocks pre-panel, unavailable never blocks); /spec:release adopts the same derivation under a release profile with production as a required leg, wired on both its fail-fast STOP path and its Phase 4 success path; the ledger row itself is now complete — review rows carry runId (via --run-id, sourced from the wf-review Workflow invocation) plus manifest-derived smoke/testsSkipped, and release rows carry orchestrator-supplied milestone/briefs identity fields alongside manifest-derived staging/e2e/journeys/substrate/production, closing the gap between what review.md/release.md documented and what verdict.js actually emitted; `done` now carries a derived observation sub-state — observe-ci.js (built on ci-query.js) appends ancestry-validated stage:\"observe\" rows to the run ledger at the next /spec:status or /spec:review invocation (no daemon), spec-status.js renders done-unobserved/done-but-red and turns a red observation into the dashboard headline plus the --next top pick as a full oracle-shaped /spec:escape entry, and /spec:escape gains a red-observation derive path that reads the failing run and refuses to record against a spec the run doesn't implicate — closing the window where a green dashboard could coexist with a silently red run of the landed code; spec/INTAKE.md's accepted-findings table now carries a `Fix` column (`mechanism(<path>)` | `prose(<reason>)` | `—` | `pre-contract`) and a repeat-Category row may only close `prose(...)` with a stated impossibility reason — enforced every suite run by tests/intake/intake-discipline.test.js, so a recurring incident class can no longer get a second prose patch instead of a mechanism; observation now slims to a red alarm only (specs/20260807/01) — the done-unobserved certification half is retired by owner ruling (merge-back never pushes, so a historical close can never validate against a run and `pending` was a permanent nag), observe-ci.js checks the default branch's latest completed run once per invocation, attributes a red run by ancestry to the latest-closing implicated spec and clears it only via a later green run's ancestry over the same close commit, and spec-status.js derives observation as n/a|ok|red with nothing rendering for ok; the --pretty dashboard is also bottom-anchored — Roadmap, red-alarm lines, and anomalies print first, with 🎯 Next and the headline verdict line now the final two things on screen; spec-pipeline de-bloat (specs/20260807/03) — observe-ci.js's zero-consumer `--json` flag is deleted (unknown-flag path now covers it), observation.js's stale pending-era comments are corrected to the live winning-row/unobserved contract, the triply-stated autopilot SDK-import exception now lives once in `.claude/rules/spec-pipeline.md` § Review Checks with the other two copies reduced to citations, and scaffold-ledger.md's release-profile row gains an explicit two-consecutive-quarter zero-rows retire condition; claims registry (specs/20260807/04) — every blocking-consequence claim (`**hard**`, `hard finding`, uppercase `STOP`/`MUST`/`NEVER`/`ALWAYS`) in spec/commands, spec/doctrine, and spec/agents now carries an inline `<!-- enforcedBy: <path> -->` or `<!-- unenforced: <reason> -->` marker (grammar bound in shared.md § Doctrine Authoring); claims-lint.js is the sole derivation of the inventory and a dual line-count/orphan ratchet against claims-baseline.json (`--check`/`--json`/`--update-baseline`, wired as the `claims-lint` spec-paths key), gates tests/claims/claims-lint.test.js in npm test, and backs /spec:doctor check 18; review.md is the seed conversion (its 3 live bar-matching claims pointered, restated prose deduped to its named carriers); scaffold-ledger.md and .claude/rules/spec-pipeline.md § Review Checks (baseline-hunk bullet) and § Risk Tiers (claims-lint.js joins the sole-derivation T3 list) register the new gate; explore taste-channel hardening (specs/20260807/05) — /spec:genesis-explore Setup now hard-blocks on a render-capability precondition (Claude-in-Chrome or Playwright MCP), Phase 2 has the session author each position's starter tokens.css from the new spec/templates/design-positions.md field contract before the Sonnet builder fan-out (builders consume by role, append missing tokens, never alter an authored value — enforced by a pre-fan-out commit plus an additions-only `git diff` check), and Round 0's render→screenshot→critique loop is unconditional rather than browser-gated; scaffold-ledger.md's render-loop row is amended (not duplicated) to split gate-in-explore / advisory-elsewhere on the fired promote condition; /spec:enforce baseline carve-out (JJ-20260809, hearwell) — a verified enforcer's own write/fix mode may run ONCE over the tree to establish the baseline its new gate check requires (the `format` unwired-formatter case), conditioned on two-stage verify, pre-existing gate green before + full gate green after, and a `baselineRun` provenance entry in the enforcement manifest; the sole exception to enforce's never-edit-application-source rule, which otherwise stands; /spec:sketch model placement (2026-08-10 ruling on post-Fable-5 field evidence) — sketch mocks are authored by the session model itself in one sequential pass over the brief's gap surfaces, retiring the parallel per-surface Sonnet fan-out as a cross-surface drift source, with an overflow valve past 5 gap surfaces (one sequential Sonnet dispatch over the remainder, session-authored mocks cited as exemplars) and loop edits split taste-bearing-in-session vs mechanical-via-Sonnet; the genesis-design/design-stage half of the same ruling (registry-weighted canon, registry-grounded workers, session fidelity review at exit) is deferred to docs/roadmap/02-design-path-model-placement.md; design ITERATE handoff 🔗 lines now lead with the catalog's searchable keyword in backticks — the title's last segment + story name as space-separated words (e.g. Showcase Chat Thread), since Storybook's Find-components box matches words not slashed paths (verified live 2026-08-10) before the deep link (JJ-20260810 — URLs are painful to paste into an address bar); design-path model placement, genesis/design-stage half (2026-08-10 ruling, specs/20260810/01 — the sketch half shipped in v6.48.0) — /spec:genesis-design now seeds a component **vocabulary** into design/components.json alongside its base-primitive entries: commitment entries (name, purpose, optional boundaries) for every building block the ratified direction/doctrine/winner mocks commit the product to, structurally distinguished from landed entries by absent props/mockRefs; the new components-check.js (spec-paths components-check) is the manifest's schema authority, running fail-closed in genesis-design's commit step and advisory in the design driver's preflight; wf-design workers gain componentManifestPath and ground against the vocabulary exactly like tokens — a bound/imported/authored block fulfils each entry, never a lookalike, and a boundaries contradiction is a fork; /spec:design's two overlapping exit-time visual mechanisms (config-gated VISUAL step, advisory vision-review consult) are retired and replaced by one unified post-gate FIDELITY_REVIEW state (fidelity-reviewed mark, legacy visual-done mark satisfies it on resume) whose findings route into the iteration loop, never a gate; /spec:review's component-manifest leg now treats commitment entries as first-class in its near-duplicate comparison and in author-decision justifications; /spec:atlas's gap sweep adopts the sketch ruling's sequential, exemplar-grounded dispatch contract in place of parallel per-surface dispatch, recorded once in shared.md § Design Atlas as the single doctrine home covering both the sketch and atlas shapes; intake 2026-08-10 (UPWELL-20260810-01/-02, holistic dispositions) — plan.md's AC-shape rule now requires a cross-file Decision (producer → carrier → consumer → display) to pin the chain's terminal observable fed by the production path, with fixture-fed observations ruled non-terminal (the reported Wiring Plan artifact + wiring-matrix review leg was rejected as additive: the chain already lives in the Decision's prose, and the existing ac-matrix leg carries the tightened rule for free), and a resumed review whose branch moved on no longer diffs an unattributable blob — review.md Phase 0 derives the spec's last commit, detaches a frozen worktree when HEAD has moved, and passes wf-review the new frozenRoot arg so the panel reads and diffs the spec's own change while executed legs still run against the tree that ships; terminal-observable ACs (specs/20260810/02, closing the pipeline's dominant escape family — dead surfaces that typecheck/lint/test green but never execute or render nothing) — every Decision that promises a user-observable surface now owes a terminal-observable AC whose test asserts on the observable itself, reached through the spec's own in-repo route, fed by a fixture that is produced by the spec's own producer chain (view-model, assembler, defer-derivation) on realistic wire data, never a hand-authored props object — the anti-pattern is named invented-fixture liveness; the pure-UI TDD exemption is narrowed to appearance only at all four of its homes (build.md, the spec template's AC comment, shared.md § Design Canon, and init.md's generated Test Rules exemplar) — reachability is never exempt; /spec:plan's lock audit widens its existing Goal→Decision→red-capable-AC trace to Decision-level observable promises, the Phase 3 refuters gain one sentence treating a mocked in-repo hop between producer and terminal as top-severity, and /spec:build's `blocked` ruling row now requires a ruling that adds or changes an observable promise to add or update its terminal-observable AC in the same spec edit; no new script, section, or review leg — enforcement rides the existing AC↔test matrix, red-check, and skipped-test reconciliation gates, registered as one scaffold-ledger row; ratchet-mode architecture enforcers (specs/20260810/06) — /spec:enforce's category taxonomy grows two research-backed AI-signature smell classes, `duplication` and `cycle`, enforced as per-host ratchets: Phase 2 research requires candidates to support a baseline/known-violations mode, Phase 3 verify confirms the candidate goes green over its own established baseline, Phase 4 wiring establishes the baseline once and records it in the manifest enforcer entry as `baseline: { path, establishCmd }` with the gate invocation running in no-new-violations form (kept structurally distinct from the existing `baselineRun` write-mode carve-out, which edits source), and Phase 5's gap-hunt gains architecture-smell gaps as an explicit propose-never-auto-author class; the reserved taxonomy enum is now pinned byte-identical across its four homes (grounding-contract.md, enforce.md, wf-enforce.body.js CATEGORIES, doctor.md) by the new tests/enforce/taxonomy.test.js, closing a previously-unpinned drift surface; scaffold-ledger.md registers the ratchet mechanism with a promote/retire condition; per-SHA CI legs (specs/20260810/07, Prax stale-CI review-stop incident) — ci-query.js gains a `--commit <sha>` mode (exclusive with `--branch`, byte-identical normalization) so review's ci leg keys on the reviewed commit's own HEAD instead of the branch's latest run: a completed red run on the exact reviewed commit still hard-stops and derives GATE_RED, but a stale, unpushed, in-progress, or CI-less answer now records informationally and never blocks; `ci` stays in verdict.js's REVIEW_BLOCKING so that mapping is enforced by the sole derivation, not review.md prose; /spec:release gains the authoritative per-commit `ci` leg (RELEASE_LEGS grows to include it) with a bounded 30s/10-minute in-progress re-poll and a mandatory ⚠️ report line whenever CI never delivered a verdict on the release commit; /spec:doctor gains check 19 (advisory) verifying the host's CI workflow actually invokes the configured gateCommand, closing the local-green/CI-red drift class at its root; observe-ci.js's branch-latest keying is untouched (it answers a different question — the trunk's post-merge alarm, never verdict.js's input); command-consistency fix wave (conflicts) (specs/20260810/08) — twelve command-surface audit findings resolved to one authoritative behavior each: plan.md's T2 tier step gates on shared.md § Pipeline Entry (delegation/durability only, else STOP direct-work); review.md splits {root} (Phase 0, working tree under review) from a Phase-4-only {mainRoot} (resolved via {mergeBack} root --worktree before Phase 4 step 1) so merge-back's eleven main-tree consumers never see a worktree path, and corrects its build_base attribution to /git:enter-worktree; build.md's in-place Phase 0 step 4 now writes a durable diff_base: frontmatter field (spec.md template gains the commented field) that review.md's step 1 recovery order consumes between build_base and the branch-name fallback; doctor.md check 11 re-keys stale-implementing to the derived spec/<stem> build branch (build_base-conditional, in-place builds skipped) and adds superseded to the status enum, while check 12 raises its prose-leak threshold to ~1000 chars and enumerates fast-path build/escape/release row exemptions alongside observe; release.md states plainly that releasing is user-invoked by design, never suggested by Next pointers; init.md Phase 6 gains a pending/unrecognized-design genesis arm (warn, proceed, no design block, names /spec:genesis-design); genesis-design.md drops its self-contradicting \"Fable or Opus\" header seat claim (Opus, matching its own Phase 4) and its terminal Next pointer now routes through /spec:atlas before /spec:init; stale-reference sweep (specs/20260810/09) — twelve findings where doctrine pointed at retired mechanisms, scripts that behave differently, or nonexistent `§` headings are corrected or deleted (init.md drops the dead \"workspace\" build knob and the \"T3 checkpoint surfaces\" grounding item; \"refutation filter\" is renamed to execution-grounded verification everywhere it described the live mechanism; ledger `ts` templates in review.md/release.md/escape.md/build.md move to ISO-8601; manifest-check.sh's summary gains a machine `TOTAL=/FAILS=/INERT=` sentinel line that release.md now consumes verbatim; grounding-contract.md gains the `design.copyCatalogs` key), and the new deterministic `citations-check.js` (spec-paths `citations-check`) — a two-line-window, two-word-lookback `§` citation scanner — is wired as doctor check 20 (advisory) so the citation class this audit found can never silently regrow again; drift-duplicate reconcile (specs/20260810/10) — ten restated-rule pairs that had drifted into disagreement are given single homes: shared.md's doctor-rewrite claim is aligned to its § Host Grounding carve-out (wholesale-rewrite ban, `--fix` sanctioned), the dc-extract fail-closed reality replaces the fabricated \"Sonnet fallback\" parenthetical, reviewer.md's and wf-review.body.js's DRIFT_NOTE are reworded to the semantic-backstop framing matching review.md's canonical drift-gate ownership (review.md's false \"already calibrates this\" parenthetical corrected), shared.md's circular model-placement/design-seat pairs collapse to one full statement in § Model Placement with citations elsewhere, the runtime-LLM-rule-check sentence gains the design-harness checklist-walk carve-out, git/merge.md's cleanup sequence and build.md's args alphabet each collapse to one statement, spec-state-gate.sh's header now states its actual four-command gated list, and scaffold-ledger.md's table-breaking blank line is removed — no mechanism changes, prose-only except the hook's comment; review advisory smell lens (specs/20260812/01) — wf-review gains a dedicated Sonnet lens agent (no `agentType`, launched in the panel's parallel barrier, full-scope only) reporting two research-backed AI-signature smell classes, cross-file semantic duplication (counterpart required, code-filtered) and error masking needing cross-file context — output lands only in a new `smells`/`lensFailed` return field on all three return sites, bypassing `findings`/the verify loop/`verdict.js`/the ledger row by construction (fail-open on a null lens result); /spec:review Phase 2 presents smells in their own labeled group after survivor dispositions with one batched keep/drop question, appending accepted findings as dated rows to the host's docs/audit/advisory-findings.md; reviewer.md, init.md's § Review Checks template, and this host's own § Review Checks each carry the carve-out sentence that the two classes are plugin-owned advisory lens output, never a blocking reviewer finding; hotspot-targeted debt audit (specs/20260812/02) — the pipeline gains its slow whole-repo judgment layer, /spec:audit (Opus, named exception in § Model Placement alongside /spec:enforce): Phase 0 reads the host's docs/audit/debt-ledger.md first, suppressing findings already dispositioned rejected for the same class+location and triggering an AskUserQuestion promotion check whenever a class slug reaches ≥2 refactor-brief/rule-row rows, then ingests docs/audit/advisory-findings.md's accepted rows as seed candidates; Phase 1 targets only the top hotspots from the new sole-derivation spec/scripts/hotspot.js (spec-paths hotspot; score = commits × (1 + indentation-complexity) over a --since window, git log --no-renames --numstat, excluding pipelineOwnedPaths and the shared pipeline-noise baseline now factored into spec/scripts/lib/glob-match.js and shared by the hardened scope-reconcile.js); Phase 2 fans out inline parallel Sonnet readers (no new workflow) hunting duplication/boundary-erosion/dead-seam/error-masking with file:line evidence; Phase 3 verifies each finding against live code before it's ever shown; Phase 4 dispositions survivors through batched outcome-phrased AskUserQuestion calls, executing each fate immediately (refactor-brief(NN) roadmap briefs, rule-row doctrine appends, enforcer records for /spec:enforce, or rejected(<reason>)) and appending the ledger row; the audit never edits host source and never gates any pipeline stage; /spec:release's Phase 4 report gains an optional closing offer of /spec:audit at the milestone seam, and spec-status.js is pinned to never suggest it; review self-report integrity (specs/20260813/01) — wf-review's verifyPrompt gains a SANCTIONED self-consistency guard (the MISCITED path's 6.37.0 twin) plus a shared-stateful-substrate fence forbidding a verifier from mutating databases/services/env beyond its own repro file; a new deterministic post-verify `auditKilled` pass resurrects any `killed[]` entry whose evidence is missing or contradicts its `killedBy` label as a survivor flagged `kill-contradicted`, counted additively in `verify.killContradicted` and registered as a scaffold-ledger gate row; the fix-delta full-gate re-assertion contract (CROSS-20260727-01) is now stated in present tense at the wf-review source site that implements it; review.md's Phase 3 close gains a mandatory `git status --porcelain --untracked-files=all` hygiene sweep before the commit, and Phase 0's smoke-leg bullet gains a session-applied inert-falsifier check — a `runtime.inert` declaration contradicted by the spec's own File Plan/diff adding a bootable entry point is now an automatic hard finding naming its remedy (declare the runtime block, re-run /spec:init Phase 1.5, or record sanctioned inertness); gate script mechanics (specs/20260813/03) — claims-lint.js resolves its own shipped corpus from any CWD instead of `process.cwd()` (an upward walk from `__dirname` to the first dir holding `spec/doctrine/claims-baseline.json` restores full repo-root semantics in this repo and every dev/marketplace checkout; an installed-plugin layout with no such dir falls back to a `PLUGIN_HOME`-anchored mode, with `enforcedBy:` pointers into unshipped trees like `tests/…` counted informationally in a new `skippedPointers` key rather than flagged stale) — doctor check 18 now actually scans something everywhere it runs, closing PRAX-20260813-04; scope-reconcile.js expands File Plan glob rows (`*`/`?`/`[`) via the existing `lib/glob-match.js` matcher so a planned codegen output stops double-reporting as both `outOfPlan` and `unrealized`, with realization counted only against non-excluded changed files so pipeline-owned noise can never fake a row realized, closing PRAX-20260813-07; verdict.js makes `--workflow` optional on the review profile exactly as narrow as review.md's documented pre-panel GATE_RED invocation — a green-and-complete manifest with no `--workflow` still exits 2 naming the remedy, so a panel-less CLEAN stays impossible — with every `workflow.*` dereference guarded across all three call sites and a partial no-workflow ledger row, closing JJ-20260808-01; doctor check 12 learns that a review row's `runId` is optional exactly when its `verdict` is `GATE_RED` (a pre-panel hard stop structurally has no run id), never a blanket row-class exemption, since an in-workflow red iteration still mints one and keeps it; durable verification qualifiers (specs/20260813/02) — three run-time qualifiers that used to live only in transient console output now survive into the durable artifacts: review.md Phase 0 step 6 widens the skip-reconcile leg's observed string to `skipped=<N> sanctioned=<M>` (legacy `skipped=<N>` still parses, `M` absent reads as 0), and verdict.js derives `row.testsSkipped` as `{total,sanctioned,unsanctioned}` instead of an undifferentiated scalar (undeclared skips are unsanctioned by definition, the conservative reading); release.md/verdict.js gain the distinct release-profile word `CLEAN-with-qualifier` (CLEAN-family, exits 0, promotable exactly as CLEAN) for a milestone whose only gap is a `ci` leg that structurally never delivered a verdict (`unavailable`/`in-progress`) — the word threads through release.md's enum, its Phase 4 report headline and ⚠️ slot line, its documented ledger row (which also gains the previously-undocumented `verdict` key verdict.js already emitted), and the standalone final exit line (now a CLEAN-family check); and specs may declare a non-test oracle per AC via `[oracle: <manifest leg>]` (sibling syntax to `[env:]`, a manifest leg name only — never free-form command text, the laundering route the panel closed) — review.md's step 5 coverage matrix and the Drift gate section (both driftScript and no-driftScript branches) treat a declared oracle as covered-by-declaration with a named warning, hard when the oracle leg is red or absent; scaffold-ledger.md's Verdict-derivation and skipped-test-reconciliation rows record the new shapes and their promote/retire conditions; plan-lock obligation→carrier sweep (specs/20260813/04) — plan.md's Phase 4 lock check gains one obligation→carrier sweep spanning four corroborated shapes (a Decision naming a file by path owes a File Plan row, a Decision ordering a persisted rendered artifact owes a Contracts/schema row, a spec whose tests import CREATE-d modules owes those modules' factory signatures pinned in Contracts, an AC whose value is computed by a helper owes that helper's own ground-truth carrier), registered as a new scaffold-ledger gate row alongside the existing Goal→mechanism-audit and Terminal-observable-ACs rows; plan.md's File Plan drafting guidance gains the row grammar rule (every touched file its own row, no bundled second-file edits, closing UPWELL-20260725-01) and its AC-shape bullet gains the library-default split (behavioral library-mechanism pin plus a separate shipped-config-echo assertion, never only the library's resolved internals, closing PRAX-20260719-01); init.md's Deliverables section preamble gains a generation-wide authoring rule — generated prose about a volatile enumerable fact (routes, table lists, package inventories, token homes) must name the derivation rather than inline the enumeration itself — placed upstream of every generation phase including Phase 1's skill authoring, the founding incident's own artifact class, closing PRAX-20260813-06; workflow correctness repairs (specs/20260813/05, audit Class F) — spec-design-driver.js's design gate resolution moves from raw unsubstituted pass-through to a top-level-only `&&` leg splitter (depth/quote-aware: `&&` inside balanced parens or quotes never splits, unbalanced input passes through unsplit) that drops any leg still carrying an unresolved `{...}` placeholder, logging each drop into the emitted step text, with an optional `design.gateCommand` config override bypassing the derivation entirely; when every leg drops (or `gateCommand` is empty) the driver emits the literal `__UNGATED__` and wf-design returns the new `stage: 'complete-ungated'` (never `'complete'`) for it, closing the design gate that could never pass on `{testDirs}`-using hosts; review.md's own gate leg gains the same `{testDirs}`-to-glob-form substitution build.md already performs before the leg runs (cited, not duplicated), an unresolvable placeholder now naming the token as an `unavailable` red leg rather than executing the raw placeholder-bearing command; wf-review.body.js's `reviewerPrompt` now appends the full AC/File-Plan/Contracts/Decisions coverage emphasis (incl. the drift-note semantic-backstop text) to every reviewer prompt unconditionally instead of gating it behind the second seat that never runs on single-reviewer (majority-path) panels, and its verifier prompt's closing rule now permits `git log` alongside `git status` — the step-1 command it previously contradicted — and downgrades the cleanup guarantee to best-effort; the red-check prompt now mandates a per-file `AUDIT_GREEN:`/`AUDIT_RED:` sentinel pasted into a new `RED.sentinels` array cross-checked against the reported red/green state (mismatch or missing sentinel = unverified-red), with the sanctioned-green-carriers hand-built literal exempted via `sentinels: null`; the gate-repair loop wf-build and wf-design each inlined separately is extracted into one shared `fragments/gate-loop.js.frag` (byte-identical spliced region, the `gateCmd` interpolation name preserved for tests/workflow-guards.test.js) carrying the repair ceiling as one named `REPAIR_CEILING` constant and, for the first time, wf-design also gains the anti-oscillation history, phantom-failure hardening, and deviations-sidecar accumulation build already had, plus a new `exhaustedBy` cause (`agent-died`/`no-attributable-failure`/`oscillation`/`ceiling`) on every gate-exhausted return; three named trust-boundary asserts (`assertGateArgs`, `assertBatchKinds`, `assertResolutions`) throw naming the offending key/value at the args boundary instead of crashing cryptically or silently misrouting a batch; scaffold-ledger.md registers the four previously-unregistered guards this spec's mechanisms depend on (`MAX_VERIFIES`, the shared repair ceiling, the red-check sentinel cross-check, the `__UNGATED__`/`complete-ungated` guard) with promote/retire conditions. The shared gate probe now runs the host gate command as a standalone `( set -e; … )` subshell and prints its pass sentinel from a separate `$?` test — the previous `( … ) && echo` shape reported only the last statement of a `;`-joined gate, so a failing early leg still read as green, and folding the fix back onto `&&` (or into an `if` condition) silently disables the `set -e` because errexit is ignored for the non-final command of an AND-OR list.; report renderer (specs/20260813/06-report-renderer.md) — spec/scripts/report-render.js (spec-paths report-render) becomes the sole authority on the end-of-run console report skeleton: a command writes a slots JSON file (outcome/bullets/pins/warns/blocks/artifacts/found/next) and prints the script's stdout verbatim instead of freehanding the shape at report time; next is required and takes one of three kinds — command (a single line that must not end in `?`), status-verbatim (spec-status --next's own output passed through byte-identical, so the report may legitimately end with the script's own 🎯 lines), or none (a stated reason rendered as \"nothing needs you — {reason}\") — closing the loop is now mechanically enforced rather than a prose habit, and a contract violation exits 2 naming the offending slot with the recovery rule stated in the script's header: fix the slots file and re-run the renderer only, never re-run the command's phases, since a render-time failure follows side-effecting work already done; shared.md § Console Output Style is edited in place to name the script as render authority, cross-reference the 📌 announce-line already defined in § Question Style instead of redefining it, and give every failure close the same two-line shape via the script's `outcome.anchor: 🚫`, staying within its pre-change 44-line bound by deleting the now-script-owned template-mechanics prose; the six workflow return envelopes each gain a pinned runId plus the audit's missing silent-degradation carriers — wf-review findings gain a required plain-English impact line (feeding the report's finding display; identifiers stay in claim/evidence for the ledger) and review.md's documented return-shape string is synced to match; wf-build and wf-design gain agentsFailed; wf-panel gains agentsFailed and now throws before aggregation when fewer than 3 proposals survive filtering; wf-research gains verifyFailed (a dead currency verifier no longer silently reads as a fresh answer) and alsoConsidered for cap-cut options; wf-enforce's notes becomes required (feeds the report's mandatory ⚠️ fallback line) while wf-build's dead, zero-reader GATE.summary is dropped from the required set (files[].summary, which repair prompts actually consume, stays required); scaffold-ledger.md registers the renderer as an already-promoted gate row. Command-side adoption of the slots mechanism across the pipeline's commands is spec 07's; return-envelope corrections (specs/20260813/06a-return-envelope-corrections.md) — a post-build consult refuted two of spec 06's premises before spec 07 built 19 command reports on top of them: the `runId` echo spec 06 added to all six workflow return envelopes is repealed (every `runId: args.runId,` assembly evaluated to `undefined` — the harness delivers the run id only in the caller's Workflow tool result, never in `args` — so review.md's return-shape strings and its now-false `runId` sentence are corrected too, while the orchestrator-sourced ledger row and report line are untouched); `const GATE` is now defined exactly once, in `fragments/gate-loop.js.frag` beside its sole reader, repairing the wf-build/wf-design schema drift spec 06's wf-build-only D7 wording created; and wf-research's option cap is hoisted into a named `capOptions(menus)` that cuts non-minority options worst-rank-first and protects `is_minority` options from silent truncation, recording each cut as a dimension-attributed `{dimension, label}` in `alsoConsidered` (was a flat pooled label array). 6.68.0 settles a doctrine/script contradiction the self-hosted review of spec 20260813/06 surfaced (INTAKE JJ-20260814-01): review.md forbade writing `CLEAN` on any row whose `survived` was non-zero, while verdict.js derives CLEAN as soon as every survivor is dispositioned — so the sanctioned all-waived close printed exactly the forbidden row. The script was right; the hole was always CLEAN with an *undispositioned* survivor. review.md now states that invariant (and keeps the real prohibition: never hand-write the verdict word), and tests/clean-row-survivor-consistency.test.js pins it by EXECUTION in both directions — a fully-waived survivor closes CLEAN with its disposition recorded, an undispositioned one derives HARD_FINDINGS and cannot reach the Phase 3 close — so the sentence can no longer drift from the script it describes. Command report conformance (specs/20260813/07-command-report-conformance.md) — every non-exempt command (all spec commands but status.md, plus all three git commands) now carries a fenced ```report example plus the report-render invocation, closing audit Class A (8 of 16 spec commands ended off-contract, findings commands never staged the fix, failure paths ran ungoverned free prose) and Class D2 (the git plugin was a contract-free zone): review.md's non-CLEAN arm closes `Next: /spec:build {specPath} — fix the N hard findings` and re-prints the one-line verdict contiguous with the close after merge-back; escape.md leads `✅ **escape logged — …**` and closes with the step-6 prevention-delta command else the spec-status placeholder; audit.md's Next is unconditional (enforcer fate routes to /spec:enforce, else the placeholder); atlas.md gains a terminal report block with a Next arm; init.md collapses its identifier dump to two plain lines and closes with the spec-status placeholder rather than a redundant `Next: /spec:enforce`, since Phase 8 already auto-chains it; release.md's optional 🧹 next line becomes an unconditional branched Next; enforce.md prints the resolved gateCommand verbatim in its Next and retires its bespoke 🔒 glyph to a plain bullet; review.md's bespoke 🔍 glyph retires to a 📦 artifacts entry; every STOP/failure path in the migrating set adopts the shared two-line STOP shape (`outcome.anchor: 🚫`, a required `next` naming the remedy) via the renderer — git merge's dirty-tree STOP stages `/git:commit` (recommended) with `git stash -u` as the same-line alternative, enter-worktree ends with a report block instead of \"Done.\", and commit.md, which carried no report shape at all, gains a net-new minimal terminal template (`✅ **committed — {subject} ({N} files)**` closing `Next: nothing needs you` or `/git:merge` on a worktree branch); each git command file opens its report section with a one-line contract citation instead of a new shared-for arm; a new tests/consistency/report-shape.test.js pins the whole tagged surface at once — every report block's first-line anchor pattern, `Next:`-or-placeholder close, and closed anchor set, plus per-file report-block-and-invocation presence — registered in scaffold-ledger.md as a gate retiring only with the renderer contract itself; the pre-existing genesis-design ending pin is retagged AC-20260813-07-5 and stays green; claims-baseline.json is re-stamped for the 18 doctrine files' line-count deltas in the same commit. Question contract (specs/20260813/08-question-contract.md) — the wired `question-style-gate.js` hook enforced consequence-bearing options and reasoned recommendations at ask time, but the workflow schemas that manufacture fork/menu questions carried neither field and four doctrine sites taught the opposite it blocks: wf-panel's AGGREGATE_SCHEMA now requires `consequence` on every `hard_fork_list[].conflicting_positions[]` item and `recommended_first_reason` on every `hard_fork_list[]` entry, with the aggregator prompt gaining the ten-second cold-test clause; wf-research's OPTION_SET_SCHEMA now requires `why_recommended` and its prompt's \"do NOT lead the user\" symmetric-menu instruction is replaced by a labels-stay-neutral/ranking-and-why_recommended-carry-the-recommendation rule; wf-build's and wf-design's `blocked` escalation schemas now require `{option, consequence}` items with a sharpened `recommendation` field description; every question-building consumer site (genesis-architect.md's menu- and fork-question steps, genesis-design.md, genesis-explore.md, genesis.md's field-shape description, build.md's blocked-escalation step, spec-design-driver.js's blocked step text) names the new fields so none goes unread, closing the same unconsumed-required-field defect spec 06's GATE.summary was retired for; and doctrine sites that contradicted the wired hook are corrected in place — review.md now recommends the evidence-implied disposition in batches of ≤4, doctor.md batches patch approvals up to 4 instead of forbidding batching outright, build.md's \"(Recommended)\" gloss on a supported pick is mandatory rather than optional, genesis-architect.md's blanket \"neutrally worded\" clause narrows to a stated vision/taste-dimensions carve-out, enforce.md's repo-wide format write-mode pass and audit.md's brief-writing fate each gain a confirm/preview step before the irreversible-ish action, and the worst B6 question-teaching examples (init.md, sketch.md, atlas.md, release.md, plan.md) are rewritten to gloss-in-plain-English plus recommended-first plus per-option consequence; scaffold-ledger.md registers the schema-level fields as a gate row retiring only if the hook itself retires; claims-baseline.json is re-stamped for the doctrine files' line-count deltas in the same commit. Model placement mechanics (specs/20260813/09-model-placement-mechanics.md) — the panel aggregator (wf-panel's single highest-judgment workflow seat) moves from a pinned Opus seat to Fable-first with an Opus fallback, closing the doctrine-vs-code contradiction the 2026-08-13 style audit found (shared.md carried both \"Fable-first with Opus fallback\" and an \"Opus seat\" carve-out, and the code took the carve-out): fragments/dispatch.js.frag gains a `MODEL_FALLBACK = { fable: 'opus' }` single retry, and the pre-existing agentType-not-found retry now recurses through `dispatch()` itself instead of calling `agent()` directly, so a fable-plus-unknown-agentType seat's worst case genuinely composes into two retries (general-purpose, then opus) instead of the second failure propagating past an unreachable fallback; wf-panel's aggregate seat now runs `model: 'fable', effort: 'high'`, and every doctrine site that still asserted the old Opus seat — wf-panel's own meta/phases text, shared.md § Model Placement's Exceptions bullet, genesis-architect.md's and genesis-design.md's model checklists — is corrected in the same spec, closing the four-site doctrine-vs-code gap a code-only fix would have reintroduced. Every `agent()`/`dispatch()` call site across the six workflow bodies now declares `effort:` explicitly alongside `model:` — a closed three-band set (`low` mechanical/transcription seats: gates, extraction, red-check, currency probes, wf-design's expansion workers; `medium` analysis seats: proposers, researchers, reviewers, verifiers, wf-build's implementation workers, enforce research; `high` the panel aggregator's synthesis/judgment seat) replacing what was previously effort set explicitly at only 3 sites, always `'low'`, with judgment seats inheriting an unstated default; the five pre-existing `'low'` sites stay byte-identical. shared.md § Model Placement's \"uncorrelated model\" review-independence claim — never literally true, since this pipeline's reviewers and verifiers are all Sonnet — is narrowed at both loci where it appeared (the headline sentence and its ~15-line-later restatement) to one literal sentence: review independence comes from blind-to-author dispatch and execution-grounded verification, never model diversity. § Question Style gains an underivable-fork clause: a genuine fork with no derivable default from the codebase, session, or ledgers, AND an empty payload `recommendation` field (spec 08 D3), now consults the Fable retainer for a decision brief (never a decision) and appends one row to the host's `docs/consults.md`; audit.md's Phase 0 mines that ledger, and any fork class reaching ≥2 rows is promoted through `/spec:audit`'s existing closed fate enum (`rule-row`/`enforcer`/`refactor-brief`) — no new fate, no enum change. `tests/helpers.js`'s `extractFn` gains async-keyword preservation, the prerequisite fix that makes `dispatch`'s sanctioned source-shape test mode usable at all — `dispatch` is this repo's only top-level async extraction target, and evaluating it via `evalFns` threw a SyntaxError until this fix landed. scaffold-ledger.md registers the model fallback, the explicit effort bands, and the Fable consult ledger as three new rows with promote/retire conditions; claims-baseline.json is re-stamped for the doctrine files' line-count deltas in the same commit. Host capabilities (specs/20260813/10-host-capabilities.md) — closes audit Class C's silent-failure family (a command assumed GitHub/a universal skip-count format/pnpm-shaped monorepos/Storybook-shaped previews and went inert silently on a host where the assumption missed): spec.config.json gains one closed `capabilities` block (`forge`, `skipReportPattern`, `ciPoll`), documented in grounding-contract.md's new § Capabilities and written by /spec:init's detection pass (forge from the origin remote + `gh` availability; skipReportPattern proposed from the detected test runner and confirmed by the user — probe silence is never evidence, since many runners print skip lines only on nonzero skips); ci-query.js and observe-ci.js gate on `capabilities.forge`, printing the canonical `unavailable — no supported forge adapter` line and exiting cleanly on \"none\" while an absent block keeps today's dynamic gh probing unchanged; review.md's and release.md's skip-capture legs drop the false \"every mainstream runner prints skip counts\" claim and report `unavailable — host runner declares no skip format` when no pattern is declared or matches, instead of assuming zero; doctor.md folds an undeclared-capabilities nudge and a forge:\"none\"-but-GitHub-looking staleness hint into check 2, with no new numbered check; verdict.js's review profile now derives `CLEAN-with-qualifier` (the same word/exit family spec 20260813/02 gave the release profile) when the disposition branch would return CLEAN but a declared observation leg is unavailable, closing the gap where honest `unavailable` legs were silently swallowed into plain CLEAN; release.md's CI poll loop reads `capabilities.ciPoll` with the existing 30s/600s constants as its unchanged default; init.md's monorepo trigger generalizes from `pnpm-workspace.yaml` to \"more than one test-collecting package/module\" (filenames demoted to a parenthetical example) and its component-catalog detection generalizes from Storybook/Widgetbook by name to \"a component-preview host\"; wf-review's verifier runner-discovery, wf-design's and spec-design-driver.js's preview-host phrasing, and genesis-explore.md's render-capability STOP all generalize from enumerated tool names to capability-shaped language, with the STOP's pinned hardness retagged (not weakened) in tests/model-placement.test.js; this repo's own .claude/spec.config.json dogfoods the block (`forge:\"github\"`, a skipReportPattern for node:test's `ℹ skipped N` line); scaffold-ledger.md registers the capabilities block as a structural, retire-never-while-multi-host mechanism. AC-matrix mechanization (specs/20260814/01-ac-matrix-script.md) — review.md Phase 0 steps 5-6 (the AC-line lint, AC↔test coverage matrix, `[oracle:]`/`[env:]` handling, and skipped-test reconciliation) were hand-executed from prose every review session, a leg that drifts per session and per model exactly as shared.md § Rule Enforcement's own strict-downgrade class predicts; the new dependency-free spec/scripts/ac-matrix.js (spec-paths `ac-matrix`) becomes their sole derivation, appending its own `ac-matrix`/`skip-reconcile` manifest rows in the same byte-identical observed format so a hand-transcribed stale row is now structurally impossible; spec/scripts/lib/file-plan.js gains an additive Layer-column exposure (existing callers unchanged) so the script can identify File Plan tests rows without a second table walker or a tests/-prefix heuristic; review.md's steps 5-6 shrink to invocation lines, keeping only the retained doctrine — the mode split, finding severities, the `[oracle:]`/`[env:]` consequence sentences (now `enforcedBy: spec/scripts/ac-matrix.js`), the Drift gate section's carve-outs citing the script as executor, and the gate-completes-first sequencing note; ac-id-lint.test.js and verification-qualifier-durability.test.js are retagged onto the script as the new source of truth (lift-and-execute technique preserved); scaffold-ledger.md registers the script (advisory-equivalent, promote/retire conditions) and amends the skipped-test-reconciliation row's executor attribution; claims-baseline.json is re-stamped for review.md's and scaffold-ledger.md's line-count deltas in the same commit. Doctor/merge-back/fidelity mechanics (specs/20260814/02-doctor-mergeback-fidelity-mechanics.md) — four small prose-executed algorithms become deterministic: doctor.md check 19 shrinks to an invocation of the new spec/scripts/ci-gate-parity.js (spec-paths ci-gate-parity), which now owns the gateCommand segmentation/substring-parity algorithm and its inapplicable sentinels; merge-back.sh gains a print-only `branch-for <spec path>` subcommand (special-cased before the generic flag loop, no git ops) as the sole owner of the `spec/<stem>` build-branch rule — doctor.md check 11 and git/commands/enter-worktree.md step 1 now invoke it instead of restating the rule freehand, and git/commands/commit.md's reverse parse gains one clause naming it as the rule's owner; doctor.md checks 16 and 17's legacy-migration arms (the missing per-surface-casing-ownership label, the stray roadmap/deltas/ dir) now gate on the config's generatedBy version (< 6.7.0 / < 6.18.0 respectively) instead of firing forever, reporting as ordinary drift on a current grounding; check 12 reorders so the JSON/shape checks are the primary signal and the ~1000-char line-length check demotes to an advisory tripwire, never a standalone broken finding; fidelity-check.js gains root-binding over-claim detection — a root-bound skeleton whose pass fully satisfies some obligation-bearing child regions while fully missing others now gets one prepended `over-claim:` diagnosis line naming the true cause, and design.md's incident-shaped multi-line chrome-misdiagnosis narration collapses to one class-level sentence; scaffold-ledger.md amends the CI-gate-parity row to a script pointer and adds rows for `branch-for` and the fidelity over-claim diagnosis; claims-baseline.json is re-stamped for the doctrine files' line-count deltas in the same commit. testDirs glob-form resolution (INTAKE JJ-20260815-04) — build.md Phase 0 step 3 now states at the point of resolution that `{testDirs}`/`{scopeDirs}` substitute in the form the host's runner actually executes (for `node --test`, the glob form `node --test 'tests/<dir>/*.test.js'` — a bare directory reports MODULE_NOT_FOUND and exits non-zero on Node 26, a red gate leg with nothing wrong under it, reproduced twice in this repo), which makes review.md's cited-not-duplicated reference to build.md true instead of a citation to a rule its target never stated; pinned by tests/testdirs-glob-resolution.test.js. Suite baseline (specs/20260814/03-suite-baseline.md) — the repo's sanctioned-red test set becomes a checked-in artifact, closing the 2026-08-14 escape on specs/20260813/10-host-capabilities.md where a shared script's changed return value broke five green-at-base pins outside the spec's scoped gate, invisible to gate, panel, and Gotcha: the new spec/scripts/suite-baseline.js (spec-paths suite-baseline) is the sole derivation of expected-vs-observed suite-failure comparison, reading .claude/suite-baseline.json (host-owned, absent = empty set, an optional flaky:true row exempting it from all drift directions) and comparing it against a bare testCommand run's parsed failing-test trailer, both as a plain NEW-FAILING/FIXED-NOT-REMOVED --check and as a pre-image-attributed --snapshot/--check --pre mode that answers \"did this build cause it\" rather than \"is this expected\"; review.md Phase 0 gains a recorded-but-not-required suite leg (advisory — verdict.js untouched, suite excluded from REVIEW_LEGS/REVIEW_BLOCKING by construction) that surfaces drift as one mechanical finding, while /spec:build gains a real blocking consumer with exact attribution — Phase 0 snapshots the pre-image at the diff_base write (never on a resume), Phase 4 runs one --pre check after the gate is green, and only preNewFailing (this build's own breakage) enters the repair path; arrived-broken drift and baseline hygiene gaps warn loudly but never block; .claude/rules/spec-pipeline.md's Test Rules pointer replaces the stale hardcoded pin count with the artifact; scaffold-ledger.md registers the mechanism as one row with two consumers and independently-tuned promote/retire conditions; 6.76.1 disambiguates that same Phase 4 bullet — a missing pre-image is the caller's fallback (re-invoke --check with no --pre flag at all), because suite-baseline.js deliberately exits 2 rather than degrading silently to a baseline-only comparison, so exit 2 now unambiguously means a build-config defect. Collision closure (specs/20260814/05-collision-closure.md) — the plan-time collateral-damage sweep stops being hand-executed prose: the new spec/scripts/collision-closure.js (spec-paths collision-closure) is the sole derivation of plan-lock's paths-leg (File Plan paths tested against the derived test roots, D12-tiered into likely/mentions by deepStrictEqual proximity) and literals-leg (planner-supplied stems tested against the whole repo, excluded via lib/glob-match.js's pipelineOwnedGlobs) collateral-damage listing, advisory only, never blocking — execution adjudication stays owned end-to-end by specs/20260814/03's build Phase 4 whole-suite check; plan.md's Phase 4 obligation→carrier sweep replaces its hand-executed stem-level-grep instruction with a script invocation line (enforcedBy: spec/scripts/collision-closure.js), and .claude/rules/spec-pipeline.md's colliding-pin Gotcha collapses to the two live mechanisms plus a third cited recurrence (the spec-paths key-set collision that landed out-of-plan and had to be waived at review); scaffold-ledger.md registers the mechanism with independent per-leg retire conditions; claims-baseline.json is re-stamped for the doctrine files' line-count deltas in the same commit. Recurrence carriers (specs/20260815/01-recurrence-carriers.md) — two ledgered-but-recurring defect classes stop depending on authoring-time memory: review.md's advisory smell presentation drops its keep/drop `AskUserQuestion` (a fork review.md itself declares consequence-free, which the question-style judge correctly bounced three times) in favor of derive-and-announce — the session prints one plain-language line per smell then hands the array to the new spec/scripts/advisory-append.js (spec-paths advisory-append), the sole review-time writer of docs/audit/advisory-findings.md, which appends, dedupes (still-open rows suppress; RESOLVED-only matches re-append, preserving the recurrence signal), creates the ledger with its header on first append, and 📌-announces the auto-keep; and the private-config-read class (four live offenders, two never ledgered) is paid down — lib/host-config.js gains `readConfigStrict` (throws on absent/unreadable/unparsable, returns a successful parse verbatim with no shape coercion), all four call sites swap to the lib with exit codes/degrade paths/remedies preserved, and a new suite pin (tests/host-config/config-read.test.js) walks spec/scripts/**/*.js with a pure fs read (never a shell grep — fidelity-check.js carries a stray NUL byte that makes grep misclassify it binary) asserting no line pairs spec.config.json with readFileSync/path.join outside a narrow display-join exemption; review.md's `{M} accepted` retargets to `{M} recorded` throughout, and scaffold-ledger.md's Review-smell-lens row's RETIRE condition re-anchors from zero-accepted-findings (meaningless once acceptance is automatic) to emission volume and audit-side fate, with two new rows for advisory-append.js and the config-read closure pin. Build note: the spec's literal 6.77.0 target was already taken at HEAD (6.78.0 shipped first); this bumps to the next free version, 6.79.0, per the version-bump-discipline Gotcha. 6.80.0 closes the raw-NUL class (JJ-20260816-01): a correct in-memory NUL delimiter spelled as a raw 0x00 byte in source instead of the escape '\\x00' turns a text file binary for every byte-oriented tool — BSD grep answers with a silent exit-1 \"no matches\" even for hits before the byte (which mismeasured spec 20260815/01's plan-time census and forced its mid-build D10 amendment), and inside git's ~8KB sniff window it renders a source file as Bin in its own review diff. Because the consumer surface is unbounded ad-hoc sweeps, the carrier is producer-side: tests/tracked-text-purity.test.js byte-scans every git-tracked file and fails on any raw NUL, with no allowlist until a deliberately binary fixture earns one. At-risk pins (specs/20260815/02-at-risk-pins.md, escape wf_e1da0ea6-94c, INTAKE JJ-20260815-03) — the scoped gate's compensating derivation: scope-reconcile.js gains an additive, language-agnostic `atRisk` field (path-stem matching — the changed file's repo-relative/extension-stripped/last-two-segment forms scanned across a repo-walked, `testGlobs`-filtered candidate universe, excluding File Plan tests-rows resolution and the changed set itself), byte-compatible with every existing exit code and JSON field; verdict.js's REVIEW_LEGS grows to seven with `at-risk` required-but-non-blocking on full scope (fail-closed presence — an absent leg derives UNVERIFIED — exactly reconcile's standing) and excluded on fix-delta, with the CLEAN-with-qualifier unavailable-leg set widening to include it; review.md's Phase 0 step 3 gains the `at-risk` leg — reads scope-reconcile's `atRisk` field and, when non-empty, runs the host's `testCommand` against the at-risk files, turning a red run into one mechanical hard finding (\"pins that live outside the scoped gate failed on this diff\") that enters ordinary Phase 2 disposition, never a pre-panel stop; build.md's Final-gate advisory scope check now also names the `atRisk` count, visibility only, no build-time run; init.md's worked-example config documents the new optional `testGlobs` key (default covers dir-rooted and colocated test conventions); scaffold-ledger.md registers the leg with its promote/retire conditions. This spec's own self-application redded four suites outside its own gate (the SIX_GREEN verdict fixture, the byte-exact REVIEW_LEGS pin, the qualifier-manifest fixtures, and the smell-lens green-manifest fixture) — each retargeted in place, unweakened, per the executed adversarial sweep, the same class this spec fixes for every future host. Build note: the spec's literal 6.78.0 target was already superseded at HEAD (6.80.0 shipped first); this bumps to the next free version, 6.81.0, per the version-bump-discipline Gotcha. AC-matrix fail-closed (specs/20260815/03-ac-matrix-fail-closed.md, INTAKE JJ-20260815-01/-02) — two accounting holes in ac-matrix.js close: every malformed AC bullet now increments `uncovered` in both drift modes (previously dropped from the coverage denominator entirely, so a waived `malformed-ac` finding could still read `uncovered=0`), and skip-reconciliation's `[env:]` sanction lookup now falls back to the AC's owning spec (derived from the AC-ID) whenever the AC isn't declared in the spec under review — a same-spec hit still wins outright, and every owning-spec edge (missing date dir, ambiguous/missing file, missing `[env:]`) fails closed to today's `unsanctioned-skip`; the warning line for a cross-spec sanction now names the declaring file. review.md's step 6 sanction sentence is corrected to describe both loci truthfully. Observed grammars, exit codes, and finding classes are unchanged. Build note: the spec's literal 6.79.0 target was already superseded at HEAD (6.81.0 shipped first); this bumps to the next free version, 6.82.0, per the version-bump-discipline Gotcha. Runtime leg, shutdown half (specs/20260815/04-runtime-shutdown-leg.md, INTAKE JJ-20260815-05) — the runtime leg certified only \"this program starts\"; smoke.sh already sent the host's declared `runtime.stopSignal`, but only inside its EXIT trap, where the leg's verdict was already fixed and the observation discarded — the stranded pidfile lock (wf_1d6e7652-ec3) rode two CLEAN reviews on exactly this gap. smoke.sh now sends the stop signal to the boot process group after readiness (and any `--seed` seedCommand), polls liveness up to `runtime.stopTimeout` (optional, default 30s), and `wait`s for the exit status: still alive past the timeout is `shutdown-hung` (SIGKILL then exit 6), an exit status outside `runtime.stopExitCodes` (optional, default `[0]`) is `shutdown-unclean`, both `__SMOKE_FAIL__`-sentineled; the pass line now reports the shutdown elapsed time alongside boot. A host whose service deliberately re-raises the signal after cleanup (exiting 128+signum) declares `stopExitCodes: [143]` — the default rejects it because status alone can't distinguish that idiom from default-action death, and default-action death is the recorded escape. shared.md § Runtime Verification and review.md's smoke-leg bullet state the shutdown half; grounding-contract.md's runtime section and init.md's worked example document the two new optional keys (a genuine contract change — every host's stamped `contractHash` goes stale on landing, the drift warning is the designed notification); scaffold-ledger.md's existing boot-smoke-leg row is updated in place, re-anchored to boot+shutdown outcomes, with the deferred `runtime.stoppedCheck` post-stop probe named as a reopen. Exit 6 rides `verdict.js`'s existing non-zero-non-4 red-smoke semantics unchanged — no verdict-surface change. JJ-20260815-05 is fixed at this version. Env preflight (specs/20260815/05-env-preflight.md, INTAKE JJ-20260815-08, salon-os ×2 runId `wf_e4778d03-81b`) — an unset suite-gating environment variable used to reach the gate as an ordinary red, indistinguishable from broken code, burning a full repair round build cannot possibly win: spec.config.json gains an optional `testEnv` array (`{var, provision}` rows), written by /spec:init's § Test Rules step (a cross-phase write alongside the existing prose derivation, precedented by Phase 6's genesisStackDescriptor/design.rulesManifest writes); the new spec/scripts/env-preflight.js (spec-paths `env-preflight`) reads it via lib/host-config.js's readConfigStrict (specs/20260815/01) in two modes — a default presence check (unset or empty-string counts as missing, exit 1 naming each miss's provisioning command) and a `--rules <path>` registry↔prose cross-check for doctor (exit 3 naming a declared variable absent from `## Test Rules`, or the heading itself absent); build.md's Phase 0 step 3 runs it immediately after resolving the gate command and STOPs on exit 1 before Phase 1, the red-check probe, or any repair loop — an unprovisioned environment must never enter repair; design.md's § Rules gains the identical preflight before the `wf-design` invocation step, since wf-design splices the same gate-repair loop build does; doctor.md check 6b's env clause is replaced by the `--rules` invocation, with the reverse direction (rules name vars, config has no registry) staying a model-judged legacy-drift flag; grounding-contract.md's optional-keys list gains `testEnv` (a deliberate contract-hash change, pre-answered by the spec so no build-time consult was needed); scaffold-ledger.md registers the mechanism as a gate row entering directly (not ADVISORY) on the measured-incident precedent, scoped to hosts that opt in by declaring the registry. JJ-20260815-08 is fixed at this version. Red-check load attribution (specs/20260815/06-redcheck-load-attribution.md, INTAKE JJ-20260815-07, hearwell 2026-08-14) — a red-expected test file whose import crashed at load time reported runtime-red while executing zero assertions, and a spec's first act is overwhelmingly a new export, so the vacuous case was the common case; measured consequence: deleting the six words implementing a spec's headline guarantee left 9/9 tests green through a two-reviewer CLEAN panel. `RED.sentinels` entries now carry a required `assertionsRun` count; `crossCheckSentinels()` in wf-build.body.js accepts a runtime-red observation as satisfying a red expectation only when `assertionsRun ≥ 1` (0/absent/non-numeric fails CLOSED as `not-collected`, OR-composing with an attributed non-resolution typecheck red so the 20260813/10 deadlock class stays classifiable); a typecheck-only red whose evidence is resolution-shaped (`TS2307`/`TS2305`/module-not-found/etc.) no longer satisfies red on its own, closing the commoner missing-export half of the class alongside the missing-module half; the RED dispatch prompt (and build.md's fast-path step 1, by hand) now instructs a demonstration route for load-blocked red-expected carriers — write an inert stub at the File-Plan CREATE path, re-run, report the post-stub observation, delete the stub(s), and verify only the created stub paths are gone (never a whole-tree emptiness check); the sanction that blessed the vacuous class as compile-time-only is dropped from all three loci (the schema comment, the live RED prompt string, and build.md's `tdd-red-check` row), and that row's pre-existing \"strictly redder than red\" exception is scoped explicitly to collection-level absence so a load-shaped `not-collected` never proceeds on the spec's authority; scaffold-ledger.md's red-check sentinel cross-check row is extended in place with the attribution/stub/resolution-refusal mechanism and this incident's evidence. JJ-20260815-07 is fixed at this version. File Plan per-table header binding (specs/20260816/03-file-plan-table-scoped-parsing.md, escaped from specs/20260814/01's review, Salon OS 2026-08-17) — `parseFilePlanRows`/`parseFilePlan` in `spec/scripts/lib/file-plan.js` resolved Action/Layer column indices section-wide after the walk, so a second table under a `###` subheading inside `## File Plan` (e.g. a status table) clobbered the mapping — every row returned `layer: null`, `ac-matrix.js` found zero tests-layer rows, and review hard-blocked on false uncovered-ac findings with no host-side workaround; one shared table-aware walker now binds Action/Layer indices per table, resetting at every table boundary with only a table's own first row eligible as its header, while return shapes, the module's export list, and existing single-table/reordered-column/no-Layer-column behavior stay unchanged. Release migrations leg (specs/20260815/07-release-migrations-leg.md, INTAKE JJ-20260815-09, hearwell) — /spec:release's Phase 1 migrations row is retired as vacuous-by-timing and replaced by a conditional post-deploy `migrations` leg in Phase 2: a host declaring the new optional `release.migrationsCheck` config key (an explicit `\"none\"` records a decline; absent = legacy) runs it after deploy+ready, appends `{\"leg\":\"migrations\",\"exit\":<n>,\"observed\":\"pass\"|\"fail\"}`, and gets `migrations` added to Phase 2's blanket STOP enumeration and Phase 4's report observed-bullet; Phase 0 gains an every-run migrations-directory detection+ask step (drizzle/prisma/migrations/db-migrate/alembic/supabase), writing a decline as the literal `\"none\"`; verdict.js gains a repeatable `--require <leg>` flag (adds a leg to the active profile's required set and, on the release profile, its blocking set too) that release.md's verdict invocation passes as `--require migrations` exactly when a runnable `migrationsCheck` is declared — an absent leg then derives `UNVERIFIED`, a red leg derives `GATE_RED`, no new checker or ledger field; grounding-contract.md's § Release gains `migrationsCheck` as an optional key, and scaffold-ledger.md's Release-stage-executed-checks row is extended with the mechanism, its evidence, and an independent retire condition. Gate/baseline reconciliation (specs/20260816/01-gate-baseline-reconcile.md, INTAKE) — a scoped gate run whose only failures are the repo's sanctioned always-red intake pins now exits 0 by derivation instead of a session hand-verifying and overriding the red: suite-baseline.js gains a fourth mode, `--gate \"<command>\"`/`--gate-file <path>`, that runs the command, parses its failing-test trailer, subtracts `.claude/suite-baseline.json`'s sanctioned set, and either passes a genuinely green child through untouched, exits 0 with a `__SUITE_BASELINE__ ... residual=0` sentinel when every failure is sanctioned, or prints `NEW-FAILING` lines and exits 1 on real residual failures — passing through unparseable/non-test failures unchanged; build.md Phase 0 step 3 wraps the resolved `gateCommand` at the single resolution seam (never `testCommand`), and review.md's own gate leg (Phase 0 step 3 and the fix→re-review re-run) states the same wrap explicitly rather than by citation, capturing `sanctionedReds` from the sentinel into the gate row's `observed` string; verdict.js's `deriveTestsSkipped` regex tolerates the optional `sanctionedReds=<K>` suffix without summing it, so a green-by-subtraction gate still derives CLEAN-family; the shared gate-loop fragment's repair-agent prompt gains one sentence teaching the sentinel so a sanctioned-red gate never triggers a wasted repair round; scaffold-ledger.md registers the mechanism with its promote/retire conditions; claims-baseline.json is re-stamped for the doctrine files' line-count deltas in the same commit. 6.89.0 review fix-delta: `--gate`'s wrapped child now fails closed (a non-passthrough, non-zero exit) when it dies with no exit code at all — killed by a signal, failed to spawn, or its output overflowed spawnSync's maxBuffer — instead of the absence of a parseable trailer ever reading as green; and both sentinel readers introduced by the base spec (review.md's `sanctionedReds` capture and the gate-loop fragment's D7 repair-agent instruction) are now anchored to read only the wrapper's own FINAL `__SUITE_BASELINE__` line, since an earlier line quoted inside the wrapped child's own output (e.g. this spec's own suite-baseline tests asserting on sentinel text) is never the wrapper's verdict.",
  "author": {
    "name": "JJ",
    "email": "jj@555.ventures"
  }
}
