{
  "name": "spec",
  "version": "2.10.3",
  "description": "Full project lifecycle: greenfield genesis (/spec:genesis-architect + /spec:genesis-design) → plan → design → build → review with deterministic workflows, a hook-enforced state machine, per-repo grounding bootstrapped by /spec:init, and deterministic rule enforcement generated by /spec:enforce",
  "author": {
    "name": "JJ",
    "email": "jj@555.ventures"
  }
}
