# Intake Ledger: Host Findings → Plugin Fixes

The plugin-side half of the feedback loop. Hosts emit findings (feedback briefs in
`docs/spec-feedback/`, `[plugin]`-tagged Gotchas, escape rows); the plugin repo's `/intake`
command triages them into this ledger. This file **ships with the plugin** so host-side
`/spec:doctor` (upstream-fixes check) can compare its rows against the host's stamped
`generatedBy` version and report which findings the host is still working around that a
newer plugin already fixed.

**The intake contract:** an accepted finding is a **failing test first** — reproduced
against this repo's test suite (a fixture host under `tests/fixtures/` where the finding
needs one) before any fix lands. The test is the backlog item, the duplicate key, and the
regression guard; "when did this test start passing" is the `Fixed in` column. A finding
that cannot be reproduced is either host-specific (routed back as a `[host]` gotcha) or not
yet understood (not accepted). Rows predating this contract (the UpWell 2026-07 brief,
absorbed into v5.3.0) cite the scaffold-ledger row or doc test that pins the fix instead —
marked `pre-contract`.

Statuses: `open` (accepted, test failing, fix not landed) · `fixed@<version>` ·
`rejected` (with reason) · `already-fixed` (reporter's version predates the fix).

| ID | Source | Category | Stage | Pinned by | Fixed in | Fix |
|---|---|---|---|---|---|---|
| UPWELL-20260716-01 | UpWell brief Part 1–2 (static-only verification passed a program that cannot boot; 2 CLEAN reviews on a crash-loop) | missing-substrate | review | `tests/smoke-manifest.test.js` (boot smoke leg, fail-closed) | 5.3.0 | pre-contract |
| UPWELL-20260716-02 | UpWell brief 3.2 (skipped tests reported as passes; AC matrix counted collected, not executed) | reporting-integrity | review | `pre-contract` — review.md skip reconciliation + scaffold-ledger "Skipped-test reconciliation" row | 5.3.0 | pre-contract |
| UPWELL-20260716-03 | UpWell brief Part 2 (init stamped complete with Deliverable #6 never written; authored ≠ activated) | reporting-integrity | init | `tests/smoke-manifest.test.js` (manifest-check fail-closed) | 5.3.0 | pre-contract |
| UPWELL-20260716-04 | UpWell brief 3.4 (ADR bound a convention the pinned dependency rejects; spike triggered on felt uncertainty) | doctrine-rot | genesis | `pre-contract` — scaffold-ledger "Shape-triggered micro-spike" row | 5.3.0 | pre-contract |
| UPWELL-20260716-05 | UpWell brief 3.5 (poisoned generated doctrine had no repair path short of full re-init) | doctrine-rot | doctor | `pre-contract` — doctor.md `--fix` repair mode | 5.3.0 | pre-contract |
| UPWELL-20260716-06 | UpWell brief Part 4 (escape ledger existed, qualifying incident occurred, ledger empty — no consumer, no capture trigger) | workflow-defect | escape | `pre-contract` — `preventedBy` field + commit-time capture (git plugin 1.2.0) + scaffold-ledger "Prevention delta" row | 5.3.0 | pre-contract |
| UPWELL-20260716-07 | UpWell brief 3.6 (plugin defects living as host folklore; each host re-pays the discovery) | doctrine-rot | doctor | `tests/feedback-loop.test.js` (provenance tags, brief flush, this ledger) | 5.4.0 | pre-contract |
| UPWELL-20260716-08 | UpWell brief Part 4 (state gate trippable by describing the marker syntax in prose) | workflow-defect | plan | `tests/state-gates.test.js` (frontmatter marker counter authoritative) | 5.3.0 | pre-contract |
| UPWELL-20260716-09 | UpWell brief Part 4 (AC-ID tags collide across specs touching one test file) | template-bug | plan | `pre-contract` — `templates/spec.md` AC-{YYYYMMDD-NN} namespacing | 5.3.0 | pre-contract |
| UPWELL-20260716-10 | UpWell brief 3.7 (designed affordance entered build with spec authority and no spec scrutiny) | workflow-defect | design | `pre-contract` — scaffold-ledger "Affordance ↔ contract reconcile" row | 5.3.0 | pre-contract |
| UPWELL-20260716-11 | UpWell brief Part 3 (per-spec CLEAN verdicts don't compose; no executed milestone gate) | missing-substrate | release | `pre-contract` — commands/release.md + scaffold-ledger "Release stage executed checks" row | 5.3.0 | pre-contract |
| UPWELL-20260716-12 | UpWell genesis run (chain ended with no plannable unit; roadmap hand-authored) | missing-substrate | genesis | `pre-contract` — scaffold-ledger "Roadmap as genesis phase" row | 5.2.0 | pre-contract |
| PRAX-20260717-01 | Prax widened audit (ops-conventions ADR dictation omits naming/identifiers and wire-representations rows; produced the run_id/runId contradiction inside ADR-0012's own output, three divergent id spellings in byte-locked artifacts, a +00:00 timestamp defect, and an unrepresentable bigint — executor exceeded the list once by luck, never reliably) | checklist-gap | genesis | `tests/ops-conventions-rows.test.js` | 6.4.2 | pre-contract |
| PRAX-20260717-02 | Prax brief 2026-07-17 (worktrees live INSIDE the parent repo at `.claude/worktrees/`, so a build worker whose toolchain resolves upward for a project root ran `pnpm gate` against the live parent checkout — another session's uncommitted work — and turbo's shared cache replayed a main-checkout log as a worktree hit; no wf-build prompt pins gate/red-check/self-verify CWD) | workflow-defect | build | `tests/worktree-cwd.test.js` | open | — |
| UPWELL-20260718-01 | UpWell `[plugin]` gotcha (wf-build tdd-red-check joins repo-root-relative File Plan paths onto a workspace-filtered `pnpm --filter app test`; vitest resolves them inside the workspace, collects zero files, red-check returns conservatively, and every TDD build falls back to direct dispatch — reproduced at `spec/workflows/src/wf-build.body.js` RedCheck phase, which appends `testFiles` verbatim) | workflow-defect | build | `tests/redcheck-workspace-paths.test.js` | 6.29.0 | pre-contract |
| PRAX-20260719-01 | Prax `[plugin]` gotcha (spec 20260713/03-auth D17: a requirement satisfied by a library DEFAULT got an AC pinning the library mechanism that passed before any implementation existed, leaving the shipped config's engagement unobserved — `rateLimit: {enabled: false}` would disarm production abuse gating with every AC green; plan.md's AC-shape guidance has no library-default split rule) | checklist-gap | plan | `tests/ac-split-library-default.test.js` | 6.62.0 | mechanism(spec/commands/plan.md) |
| PRAX-20260719-02 | Prax `[plugin]` gotcha (falsified plan-time vendor reference must re-open orchestrator-level MCP lookup; an orchestrator enforcing the worker no-MCP rule on itself burned a retainer cycle authoring a remedy the vendor docs contradicted — reported at spec@6.7.1, already fixed upstream) | doctrine-rot | build | `pre-contract` — build.md § "Falsified embedded reference → orchestrator refreshes it first" | 6.9.0 | pre-contract |
| PRAX-20260721-01 | Prax session 2026-07-21 (roadmap delta binding 01-app-spine carried the security rules for a T3 auth change — OAuth account-linking hazard; brief 01 predated the delta and cited nothing; plan.md Phase 0 read only "every delta the brief cites", nothing derived or checked the `Binds:` backlink, delta silently skipped at plan time. Disposition removes the artifact class instead of instrumenting it: post-genesis decisions are amendment ADRs with an `Applies to` list whose effects are edited into the named briefs at decision time — `Amended by ADR-NNNN` backlinks, letter-suffixed successor briefs for consumed briefs, doctor check 17 bidirectional audit + legacy `deltas/` migration flag, plan Phase 0 unpropagated-amendment net) | workflow-defect | plan | `tests/roadmap-amendments.test.js` | 6.18.0 | pre-contract |
| JJ-20260720-01 | Fable hardening-review brief 2026-07-20 (Sonnet-planned spec promised an outcome in its Goal with no delivering Decision; ACs written from Decisions stayed green in the mechanism's absence; Phase 3 refuters verify claims the spec makes, not promises made without a mechanism, and build/review check diff-against-spec, never spec-against-goal — the class survives the whole pipeline; user-attested, source spec not re-executed) | checklist-gap | plan | `tests/goal-mechanism-audit.test.js` | 6.15.2 | pre-contract |
| HEARWELL-20260721-01 | Hearwell brief 2026-07-21 (TDD red-probe runs the runtime test-runner only, so a test that is red under the gate's typecheck leg — optional-property addition, TS2339 at probe time — passes vacuously at runtime and is falsely reported "not red"; TDD silently lost for every spec whose red-ness lives in typecheck: union members, widened signatures, assert-absence tests) | workflow-defect | build | `tests/redcheck-typecheck-leg.test.js` | 6.29.0 | pre-contract |
| HEARWELL-20260721-02 | Hearwell brief 2026-07-21 (`rulesEnforcementHash` matched no committed revision of `enforcement.json` ever — stamp computed against an intermediate state, manifest written afterward; doctor check 10 fires unconditionally, so the enforcement drift detector was dark from install; sibling `designRulesHash` recomputes exactly, isolating the defect to the enforce stamp order) | reporting-integrity | enforce | `tests/enforce-hash-stamp.test.js` | open | — |
| PRAX-20260721-04 | Prax brief 2026-07-21, corroborated by upwell escape 2026-07-24 (a gateCommand leg can be a cache lookup at TWO levels — the runner's cache and the tool's own incremental cache; ruff served stale "All checks passed" over red files while `turbo --force` dutifully re-invoked it, 6 files invisibly red across ~36 commits behind a CLEAN; upwell's `dataplane:test` turbo-cache replay behind a CLEAN is the same class. Fix shape: activation probe at init/doctor — inject a red probe per leg, require non-zero exit — plus per-tool cache-defeat doctrine). Corroborated again 2026-08-12 (prax brief 2026-08-13): first `pnpm gate` run reported `TIER_NOT_EXECUTED`/`BELOW_FLOOR` from a stale turbo receipts summary, three retries exited 0 at full counts — the checker-reads-cached-summary direction can go green-when-it-shouldn't too; host flagged retry-green rather than treating it as resolution | workflow-defect | init | `tests/gate-activation-probe.test.js` | open | — |
| PRAX-20260721-02 | Prax brief 2026-07-21 (a cross-plane contracts change needs an orchestrator-only codegen step BETWEEN implementation batches — regenerated schemas + pydantic mirror are read-only worker surfaces, so workers correctly block, the repair loop burns rounds nothing can fix, and the build abandons the workflow for fastPath; any host with a codegen seam hits this on every spec touching it. Fix shape: declared orchestrator step at a layer-group boundary) | workflow-defect | build | `tests/build-codegen-seam.test.js` | open | — |
| PRAX-20260721-03 | Prax brief 2026-07-21 (parity-check planes are file-globbed but casing conventions scope by syntactic role and seam side: 20/20 remaining findings false positives — `class RunEvent` folded against its own module name `run_event.py` (PEP 8), and snake_case internals constructing camelCase wire kwargs lines apart. The same run's contracts plane caught a genuine invisible contradiction — fix the plane grammar, do not downgrade to advisory). Re-measured 2026-08-13 (prax brief): 169 findings, zero true, across all five ADR planes; second structural cause new since filing — embedded SQL literals inside TS integration tests counted as TS-plane identifiers, inexpressible by any plane glob. Host has scoped check 16 to the SQL plane only pending this row; priority signal upgraded | workflow-defect | doctor | `tests/parity-check-roles.test.js` | open | — |
| PRAX-20260726-01 | Prax `[plugin]` gotcha ×2 (specs 20260725/01 D14, 20260725/08) + hearwell `[plugin]` gotcha (spec 20260724/01 design-landed components) — red-check's sanctioned-green exemption is the literal phrase "SHALL CONTINUE TO" (6.25.0), but the class is any AC whose carrier is a legitimately-green test: negative-invariant/absence pins, tag-only re-tag ACs, and tests against components pre-landed at the design stage; each instance loops tdd-red-check into fastPath abandonment | workflow-defect | build | `tests/redcheck-green-carriers.test.js` | 6.29.0 | pre-contract |
| UPWELL-20260725-01 | Upwell `[plugin]` gotcha (spec 20260725/03: a File Plan row bundling an edit to a different file — "create cli.ts; add the npm scripts to app/package.json in this row" — hands the worker a file its contract forbids touching, so the bundled edit silently becomes an unrecorded orchestrator duty and is dropped) | checklist-gap | plan | `tests/fileplan-bundled-edits.test.js` | 6.62.0 | mechanism(spec/commands/plan.md) |
| CROSS-20260727-01 | Escape-ledger rollup, 3 hosts (zubu `wf_e717bc0b-a06`; prax `wf_1d2110d7-d3c`, `wf_f988caea-506`; upwell `wf_cce37245-ec1`, plus cache-replay CLEAN `wf_e260acd3-1da`) — every recorded escape through a CLEAN went through a fix-delta iteration-2 CLEAN or a cache-inherited gate leg: the fix-delta pass scopes one reviewer to the fix diff + prior findings and INHERITS the full diff's gate/smoke state instead of re-asserting it, so the verdict certifies executed state nobody re-executed; ZUBU-20260716-01's recorded reopen condition fired | workflow-defect | review | `tests/fixdelta-full-state.test.js` | 6.59.0 | mechanism(spec/workflows/src/wf-review.body.js) |
| UPWELL-20260730-01 | UpWell brief 2026-07-30 (ITERATE handoff names components but never WHERE they live; against a single mega-showcase catalog — 5,400-line stories file, 12 exports, specs appending at the bottom — "run storybook, six component names" sends the reviewer hunting, degrading with every spec; Storybook deep links are mechanically derivable from title+export slugs, so omission is inexcusable) | template-bug | design | `tests/design-driver.test.js` (ITERATE 🔗 navigation-line pin) | 6.30.0 | pre-contract |
| UPWELL-20260730-02 | UpWell brief 2026-07-30 (deltas.json addressed with a region label — `investigation-unpack.handoff.provenance` — where the checker wants the surface id; unknown surfaceId fell through to "sliceQuote not found verbatim", blaming grep-verified evidence for an addressing error and burning a debugging cycle) | workflow-defect | design | `tests/fidelity-check.test.js` (region-label addressing error) | 6.30.0 | pre-contract |
| UPWELL-20260730-03 | UpWell brief 2026-07-30 (the sanctioned verbatim-carrier-comment fix for merged copy strings silently fails when the `//` comment wraps: norm() collapses the newline but the continuation line's own `//` marker interleaves into the collapsed haystack, breaking the contiguous match) | workflow-defect | design | `tests/fidelity-check.test.js` (wrapped `//` carrier comment) | 6.30.0 | pre-contract |
| PRAX-20260804-01 | prax user message 2026-08-04, corroborated by escape `wf_82cfbb56-a7b` (a T3 spec written to eliminate unfalsifiable checks shipped two of its own: `ENABLE ROW LEVEL SECURITY` without `FORCE` leaves `relforcerowsecurity` false so the mutation reddened nothing, and drizzle's migrator never re-applies a recorded migration so the restore AC asserted the opposite of reality; every positive dependency-adjudicated claim was executed per Phase 1.5, the negative ones weren't — all the trigger's example shapes are positive) | checklist-gap | plan | `tests/negative-claim-microspike.test.js` | 6.37.0 | pre-contract |
| CROSS-20260804-01 | hearwell gotchas ×2 (spec 20260731/06 — sole finding killed MISCITED over a line-number typo whose content the verifier's own evidence confirmed, CLEAN with zero survivors; spec 20260801/05 — real finding killed on "does not exist" from a stale `.claude/worktrees/agent-*` checkout) + prax gotcha (spec 20260731/07 — structured MISCITED result contradicting its own evidence prose, only the structured field feeds `verdict`): three uncovered false-kill paths in one shared verifyPrompt | workflow-defect | review | `tests/verifier-kill-integrity.test.js` | 6.37.0 | pre-contract |
| HEARWELL-20260804-01 | hearwell gotcha (spec 20260801/07 build `wf_32360253-a2c` — the gate agent enumerates failures from stdout, so a passing file's deliberate `mockRejectedValueOnce` log line ("metering db unreachable") became a phantom failure entry every round; being outside the File Plan it hard-returned `out-of-scope-failure` on round 0, masking the real TS2493/TS2339 failure underneath) | workflow-defect | build | `tests/gate-phantom-failures.test.js` | 6.37.0 | pre-contract |
| HEARWELL-20260804-02 | hearwell gotcha (spec 20260801/05 — a spec that creates its own workspace package leaves red-check structurally blind: turbo/spec-test.sh cannot collect tests for an unregistered package, every red-expected file returns `not-collected`, and the build loops tdd-red-check; not-collected-because-the-spec-creates-the-home is strictly redder than red) | workflow-defect | build | `tests/redcheck-new-package.test.js` | 6.37.0 | pre-contract |
| PRAX-20260801-01 | prax brief 2026-08-01 finding 01 (dc-extract on a static local bundle classes all 1854 strings `copy` — no `{{ }}`/`sc-for` syntax to class on; classification pressure absorbed by templates + 6.32–6.34 matching fixes; accepted residual: inline markup splits one translatable sentence into 3 catalog fragments — extraction-side adjacent-text join is the cleaner home than 6.33.0's matching-side fix) | workflow-defect | design | `tests/dc-extract-inline-join.test.js` | 6.37.0 | pre-contract |
| PRAX-20260801-02 | prax brief 2026-08-01 finding 02 (future-brief capability entangled inside a region the current brief must bind costs an evidence-gated delta row where a separate region would be inherited free via the coverage ledger; measured on verdict-standing's standing-object region — two hand-proven delta rows; retainer-drafted sketch.md line, ratified 2026-08-04) | checklist-gap | sketch | `tests/sketch-region-granularity.test.js` | 6.37.0 | pre-contract |
| PRAX-20260801-03 | prax brief 2026-08-01 finding 03 (fidelity-check compares normalized text only — a mock `<a>` rendered as an inert `<div>` passes, and the reverse would too; measured: 15 raw anchors shipped where the route family requires typed Links, caught by diff-reading not the gate; the reconcile affordance↔contract matrix covers it in doctrine, the gap is mechanization) | missing-substrate | design | `tests/fidelity-element-semantics.test.js` | open | — |
| PRAX-20260804-02 | prax gotcha (spec 20260731/04 design — a skeleton named exactly as the surface id claims every region of the mock including the fake iOS status bar, and the fidelity refusal names the `09:44` clock copy rather than the naming collision, reading as a transcription miss) | checklist-gap | design | `tests/skeleton-subset-binding.test.js` | 6.37.0 | pre-contract |
| JJ-20260801-01 | 04-live-smoke.md D8, Fable consult 2026-08-01 (specs 01–03 shipped CLEAN with a `runtime.inert` declaration that was true when written and became false the moment `autopilot/bin/autopilotd` landed as a bootable entry point — nothing re-validated the exemption, so this repo's one executed-verification gate was silently off for three consecutive reviews; the instance is fixed in 04-live-smoke.md D2–D4, this row is the class-level falsifier: a spec whose File Plan adds a bootable entry point under a repo still declaring `runtime.inert` should be a hard finding) | missing-substrate | review | `tests/review-runtime-inert-falsifier.test.js` | 6.59.0 | mechanism(spec/commands/review.md) |
| JJ-20260808-01 | spec-05 review session 2026-08-08 (review.md Phase 0 step 8 documents the pre-panel hard-stop verdict invocation as "no `--workflow` — the derivation reaches GATE_RED from the manifest alone", but verdict.js:60 exits 2 (usage) whenever `--workflow` is absent under the review profile — the documented contract is unimplementable, and the 05 review's GATE_RED row was only ledgered by hand-crafting a stub workflow file, a seam every future aborted review re-pays). Third occurrence 2026-08-13: prax gotcha (spec 20260812/02 review; also notes `derive()` dereferences `workflow.*` unconditionally, so relaxing only the arg check would NPE). Widened 2026-08-13 by prax brief finding 05 — same root cause, second surface: doctor check 12's required-field exemptions (observe/fastPath/escape/release) never learned the pre-panel GATE_RED row shape, so 5 of 6 prax GATE_RED rows (`runId:null`) trip ledger hygiene on a host following review.md verbatim; adopted contract is `runId` OPTIONAL on GATE_RED review rows (an in-workflow red iteration legitimately carries one), never a blanket exemption; pin extended with the doctor.md facet | workflow-defect | review | `tests/verdict-gatered-no-workflow.test.js` | 6.60.0 | mechanism(spec/scripts/verdict.js) |
| UPWELL-20260810-01 | UpWell `[plugin]` gotcha (spec 20260731/04, build + review 2026-08-10 — SIX severed-chain defects in one spec: always-false render condition, computed number hardcoded null at its one render site, defer-time write rebuilt away by settle, orphaned caller behind a too-narrow filter, heatmap whose only production producer pinned null, ranked list never sorted; every File Plan row implemented faithfully, every AC green, only the panel caught them). Holistic disposition: the reporter's Wiring Plan artifact + wiring-matrix leg + verdict entry REJECTED as additive (the chain already lives in the Decision's prose); the mis-aim is the AC-shape rule — an AC may pin an intermediate hop, so the terminal observable has no observer. Fix: cross-file Decisions must carry ≥1 AC asserting the chain's terminal observable fed by the production path (fixture-fed ≠ terminal); the existing ac-matrix leg then carries it with zero new machinery | checklist-gap | plan | `tests/ac-terminal-observable.test.js` | 6.50.0 | prose(AC-shape rule tightening in plan.md; the existing ac-matrix leg mechanically carries terminal-observable ACs, so no new checker exists to point at; pin re-aimed at the disqualification clause in 6.51.0 when specs/20260810/02 renamed the class to "invented-fixture liveness") |
| UPWELL-20260810-02 | UpWell `[plugin]` gotcha (spec 20260807/01, review interrupted 2026-08-07 blocked solely on a red CI leg, resumed 2026-08-09: four later specs had landed on main, so `git diff <pre-build>..HEAD` carried 27 files — 26 from other specs, two not yet reviewed — an unreviewable blob that stalls the spec or launders unreviewed code through its panel; host recovered by hand with a detached frozen worktree + per-hunk drift attribution). In-place fix, no new doctrine: wf-review detects HEAD != the spec's last commit at start and derives the frozen review base itself. The same gotcha's second gap (verdict.js requires `--workflow` on the review profile, contradicting review.md's documented no-workflow hard-stop) is JJ-20260808-01 — second occurrence, corroboration noted here | workflow-defect | review | `tests/review-frozen-base.test.js` | 6.50.0 | mechanism(spec/workflows/src/wf-review.body.js) |
| PRAX-20260813-01 | prax brief 2026-08-13 finding 01 (review `wf_5a730ede-0f8`: `killedBy:"sanction"` whose own evidence field read "Not actually sanctioned — correcting: the claim stands unrefuted"; only the structured label feeds verdict.js, workflow returned CLEAN with the real finding in `killed[]`). Residual hole of CROSS-20260804-01: the 6.37.0 self-consistency guard covers only the MISCITED path — SANCTIONED has none, and no mechanical `killed[]` label↔evidence audit exists on any path; the doctrine "no finding dies by argument" rests on a self-report. Prax carries a standing read-every-killed-evidence workaround, retirable when this closes | workflow-defect | review | `tests/verifier-kill-integrity.test.js` (extended), `tests/review/killed-audit.test.js` | 6.59.0 | mechanism(spec/workflows/src/wf-review.body.js) |
| CROSS-20260813-01 | upwell gotchas ×2 (spec 20260811/01 — D6 named a file by path with no File Plan row: work unassignable, landed as an orchestrator edit flagged out-of-plan; spec 20260731/06 — D5 ordered a persisted, later-rendered message with no contracts row, so the worker's only in-scope option was a raw English template literal invisible to jsx-only i18n lint) + autopilot-hub gotcha across 2 specs (Contracts pinning only the public seam leaves the TDD test author inventing factory shapes the implementer then conforms to, unreviewed) + prax gotcha (spec 20260812/02 — computed-expectation helper `flat_bars_after_warmup` encoded a kernel claim with zero ground-truth carrier; every use was expectation-construction, caught by retainer not panel). Class: plan lock has no obligation→carrier sweep — a Decision's promise can have no owner and every AC still goes green | checklist-gap | plan | `tests/plan-obligation-carrier.test.js` | 6.62.0 | mechanism(spec/commands/plan.md) |
| CROSS-20260813-02 | prax brief 2026-08-13 finding 02 (verifier ran `ALTER TABLE revenue_events NO FORCE ROW LEVEL SECURITY` against the shared dev database unprompted, `wf_ab878c58-31c`; restore happened to succeed, but fail-closed review EXPECTS crashed/cap-skipped verifiers — the designed failure mode leaves tenant isolation off) + prax gotcha (spec 20260812/02 — spec:reviewer left scratch `diff2.txt` in the working tree ahead of a `git add -A` close commit). Class: review-agent write discipline fences file edits and git only; execution side effects on shared stateful substrates and post-panel tree hygiene are unfenced | workflow-defect | review | `tests/review-write-fence.test.js` | 6.59.0 | mechanism(spec/commands/review.md) |
| CROSS-20260813-03 | hearwell brief 2026-08-12 ×2 (review rows write `testsSkipped` identically for `[env:]`-gated and never-ran ACs, so every downstream sweep re-opens specs to count tags; first release recorded verdict CLEAN beside `ci:"unavailable"` with the compensating ⚠️ living only in console output) + upwell gotcha (spec 20260811/01 — AC whose declared oracle is typecheck or a gate leg reports uncovered even when the oracle ran green). Cross-host anomaly corroboration this sweep: 13/25 prax and 9/20 upwell post-08-10 review rows carry undifferentiated `testsSkipped>0`, and upwell's 2026-08-02 hard escape (`wf_5f7077cc-d5a`) passed a review recording 35 unqualified skips. Class: run-time verification qualifiers (sanctioned-skip provenance, structurally-absent legs, non-test oracles) are not durable or machine-readable in the artifacts that outlive the run | reporting-integrity | review | `tests/verification-qualifier-durability.test.js` | 6.61.0 | mechanism(spec/scripts/verdict.js), mechanism(spec/commands/review.md), mechanism(spec/commands/release.md) |
| PRAX-20260813-04 | prax brief 2026-08-13 finding 04 (claims-lint.js resolves `spec/doctrine/claims-baseline.json` against the invoking CWD, so doctor check 18 — always run from a host repo — prints "no baseline" and exits 0; reproduced verbatim from prax during this sweep. A check that cannot run anywhere it is invoked, silently green — same family as skipped-tests-as-passes) | workflow-defect | doctor | `tests/claims-lint-baseline-path.test.js` | 6.60.0 | mechanism(spec/scripts/claims-lint.js) |
| PRAX-20260813-06 | prax brief 2026-08-13 finding 06 (generated run skill enumerated routes "currently `/` and `/api/health`" against 37 on disk; conventions/ui.md named a token home its own target file contradicts; sibling `[host]` gotcha: generated agent Expertise block described the tree as of init, a month stale). Class: init writes volatile enumerable repo facts as prose snapshots — no checker can exist for arbitrary generated sentences, and refreshing re-breaks on the next change. Fix shape is an authoring rule, true by construction: generated prose names the derivation (the command/location that yields the fact), never the enumeration | doctrine-rot | init | `tests/init-derivation-not-enumeration.test.js` | 6.62.0 | mechanism(spec/commands/init.md) |
| PRAX-20260813-07 | prax gotcha (spec 20260810/05 build deviation #3 — scope-reconcile does not expand File Plan glob rows: `packages/contracts/schemas/*.json` codegen outputs double-count as `outOfPlan` (the concrete file) AND `unrealized` (the glob row)), second incident spec 20260812/01 (contracts codegen ripple landed four mechanically-forced files `outOfPlan` on a build following the spec's own orchestrator-duties paragraph; prax has an open follow-up asking for a generated-artifact map). Adjacent to open PRAX-20260721-02 (codegen seam needs an orchestrator step); this row is the reconciler's row grammar, fixable independently | workflow-defect | build | `tests/scope-reconcile-glob-rows.test.js` | 6.60.0 | mechanism(spec/scripts/scope-reconcile.js) |
| TRADOYO-20260813-01 | tradoyo/dashboard spec 20260813/10 build (RedCheck cross-checked a RUNTIME-ONLY `AUDIT_RED:<path>`/`AUDIT_GREEN:<path>` sentinel against a DUAL-LEG expectation, so a compile-time-only carrier could not be classified at all: an `expectTypeOf` assertion — like an assert-absence pin or a test importing a module the implementation has yet to create — is erased at runtime and can never be runtime-red, so `expect: "red"` failed the sentinel cross-check while `expect: "green"` failed the dual-leg reading, and NO classification passed. Hit on a spec whose AC promise was itself compile-time; the orchestrator hand-verified the red state and resumed with `tdd: false` to bypass the probe entirely, losing TDD enforcement for the run. Adjacent to UPWELL-20260718-01 (same probe, different incommensurability: paths vs workspace filter) and built on HEARWELL-20260721-01, which added the typecheck leg to the *reading* but left the sentinel single-leg) | workflow-defect | build | `tests/redcheck-sentinel-dual-leg.test.js` | 6.65.0 | mechanism(spec/workflows/src/wf-build.body.js) |
| JJ-20260814-01 | self-hosted review of spec 20260813/06, run `wf_59aba53d-4a5` (review.md's ledger-row prose states in bold "never write `CLEAN` on a row whose `survived` is non-zero", but verdict.js derives CLEAN the moment every survivor is dispositioned — so the sanctioned all-waived path prints exactly the row the prose forbids. That run did: `"verdict":"CLEAN"` alongside `"survived":1,"waived":1`, appended to `.claude/spec-runs.jsonl`. Two readings are live and nothing pins either — "never hand-assert CLEAN" (script-compatible, since verdict.js is the sole writer) or the literal prohibition (script-contradicting). Consequence if the literal reading is correct: every waive-closed review row is malformed and doctor's correlations read a shape the doctrine disowns; if the permissive reading is correct, the bold sentence misleads every reader of the ledger contract. Fix shape is a ruling, then one carrier — either verdict.js refuses the combination, or the prose says what it means. RULED 2026-08-14 by JJ: the script is right, the prose was wrong — the hole was always CLEAN with an *undispositioned* survivor, never a survivor as such. review.md now states the invariant verdict.js enforces and keeps the real prohibition, never hand-write the verdict word; the pin became executed-in-both-directions rather than prose-only, which is how the sentence drifted from the script unnoticed in the first place. Companion prose pins in `tests/review/verdict-doctrine.test.js` and `tests/run-ledger.test.js` retargeted to the accurate wording, same hole) | reporting-integrity | review | `tests/clean-row-survivor-consistency.test.js` | 6.68.0 | mechanism(tests/clean-row-survivor-consistency.test.js), mechanism(spec/commands/review.md) |
| JJ-20260814-02 | self-hosted review of spec 20260813/06, run `wf_59aba53d-4a5` (spec 06 D9 required every workflow return to pin its own `runId`; no workflow can obtain one — the harness mints it at invoke time and delivers it only in the caller's tool result. The build worker's deviation cites build.md's resume clause, "reuse the prior `runId` if known", as the evidence that the orchestrator threads a run id into `args`, and echoed `args.runId` into all six returns where it was permanently `undefined`. The echoes are gone — spec 06a repealed them at 6.67.0, pinned by `tests/report/return-slots.test.js` — but the sentence that seeded the misreading is untouched: it names no source for the id it tells the reader to reuse, so it still reads as consistent with "the workflow already holds its runId". Residual class: doctrine that names a value without naming where it comes from lets the next author re-derive the same unimplementable Decision. Fix shape is one clarifying clause — the id comes from the Workflow tool result and is orchestrator-held — not a new mechanism) | doctrine-rot | build | `tests/workflow-runid-provenance.test.js` | open | — |
| JJ-20260815-01 | self-hosted review of spec 20260814/04, run `wf_e9e438d1-c94` (ac-matrix.js's `malformed-ac` path fails OPEN on the coverage denominator: `parseBullets()` matches `^- \*\*(token)\*\*` and drops any bullet failing `AC_ID_RE` from `wellFormed`, hence from both `acById` and the coverage loop — so the durable manifest row records `uncovered=0` while an unswept AC exists. The notation itself IS caught loudly, which is what makes the hole survivable-looking: waive that one finding and the coverage claim is permanently false thereafter. The review that found it had the spec's single most load-bearing pin — a signal-handler ordering AC — outside the sweep while the row read full coverage. Fix shape: unparseable = unknown = uncovered, failing closed on the denominator exactly as the skipped-test reconciliation already does; the `observed` grammar `uncovered=N oracle=M` stays unchanged so no downstream parser moves) | reporting-integrity | review | `tests/ac-matrix-coverage-holes.test.js` | open | — |
| JJ-20260815-02 | self-hosted review of spec 20260814/04, run `wf_e9e438d1-c94` (ac-matrix.js's skipped-test reconciliation resolves a skipped test's AC through `acById`, built ONLY from the spec under review, so an `[env: VAR]` declaration living in the spec that OWNS that AC is unreachable — while the scoped gate glob runs every test file in the directory. A correctly declared env-gated suite therefore reports as an unsanctioned skip, a HARD finding, on every review touching that area in perpetuity, with waive as the only remedy: the cry-wolf path straight back into UPWELL-20260716-02, this leg's founding incident. Reproduced here on `AC-20260808-01-12`, declared `[env: AUTOPILOT_ENROLL_LIVE]` in specs/20260808/01. Residual hole of CROSS-20260813-03, not a regression of it — 6.61.0 added `[env:]` sanctioning but never widened WHERE the declaration is read from. The AC-ID grammar carries the lookup (`AC-YYYYMMDD-NN[a-z]?-k` → `specs/YYYYMMDD/NN*-*.md`); fail closed when the owning spec is absent, unreadable, or carries no `[env:]`) | workflow-defect | review | `tests/ac-matrix-coverage-holes.test.js` | open | — |
| JJ-20260815-03 | escape `wf_e1da0ea6-94c` (2026-08-14, `foundBy:"user"`, hard, spec specs/20260813/10-host-capabilities.md, file tests/review/verdict.test.js): D4 changed what verdict.js's review profile RETURNS (CLEAN-with-qualifier on an unavailable observation leg); five pins in tests/review/verdict.test.js and tests/run-ledger.test.js were green before and red after, and nothing saw it — the spec's scoped gate resolved to tests/capabilities/ plus tests/model-placement.js from its own File Plan test rows, so neither the build gate nor the review panel ever executed the suites pinning the altered behavior. Review returned CLEAN-with-qualifier at 22:30Z; found by hand and fixed at 22:42Z. Not reachable by existing carriers, each checked: the colliding-pin Gotcha covers a RETIRED LITERAL and prescribes a stem grep — a behavior change leaves no string to grep; plan lock's obligation→carrier sweep confirms every STATED obligation has a carrier — the Decision states none about other suites, the collision being a property of test topology (this is precisely why the two 2026-08 gotchas about a Decision naming a backlog row or a model seat were closed as already-covered and this one is not); CROSS-20260727-01 is a different failure — that iteration-2 pass re-ran every leg green, it re-ran the wrong SCOPE. Scoping itself is correct and load-bearing (§ Test Rules red-pin baseline). Fix shape: the File Plan already names the source files, so the at-risk suites are mechanically derivable — grep tests/ for those paths and their exported symbols, and any hit outside the resolved `{testDirs}` becomes a File Plan row or a recorded waive, the retired-literal anchor's shape applied to behavior instead of text) | checklist-gap | plan | `tests/scoped-gate-behavior-collision.test.js` | open | — |
| JJ-20260815-04 | intake sweep 2026-08-15, reproduced live (`node --test tests/autopilot` on Node 26 → `test at tests/autopilot:1:1 ✖`, MODULE_NOT_FOUND, non-zero exit), plus this repo's standing `[plugin]` gotcha from the specs/20260801/02 build and a second hit during the specs/20260814/04 review: build.md Phase 0 step 3 resolves `{testDirs}`/`{scopeDirs}` "from the spec's File Plan DIRS" — the form that does not run tests — while review.md Phase 0 step 3 says to resolve it "exactly as build.md Phase 0 step 3 already does (cited, not duplicated): `{testDirs}` resolved to the GLOB FORM". review.md cites build.md as authority for a rule build.md does not state, which is a hard finding under this repo's own § Review Checks and has teeth: a session following build.md literally produces a gate leg failing for a reason unrelated to the code under test, which review then treats as a red blocking leg and hard-stops on before the panel. Fix shape: one clause in build.md at the point of resolution, after which review.md's citation becomes true — no new mechanism) | doctrine-rot | build | `tests/testdirs-glob-resolution.test.js` | 6.75.0 | mechanism(tests/testdirs-glob-resolution.test.js) |
| JJ-20260815-05 | escape-row rollup, both rows in this repo's ledger (`wf_1d6e7652-ec3` 2026-08-15 spec specs/20260810/05, autopilot/bin/autopilotd, `foundBy:"user"`, hard; `wf_e1da0ea6-94c` 2026-08-14) carry `preventedBy:"runtime-leg"`. For the first that judgment is exactly right and cheap to act on: the daemon's documented "lock released on clean shutdown" contract was false — a SIGTERM in the window between the lockfile write and the handler install stranded the lock and blocked the service's own restart — and it rode two CLEAN reviews. smoke.sh ALREADY sends the host's declared `runtime.stopSignal`, but only inside its EXIT trap where the leg's verdict is already fixed: it never waits for the exit, never checks the status, and never re-runs the host's readiness/state checks afterwards. The signal is spent and the observation discarded, so the leg certifies "this program starts" and is silent on "this program stops cleanly" — where a long-running service's state-corrupting defects live. shared.md § Runtime Verification argues only the boot half; the argument applies unchanged to shutdown. Fix shape: after readiness is observed, send `stopSignal`, wait a bounded interval, require exit, then re-run the declared readiness/state checks to assert cleanup — declared-inert hosts stay exempt) | missing-substrate | review | `tests/runtime-leg-shutdown.test.js` | open | — |
| JJ-20260815-06 | claude-plugins review of spec 20260814/05, run `wf_917cb3b3-98d` (surfaced as an out-of-plan working-tree edit and adjudicated during that review; originating host salon-os, its spec 20260814/04 mock). PRAX-20260801-01's inline-text join reunites a sentence split by `<b>`/`<i>`, but it decides "am I inside one sentence?" by whether a text RUN has started — and every formatted block opens with a whitespace-only text node, so the run also swallows the next sibling ELEMENT. Two semantically distinct siblings fed by two different props merge into one contract string, and the fail-closed fidelity gate then demands a composite no implementation can render (`<span>要送信</span><span class="qdot">対応待ち</span>` → `"要送信対応待ち"`). The obvious guard — refuse to absorb into a run carrying no real text yet — was measured and is NOT the fix: it splits the queue tile correctly and REGRESSES the case the join exists for, because `<p>⏎<b>New:</b> rest` is also whitespace-then-element. Executed 2026-08-15 over four shapes, HEAD vs guard: HEAD wrong on the two sibling shapes and on the no-whitespace emphasis shape; the guard wrong on the leading-whitespace emphasis shape. Only the SUFFIX distinguishes them — an emphasis run continues into trailing prose in the same run, an adjacent chip does not. All 25 existing extractor tests pass either way, so nothing in the repo discriminates | workflow-defect | design | `tests/dc-extract-anchor-rule.test.js` | open | — |
| JJ-20260815-07 | hearwell brief 2026-08-15 finding HOST-20260814-01, cited to spec 20260813/01's review 2026-08-13 (`.claude/rules/spec-pipeline.md:365-374`), verified plugin-side 2026-08-15. `wf-build`'s TDD red-check validates a new test by "fails on current code, passes after", but `crossCheckSentinels()` (`spec/workflows/src/wf-build.body.js`) reads a binary `AUDIT_RED:<path>`/`AUDIT_GREEN:<path>` sentinel: module-resolution failure and a failing assertion are the SAME observation to it. When a spec's first act is introducing a new export — the common case, and precisely when a spec is most likely pinning something new — importing it makes the ENTIRE file fail to LOAD, so every assertion in it clears the red bar vacuously without ever executing. Measured consequence on that host: deleting the six words implementing the spec's headline guarantee left 9/9 worker tests green through a 2-reviewer CLEAN panel. The probe that exists to prove a pin is falsifiable therefore cannot tell "this assertion fails" from "this file does not load", and only the first proves anything. Fix contract: attribute the red — a file that executed zero assertions demonstrated nothing and fails CLOSED as unverified, the shape this function already uses for malformed sentinels and unattributed typecheckRed; the discriminator must stay runner-agnostic. Composes with rather than replaces UPWELL-20260718-01 and HEARWELL-20260804-02, which cover a file that never RAN; this covers a file that ran and proved nothing | workflow-defect | build | `tests/redcheck-load-failure-attribution.test.js` | open | — |
| JJ-20260815-08 | salon-os brief 2026-08-15 finding SALONOS-20260815-01, observed twice (specs 20260814/01 and /02, runId `wf_e4778d03-81b`), verified plugin-side 2026-08-15 (no preflight exists in `spec/commands/build.md` or `wf-build.body.js`). The gate cannot distinguish "the code under test is wrong" from "the environment the test needs was never provisioned": with `DATABASE_URL` unset the DB-backed suites failed inside `env()` at zod parse time, never reaching an assertion, and `wf-build` classified that as implementation breakage, burned a full repair round on correct code, and returned `blocked`. Both suites passed unchanged once the variable was set. The repair loop is the most expensive thing build runs AND is structurally incapable of succeeding against this class — the code it would edit is not the code that failed — so the cost is paid in full every time with a guaranteed-useless outcome. Not specific to `DATABASE_URL` or that host: any host whose pipeline rules declare an environment-gated suite has the shape, and the grounding layer already carries the remedy in prose that the workflow never reads. Adjacent open class worth resolving together rather than twice: `ac-matrix.js`'s skipped-test reconciliation reads `[env: VAR]` declarations from only the spec under review | workflow-defect | build | `tests/gate-env-preflight.test.js` | open | — |
| JJ-20260815-09 | hearwell brief 2026-08-15 finding HOST-20260814-02, observed at that host's 2026-08-14 release, verified plugin-side 2026-08-15. `/spec:release` Phase 1 names "migrations path (`exec` against staging)" in prose, but nothing requires a host's release manifest to actually carry such a row and nothing distinguishes "migrations are applied" from "migrations are applied BY THE DEPLOY". Observed: the deployed database was four migrations behind — journal 6 entries, `drizzle.__drizzle_migrations` 2, every table that milestone shipped absent from the live database. The PREVIOUS release passed its substrate leg `checked=10 failed=0` carrying the identical missing wiring, green only because the journal then happened to hold exactly the two already applied. The gap became visible only because that host chose to invent the row for itself; a host that never writes one, or writes one that passes vacuously by coincidence, gets a green substrate leg and a CLEAN milestone while shipping code whose tables do not exist. Fix contract has two load-bearing halves — a row-presence obligation for any host declaring a database, and an ORDERING rule placing the applied-vs-journal comparison AFTER the deploy, since before it a host with no migrate step is indistinguishable from one that is up to date. Presence without ordering still passes vacuously | missing-substrate | release | `tests/release-migrations-applied.test.js` | open | — |

## Rejected findings

Dedupe stamps for gotcha/escape-sourced findings dispositioned `rejected` — the next sweep
matches on the ID/signature here and skips them. Corroboration from a second host reopens.

- **CROSS-20260810-R1** (JJ report 2026-08-10, upwell spec 20260731/04 session: "build
  worktree lifecycle is create-only — merge-back never removes agent worktrees; hosts
  accumulate stale ones that pollute repo-wide sweeps; doctor should flag them"). Rejected
  2026-08-10 after re-execution: the mechanism claim does not reproduce — merge-back.sh's
  `cleanup` subcommand removes the spec worktree + branch and review.md invokes it every
  merge (upwell, where it runs constantly, has ZERO stale worktrees). The six stale trees
  measured (hearwell ×4, prax ×2, all `agent-*`, tips merged, dirty) are harness-created
  Agent-isolation checkouts the harness's auto-clean skipped because they were dirty — not
  plugin lifecycle output. The systemic damage (verifier false-kills from stale
  `agent-*` checkouts) is already fixed as CROSS-20260804-01 (6.37.0); what remains is a
  one-time `git worktree remove --force` on two hosts. A permanent doctor check for a
  solved failure mode is additive bloat. Reopens if stale worktrees re-accumulate after
  the one-time sweep, or a post-6.37.0 sweep-pollution defect is recorded.
- **ZUBU-20260716-01** (zubu-menu escape 2026-07-17, `preventedBy: review-check`): batch
  existence-check in `publishAllCore.ts` treated "not in this batch" as "does not exist"
  for refs legitimately outside the batch; escaped a CLEAN fix-delta review
  (`wf_e717bc0b-a06`), found by a later spec. Rejected 2026-07-19: the defect is host-domain
  reference semantics no mechanical review check reproduces; the ledger shows the fix-delta
  review structurally sound (smoke pass, 0 skips). Retained as density signal on the review
  surface; a second-host escape through a fix-delta CLEAN reopens. **Reopen condition fired
  2026-07-27** — prax (×2) and upwell escapes all trace to fix-delta iteration-2 CLEANs; the
  host-domain defect stays rejected, the shared channel is accepted as CROSS-20260727-01.
- **JJ-20260720-02/03/04** (same Fable hardening-review brief as JJ-20260720-01): the
  brief's three heavier companion proposals, rejected 2026-07-20. (02) per-spec
  escape-ledger worklist — the planner instantiating every recorded failure shape per run
  charges every future plan O(ledger) forever and decays into rubber-stamp "why it can't
  apply" prose (the failure mode that retired the refutation filter); the intent already
  ships as escape `preventedBy` deterministic checks. (03) runtime-behavior spike trigger —
  no observed escape in the class; the existing Phase 1.5 shape trigger covers
  dependency-adjudicated claims, and "load-bearing runtime assumption" is felt-uncertainty
  wording the trigger doctrine explicitly forbids; reopens when a runtime-behavior escape
  lands. (04) optional Fable T3 mechanism-audit pass — an expensive-model dispatch designed
  to converge on finding nothing is the retired mandatory-T3-checkpoint shape; redundant
  whenever the planner was already Fable/Opus. Corroboration from a recorded escape reopens
  any of the three.
- **PRAX-20260804-R1** (same 2026-08-04 prax user message, second issue): retainer rulings
  that prescribe a command with an expected observation arrive dense with `path:line`
  citations, so the unexecuted prediction reads as pre-verified. Rejected 2026-08-04: no
  wrong-prescription incident is recorded on any host; the role brief already mandates an
  explicit could-not-verify closing line, which covers an honest retainer. Same precedent as
  JJ-20260720-03 (no observed escape in the class). The first retainer command-prescription
  that escapes wrong reopens this as an accepted row.
- **HEARWELL-20260804-R1** (hearwell gotcha, spec 20260731/02 review): `review:1` (the
  design-integrity emphasis) died twice on the StructuredOutput retry cap while
  schema-probing, re-invoke could not recover the slot (an errored agent leaves no journal
  cache entry), and the leg never ran. Rejected 2026-08-04: `REVIEWER_FAILED` fails closed —
  integrity holds, the defect is availability on a single host's single run; a documented
  recovery path (re-dispatch the dead emphasis with a model/effort change) is the fix shape
  when it recurs. A second host, or a second run on any host, reopens.
- **PRAX-20260813-03** (prax brief 2026-08-13: inline `Agent` build dispatch does not inherit
  wf-build's test-author separation — spec 20260810/04's AC-11 carrier was authored by the same
  agent that wrote the implementation it pins). Rejected 2026-08-13 after re-execution: the
  premise "no carrier on the sanctioned alternate path" does not hold — build.md's fast path
  step 1 dispatches the test-author as its own `Agent {subagent_type: agentMap.tests}` BEFORE
  the implementation dispatch of step 2, and states "the fast path changes the execution
  vehicle, not the contract". The incident is a session departing from the documented path —
  host-side; suggested re-tag `[host]`. Reopens if an incident lands through a documented
  dispatch path that genuinely lacks the separation instruction (e.g. a repair dispatch shown
  editing both test and implementation under instruction).
- **ZUBU-20260717-01** (zubu-menu escape 2026-07-17, `preventedBy: runtime-leg`): hard
  defect in `migration/src/strip-orderrank.ts` after review `wf_3f0cf1f0-06d`. Rejected
  2026-07-19: that review returned SURVIVORS (1 survived, 1 waived, 0 fixes dispatched) —
  consistent with an unactioned survivor — and one-off migration scripts sit outside the
  boot-smoke runtime leg's declared scope; no reproducible plugin mechanism identified.
  Retained as density signal; corroboration reopens.

## Adding a row

`/intake` appends rows; nothing else writes here. Every row needs all seven columns. `Pinned
by` names the failing-then-passing test (or, `pre-contract`, the doc/ledger artifact); a row
whose pin is neither is invalid — do not land it. When a fix ships, update `Fixed in` in the
same commit that lands the fix, so host doctors and this repo never disagree about what a
version contains.

`Fix` records how a closed row was actually fixed: `mechanism(<repo path>)` when a script,
gate, or test carries the discipline, or `prose(<impossibility reason>)` when none is
possible — an open row stays `Fix: —` until it closes. A fix spanning several files cites
each one, comma-separated (`mechanism(a), mechanism(b)`); every citation is checked
independently, so one dangling path still fails the row. A row whose Category already appears
earlier in this table may only close `prose(...)` with a stated reason (≥ 20 chars); otherwise
it must land a `mechanism(<path>)`, enforced by `tests/intake/intake-discipline.test.js`.
