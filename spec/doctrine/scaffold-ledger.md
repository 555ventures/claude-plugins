---
description: Registry of every distrust/guard mechanism in the spec pipeline — gate, advisory, or structural — each with its justifying incident/measurement and the condition that promotes or retires it; read by /spec:doctor's scaffold audit
---

# Scaffold Ledger: Distrust & Guard Mechanisms

Every harness component encodes an assumption about what the model can't do on its own —
and those assumptions must be stress-tested each model generation (Anthropic's harness-design
principle). This file is where the pipeline's assumptions are named, dated, and made
falsifiable, so "we've always had this check" never substitutes for evidence. The lifecycle
rule: a mechanism is **ADVISORY** until measurement promotes it to a **GATE**; a **GATE** is
**RETIRED** when its justifying data stops holding for the current model generation; **no
mechanism ships without naming the measurement that would retire or promote it.** A row with
no promote/retire condition is a mechanism nobody has agreed to ever remove or upgrade — that
is a defect in the row, not a property of a permanent mechanism (permanence is still a
condition: "retire never," stated, with its reason).

| Mechanism | Kind | Justification (incident/measurement, dated) | Earned under | Promote/retire condition |
|---|---|---|---|---|
| Worker git ban | gate | A worker's repo-wide `git checkout .` destroyed sibling workers' uncommitted edits (pre-2026). | Sonnet 4-era | Retire never — cheap, structural, zero-token. |
| Closed-alphabet args (no free text) | structural | Args JSON corruption misdiagnosed for 3 sessions as a size cap (2026-06). | Harness-level, model-independent | Retire only if the harness guarantees object delivery (no string-vs-object encoding inconsistency left to defend against). |
| Gate sentinel + self-contradiction guard (`__GATE_PASS__`) | gate | A Haiku gate-reader reported a non-zero typecheck as clean. | Haiku 4-era | Retire if gate execution moves out of model hands entirely (a script reads the exit code directly; no model narrates pass/fail). |
| Fail-closed reviewer (`REVIEWER_FAILED` ≠ `CLEAN`) | gate | A crashed sole reviewer would otherwise return a false CLEAN. | Structural | Retire never. (Enforcement re-homed into the "Verdict derivation" row below, 2026-08 — `REVIEWER_FAILED` is derivation-order's first match, still never `CLEAN`.) |
| Refutation filter (claim-only refuters) | gate — RETIRED v5 | 2026-07 ledgers: killed only 4 findings across the full window; execution audit overturned 2 of 3 audited kills. | Sonnet 4.5-era | Retired — argument-based kills ran an unaffordable false-kill rate; replaced by execution-grounded verification. Re-promote only if a future ledger shows argument-only kills recovering a low false-kill rate. |
| Mandatory T3 retainer checkpoints | gate — RETIRED v5 | 2026-07 ledgers: 100% PASS across every measured run. | Opus 4-era | Retired — a gate that never blocks is spend, not signal; surprise-driven consults remain. Re-promote if a future ledger shows a checkpoint catching a real issue. |
| Execution-grounded verification (kills need repro/sanction/miscitation) | gate | Same 2026-07 measurements (2/3 audited kills wrong). | Fable 5-era | Retire per-severity if two consecutive quarters of escape data show zero killed-and-real findings. |
| Behavioral evaluator (verify skill driving ACs) | advisory | Introduced v5 on Anthropic's measured verification-skill ROI; no local ground truth yet. | Fable 5-era | PROMOTE to gate when the ledger shows its verdicts track escapes; retire if two quarters of data show no signal. |
| Vision design review | advisory — RETIRED 2026-08-10 | Introduced v5; catches render-vs-mock divergence no deterministic check can. | Fable 5-era | Retired — replaced by the unified `FIDELITY_REVIEW` exit review (row below): the config-gated `VISUAL` step and this silenceable advisory consult were two overlapping, both-weak mechanisms; user-confirmed unification into one always-offered post-gate moment. |
| Driver-stepped design session | structural | Resumed sessions unreliably reconstructed phase state from conversation; state moved to disk (v3). | Opus 4-era | Keep while zero-token; re-evaluate only if the driver itself becomes a maintenance hotspot. |
| Diff-scaled review panel | gate-sizing | 2026-07 ledgers: a 197-loc diff drew a 308K-token review; 61% of review spend returned CLEAN. | Fable 5-era | Re-tune thresholds each quarter from ledger loc-vs-findings data. |
| Roadmap as genesis phase (no standalone /spec:roadmap command) | structural | UpWell (2026-07): the genesis chain ended with no plannable unit — the user hand-authored docs/roadmap/ to make /spec:plan invocable. The artifact was proven needed; a separate command would re-pay for context genesis already holds hot, and an optional command is skippable-by-default (the exact observed failure). | Fable 5-era | PROMOTE to a /spec:roadmap command when a brownfield (non-genesis) project needs a roadmap and the template-only path (roadmap-overview.md + roadmap-brief.md, hand-invoked) proves insufficient in practice. RETIRE the phase if two genesis projects discard or ignore their generated roadmaps. |
| Boot smoke leg (`__SMOKE_PASS__`, CLEAN requires it) | gate | UpWell (2026-07): 8/8 static gate tasks green + two CLEAN reviews on an app returning 500 on every commit — a verification stack of static legs passed a program that cannot start; the catch was a free curl nobody ran. | Fable 5-era | Retire only if the release stage's deployed checks subsume it AND two quarters of escape data show zero smoke-only catches. The inert declaration path retires per-host, never the leg. (Enforcement re-homed into the "Verdict derivation" row below, 2026-08 — `smoke` is a required, blocking leg in `verdict.js`'s manifest.) |
| Deliverable manifest check (init stamps only after `manifest-check` exits 0) | gate | UpWell (2026-07): init stamped `generatedBy` complete having never written Deliverable #6 (the verify skill) and having authored CI that executed zero times — the one LLM whose output got no mechanical verification was init itself. | Fable 5-era | Retire never while init generates artifacts (structural: authored ⇒ activated must be checked by something that isn't the author). Re-tune the row set from doctor activation findings. |
| Skipped-test reconciliation (AC↔test matrix counts executed, not collected) | gate | UpWell (2026-07): 10 env-gated DB suites — the only tests touching the real dependency — never ran anywhere; a test holding a defect the dependency rejects with a throw sat skipped through two CLEAN verdicts reported as green. | Fable 5-era | Retire only if the ledger shows two consecutive quarters with zero skipped-AC hard findings AND all env-gated suites running in CI. The `[env:]` declaration path is the per-AC exemption, never a retirement. (Enforcement re-homed into the "Verdict derivation" row below, 2026-08 — `skip-reconcile` is a required, findings-producing leg in `verdict.js`'s manifest.) |
| Shape-triggered micro-spike (dependency-adjudicated claims execute before lock) | gate | UpWell (2026-07): ADR-0006 bound `domain:action` queue names; the same-day-pinned dependency rejects colons in its own validation — panel-reviewed, research-backed, falsifiable in one executed line nothing required to run. `--spike` triggered on felt uncertainty, and ADR-settled claims feel settled. | Fable 5-era | Retire per-claim-class if two quarters of plan/genesis ledger data show zero executed-check falsifications in that class; the trigger stays shape-based, never felt-uncertainty-based, while it lives. |
| Affordance ↔ contract reconcile (design-exit matrix, blocks `designed:`) | gate | UpWell spec 03-chat-ui (2026-07): a designed card rendered a re-confirm affordance on declined items through the same handler as proposed ones; the server throws VALIDATION on that transition; build wired it as-is because the component's shape said to — design output entered build with a spec's authority and none of its scrutiny. | Fable 5-era | Retire if two quarters of design-capable-host data show zero matrix catches; downgrade to advisory if catches are all false positives (matrix flags transitions specs deliberately left server-rejected). |
| Doctor `--fix` repair path (evidence-cited line-item doctrine repair) | structural | UpWell (2026-07): a verified-wrong convention lived in generated doctrine with no sanctioned writer short of full re-init; the jobs agent was instructed to reintroduce a crash on every future spec, patched around by per-spec override Decisions forever. | Fable 5-era | Keep while regeneration ownership is init-wholesale (a repair path is the complement of ownership); re-evaluate if init refreshes become cheap enough to run per-correction. |
| Prevention delta on escapes (`preventedBy` required, `none` explicit) | structural | UpWell (2026-07): the escape ledger existed, qualified incident occurred, ledger empty — a ledger nobody writes to or reads is a confession booth; nothing converted an escape into a doctrine/enforcer/check change. | Fable 5-era | Retire the field only if two quarters show every value is `none` (then escapes genuinely carry no actionable signal); if `doctrine` rows dominate and never land, the loop is broken at step 6 — fix the offer, not the field. |
| Release stage executed checks (deployed e2e + journey walks + per-SHA CI check + confirm-gated promote) | gate | UpWell (2026-07): per-spec CLEAN verdicts don't compose — deploy drain, live-mode config, and cross-spec seams were unverifiable from any per-spec review; "ready to hand over" had no owner and no executed evidence. **2026-08-10 addition (spec 20260810/07):** the release stage gained an authoritative per-commit `ci` leg (Phase 2, `--commit` on the release SHA, bounded 30s/10-min in-progress re-poll, mandatory ⚠️ report line when CI delivered no verdict) — CI is now part of the executed evidence set that derives the milestone word. | Fable 5-era | Re-tune the journey set per milestone from release-row failures; retire a manifest row class only when its platform automates the same check (cite the platform doc). Promotion confirmation retires never (production actions are never autonomous). Delete `verdict.js`'s release profile (`--profile release`, `--milestone`, `--briefs`), its exec pins in tests/review/verdict.test.js, and release.md's wiring together if two consecutive quarters across hosts record zero `stage:"release"` ledger rows (registered 2026-08-07: zero rows to date). |
| Question-style gate (`question-style-gate.js` rejects consequence-free AskUserQuestion options) | gate | 2026-07-17: a live fork question shipped as "Closed z.enum (Recommended)" vs "Open string key" — code snippet as preview, no descriptions; the user had no way to know what either choice cost. Question Style doctrine alone (v6.10) produced no observed change — doctrine is the weakest prevention tier by this ledger's own taxonomy. | Fable 5-era | Structural checks only (descriptions present, consequence-length, recommendation reasoned, identifier density) — taste stays doctrine. RETIRE if a quarter of bounced-question data shows mostly false positives (legit terse questions); RE-TUNE thresholds from the same data. |
| Question judge (Haiku tier-2 in `question-style-gate.js`: ten-second cold test + derivability screen, fail-open) | gate | JJ (2026-07-21): a /spec:plan question round passed the syntactic tier while still all-mechanism — "allowlists, loading paths, per-package runners" with zero backticked identifiers — and 2 of 3 questions were derivable (one option literally was the bug being fixed); the user had to reject and re-teach mid-session. Concept load is a judgment property no deterministic check measures; second doctrine-only failure for this rule class, so the taste-stays-doctrine stance of the row above is amended: taste that keeps escaping gets a judge. | Fable 5-era | RETIRE (drop back to tier 1 only) if a quarter of bounce data shows mostly false blocks; PROMOTE the judge model to Sonnet if feedback/escape data shows mechanism-framed questions still reaching the user; RE-TUNE derive-verdict aggression if intake shows silently-wrong `📌` auto-picks landing. |
| Marker counter (`open_markers:` frontmatter authoritative over prose grep) | structural | UpWell (2026-07): the state gate's prose grep was trippable by DESCRIBING the marker syntax in Rationale — plan doctrine had to warn authors never to quote it; a gate trippable by narrating it is the wrong mechanism. | Fable 5-era | Keep while zero-token. The grep fallback retires when no host has pre-counter specs in flight (one plugin major version, then drop). |
| Escape derive-then-confirm (classification derived from session context, one confirm call; `killedMatch` never derived; drafted prevention-delta line severable) | structural | JJ (2026-07-17): the four-question interview sat at the recording moment — the exact point where friction converts real escapes into an empty ledger, and "zero escapes" into false review-trustworthiness evidence; the same capture-by-command failure row 38 documents, at the field level. | Fable 5-era | RETIRE (back to per-field asking for that field) if intake/doctor data shows silently-wrong derivations landing — escape rows whose classification is later contradicted by the fix or corrected post-hoc. `killedMatch` derivation stays forbidden regardless (its content is unrecoverable from any context). |
| Goal→mechanism audit at lock (every Goal promise traces to a delivering Decision + red-capable AC) | gate | JJ Fable hardening-review brief (2026-07-20, intake JJ-20260720-01): a Sonnet-planned spec promised an outcome no locked Decision delivered; ACs written from Decisions stayed green in the mechanism's absence, Phase 3 refuters attack claims made not promises unmade, and build/review verify diff-against-spec — the class survives the whole pipeline. User-attested, not re-executed at intake. | Fable 5-era | RETIRE if two quarters of plan-session data show zero promises blocked or repaired by the audit — a lock check that never fires is spend. While it lives it stays an in-session lock bullet: never an emitted table, never an agent dispatch (the brief's expensive-model variant was rejected as the retired mandatory-T3-checkpoint shape). |
| Red-check verdict = expectation × observation (orchestrator classifies each test file red/green from AC vocabulary; probe observes both runtime and typecheck legs; zero-collected = invalid observation, not green; all-green specs skip the probe) | gate | Intake 2026-07-27: six failure records across three hosts on the old "did the runner fail" contract — typecheck-red tests falsely "not red" (hearwell wf_a6f50d23-f17), sanctioned-green carriers looping builds into fastPath abandonment (prax specs 20260725/01+08, hearwell design-landed components — the 6.25.0 literal-phrase exemption broke twice in a month), and workspace-filtered runners collecting zero files (upwell spec 20260718/01). Prompt-clause patching was the generating mechanism; the redesign moves expectations into data and red-ness to gate-equivalence. | Fable 5-era | RE-TUNE the carrier classes from tdd-red-check deviation data (a new sanctioned-green shape = extend Phase 0 classification, never a prompt clause). RETIRE the typecheck leg if a quarter of red-check data shows it deciding zero verdicts. A recurrence of fastPath-forced-by-red-check in any host ledger means the classification missed a class — reopen PRAX-20260726-01's pin and extend it. |
| Feedback loop (release brief flush + shipped intake ledger + doctor upstream-fixes check) | structural | UpWell (2026-07): the plugin's defects lived as host folklore — each host re-paid the discovery ([plugin] gotchas had no upstream destination), and a fixed defect had no return signal (the D13 override doctrine-patch stood after the fix shipped, with nothing telling the host to retire it). Capture-by-command had already failed empirically (escape ledger empty through a qualifying incident) — signals must accumulate as side effects, flush at milestones. | Fable 5-era | Retire the flush if two quarters of briefs across hosts yield zero accepted intake findings (the channel carries no signal). The upstream-fixes check retires only if hosts stop pinning `generatedBy` (nothing left to compare). The intake contract (accepted = failing test first) retires never while the plugin has a test suite — it is the manifest-check principle applied to the plugin itself. |
| Review scope reconciliation (`scope-reconcile.js`: unscoped diff ∪ untracked vs File Plan; out-of-plan changes = mechanical hard finding, planned-but-untouched = mechanical soft finding; build's Final gate runs it advisory) | gate | 2026-08 confirmed host escape: `/spec:review` diffed only the File Plan's named directories, so an out-of-plan `waitForExit` edit rode a CLEAN verdict into production — the reviewer never saw it because the diff command itself excluded it, no reviewer diligence could have caught it. | Fable 5-era | Retire the hard-finding severity (downgrade to advisory) if two quarters of ledger data show out-of-plan findings are always waived as sanctioned scope creep, never a real defect. Retire never for the mechanical diff-vs-plan derivation itself while the File Plan remains a prediction reviewed against — reintroducing a File-Plan-scoped diff recreates the incident class by construction. |
| Design harness check (`design-atlas.js check` before any human look) | gate | v6 (2026-07): the CD-era fidelity economy showed value drift (off-token literals, unlabeled screens) is the dominant mock defect class and is fully mechanical to catch; catching it pre-human keeps critique rounds about taste, not hygiene. | Fable 5-era | Retire per-check if a quarter of explore/atlas runs shows zero catches for that check; the label check retires never while `regionRef`s anchor on labels. |
| Render→screenshot→critique loop (mock authors must see their work) | gate (explore) / advisory (elsewhere) | v6 (2026-07): the capability analysis of why Claude Design out-renders blind generation — sees-its-own-work was the largest replicable ingredient; unverifiable mechanically when no browser capability exists, so advisory. **PROMOTE fired 2026-08-07**: the amateur-tiles incident (spec 20260807/05-explore-taste-channels) — Round 0's critique leg was conditional on browser availability in exactly the round the look is born, and tiles read as amateur work with nothing mechanical failing "looks amateur." `/spec:genesis-explore` now hard-requires the capability at Setup (STOP + remedy naming Chrome/Playwright) and refuses candidate presentation without a recorded render→screenshot→critique per tile — the gate scope is explore only; atlas sweeps and `/spec:design` keep the advisory (shared § Design Canon, its skip-with-note clause — unchanged there, D5). | Fable 5-era | PROMOTE the remaining advisory scopes (atlas, `/spec:design`) to gate if their evidence shows the same blind-authored-worse delta measured in explore. RETIRE the explore gate back to advisory if two genesis-explore runs show the Setup block firing spuriously (a render capability was actually present but undetected). |
| Starter-tokens additions-only diff (`git diff <positions-authored commit> -- 'design/explore/r0-*/tokens.css'` at the explore critique step; any changed/deleted session-authored token line is a builder violation, sent back with the diff, counts against the two retries) | gate (explore) | Spec 20260807/05 (2026-08-07 amateur-tiles incident, root cause): Fable's taste reached builders only as lossy prose; the fix makes the session-authored starter `tokens.css` the taste carrier (D1), but D1's "builders append, never alter" rule had no mechanical carrier until this diff check — review finding 2026-08-08 registered it here. | Fable 5-era | RETIRE back to advisory if two genesis-explore runs show the check bouncing legitimate builder work (e.g. necessary value fixes misread as violations). PROMOTE to other token-consuming stages (atlas edit loops, `/spec:design`) if their ledgers show builder-altered authored tokens riding through unchecked. |
| Explore funnel (tiles → cull → prototypes → pick, before genesis-design locks) | structural | v6 (2026-07): tokens/canon lock at genesis-design; direction judged after the lock is re-work. Prior flow offered zero multi-candidate support (`variantProposals` = re-theme only) — taste was committed on the first rendered direction. | Fable 5-era | RETIRE Round 0 (go straight to 2 prototypes) if two projects' picks always match the session's pre-ranked favorite (the cull adds no information); retire the stage if two projects skip it and report no regret at genesis-design. |
| Explore state gate (genesis-design blocked until `explore: picked`/`skipped`) | gate | v6 (2026-07): same lock-ordering evidence; a coarse hook is the only thing preventing the pick from being silently skipped on re-entry. | Fable 5-era | Retire with the funnel row above; the legacy-absent-field pass lives one major version, then absent = pending. |
| Atlas gap cards (declared-but-unmocked surfaces render as explicit gaps) | structural | Prax roadmap (2026-07): brief 01's login — the product's front door — was design-marked "no" and shipped no mock; the orphaned surface was caught by a human re-read, not by any artifact. Zero-token to derive. | Fable 5-era | Keep while zero-token; re-evaluate the `surfaces` grammar if two hosts' briefs stop declaring surfaces (the input starving is the real retirement signal). |
| Design targets matrix (`design/targets.json`: declared themes × viewports; check enforces viewport meta + dark tokens block at `data-status="approved"` / `--matrix` — matrix-at-approval; draft framing = most-constrained viewport; approval two-step: direction then matrix confirm; atlas/gallery render the matrix) | gate | v6.1 (2026-07): the canon previously bound only the desktop-light framing — dark mode and small viewports were unspecified until build, where they surfaced as review findings instead of mock decisions. One responsive file per surface (per-device variants drift); absent file = legacy pass. | Fable 5-era | Keep while zero-token. RETIRE the enforcement (keep the toolbar) if two hosts' matrix violations are always trivially fixed without a human ever seeing them differently — that means responsiveness became a solved default, not a decision. |
| Rule-checklist pass (Sonnet checker walks research-brief rule IDs against every mock at direction approval + a review-panel leg on UI specs) | gate | v6.1 (2026-07): taste/psychology rules relied on authoring sessions remembering them — not an enforcement mechanism in stateless sessions; the brief's falsifiable-rule bar exists precisely so a stranger-checker can verify. | Fable 5-era | RETIRE the approval-time pass (keep the review leg) if two hosts show it never files a violation the review leg wouldn't also catch; PROMOTE rules it repeatedly cites into /spec:enforce mechanization where encodable. |
| Component manifest + author-justification gate (`design/components.json`; every author decision names nearest entry + why it fails; review verifies; vocabulary-extended 2026-08-10 with an optional `boundaries` field and genesis-seeded commitment entries, structurally distinguished from landed entries by absent `props`/`mockRefs`) | gate | v6.1 (2026-07): component duplication is a default model failure — "check the catalog first" prose demonstrably decays; the gradient must make reuse cheaper than creation (same inversion as the token near-match rule). | Fable 5-era | Keep while duplication findings occur; RETIRE the justification field (keep the manifest) if two hosts go two milestones with zero review findings against it — the manifest alone would then be carrying the weight. |
| Component vocabulary check (`components-check.js`; fail-closed in `/spec:genesis-design`'s commit step, advisory in the design driver's preflight) | gate (genesis) / advisory (design driver) | 2026-08-10 ruling: `design/components.json` gained a `boundaries` field and commitment-entry semantics with no deterministic shape check — a malformed entry (missing `purpose`, duplicate `name`, non-array `boundaries`) would silently corrupt the manifest both `wf-design` workers and review read as binding canon. | Fable 5-era | PROMOTE the driver leg to gate if two hosts show a malformed manifest reaching `/spec:design` undetected; RETIRE the genesis fail-closed gate only if genesis stops writing the file that leg validates. |
| Unified exit fidelity review (`FIDELITY_REVIEW` state, `fidelity-reviewed` mark; replaces `VISUAL`/`visual-done` and the advisory vision-review consult) | advisory | 2026-08-10 ruling: two overlapping, both-weak mechanisms — a config-gated visual step and a silenceable advisory consult — left render-vs-mock divergence catchable only by accident; one always-offered post-gate judgment moment (user-confirmed) replaces both. | Fable 5-era | Promote/retire on the same catalog-loop hit-rate measurement the retired "Vision design review" row used; a legacy `visual-done` mark still satisfies the new state for resume compat. |
| Mock-authority inversion (mock is law until `built`, then code is truth; staleness displayed never owed; lazy re-sync at next design touch) | structural | v6.1 (2026-07): mandatory continuous mock sync either rots into lies or taxes every bug fix — both destroy the canon's credibility; authority with an expiry keeps contracts honest at zero recurring cost. | Fable 5-era | Re-evaluate if a host shows repeated build-time defects traced to stale mocks being read as canon (would mean lazy re-sync fires too late — tighten to re-sync-at-plan). |
| Session grounding trio (path-scoped rules incl. `.claude/rules/conventions/`, generated settings `permissions` block, run+verify skills with `allowed-tools`; doctor flags zero-match `paths:` globs) | structural | v6.2 (2026-07): research sweep vs the mid-2026 Claude Code baseline — always-loaded pipeline rules compete for adherence in every session (upstream guidance: ambient rule files degrade rule-following as they grow; path-scoped rules load only on file match); permission prompts push hosts toward permissive modes that discard the safety layer; the run/verify skill pair is the ecosystem's per-project default that init previously half-shipped. Externally earned, no host incident. | Fable 5-era | RETIRE a piece if two quarters of doctor runs across hosts show it inert (zero-match-glob check never fires and convention rules are never cited by a review finding; permissions block untouched by any host). Re-derive the allow/deny entry shapes whenever the harness's permission-rule grammar changes — the shapes are baseline-borrowed, not plugin-invented. |
| `predicts:` observable on research-brief rules (UX-escape hook) | structural | v6 (2026-07): behavioral rules previously carried no falsifiability route — a taste rule could never be promoted or retired on evidence; one line per rule buys the hook before any host has telemetry. | Fable 5-era | PROMOTE to a wired UX-escape loop (telemetry scores rules, `/spec:escape`-style promote/retire) when a host ships telemetry; retire the field if two projects with telemetry never act on a verdict. |
| Representation parity lint (`parity-check.js`: one invocation per ADR-named surface; identifier word-structure parity + wire timestamp form; genesis Phase B fail-closed, doctor check 16 re-run) | gate | PRAX-20260717-01 (2026-07): the same seam identifier shipped as `runId` in both contract planes against the ADR's own `run_id` Logging row; three id spellings landed in byte-locked artifacts; a `+00:00` timestamp survived to review in a UTC-`Z` codebase — all string-level detectable, all human-caught after the fact. Incident-earned + deterministic + cheap = ships as gate, not advisory. | Fable 5-era | RETIRE per-check if two quarters of host doctor runs show zero catches for that check while builds are landing contract files (the class stopped occurring); downgrade to advisory if catches are dominated by false positives (legitimate same-plane variants the ADR deliberately allows — would mean the plane grammar, not the lint, needs work). |
| Ops-conventions derive pass + coverage check (generating question stated in genesis Phase A; derive proposes rows the dictated floor missed, coverage check re-reads the finished table — outputs added rows or nothing, never certification) | advisory | PRAX-20260717-01 (2026-07): floor-not-ceiling licensed exceeding the dictated list but gave executors no method — prax exceeded it exactly once, by luck. Advisory because the coverage checker shares the deriver's blind spots (same-model correlation). | Fable 5-era | PROMOTE to a dictated-row addition (fold the derived category into the floor) each time a derived row class recurs across two hosts. RETIRE the coverage check if two quarters of genesis runs show it adding zero rows the derive pass didn't already propose (pure ritual); the generating-question text itself is prose, not a mechanism, and stays. |
| Doctrine-diff review panel (plugin-repo `/doctrine-review`: two uncorrelated executor-perspective reviewers over the spec/commands+doctrine diff before each version bump) | advisory | 2026-07-17 dual independent design review (Fable + Opus red-team of the v7 hypothesis): both converged on doctrine diffs as the sole unreviewed artifact class — the recurring intake classes (closed lists: PRAX-20260717-01; ambiguous stage ownership: 6.4.1 carve-out) were write-time-detectable by an executor-perspective read, but every instance was detected in a host after a live incident. Enters advisory on the T3/refutation-filter evidence against speculative gates. | Fable 5-era | PROMOTE to bump-blocking when a recorded panel finding, left unfixed, later lands as an intake row (the panel demonstrably predicts incidents). RETIRE if 10 consecutive recorded runs produce zero findings that survive to a fix (spend, not signal). |
| Status derivation script (`spec-status.js`: the one derivation behind /spec:status, doctor check 14, and plan's `--brief` dependency preflight; skip/orphan/hand-tracked-status anomaly lines) | structural | JJ (2026-07-19): brief/spec completion was tracked by memory across sessions — briefs got accidentally skipped and back-filled after later work landed on top; the derivation rules existed only as doctor-check-14 prose greps a model re-executed per run (a session cost nobody paid between sessions, so nobody looked). | Fable 5-era | RETIRE an anomaly kind if two quarters of use show zero true catches for it. The derivation itself retires never while statuses are derived-not-stored — it is the viewer that makes that rule livable; if it goes, hand-tracked status columns return. |
| Derived next-pointer (`spec-status.js --next`: the status→command mapping incl. `designed:`-aware design routing; /spec:status section 2 and the review-close Next line print it verbatim) | structural | JJ (2026-07-22): the end-of-run "Next:" suggestion had no doctrine producing it — freehand improvisation from stale conversation context, repeatedly contradicting the derivation (a dependency claimed satisfied that wasn't; specs with `designed:` set re-routed to /spec:design because nothing parsed the stamp). The mapping existed only as renderer prose the model re-applied per run. | Fable 5-era | RETIRE never while statuses are derived-not-stored — the next-pointer is the same viewer rule (row above) applied to the "what now" question; freehand next-lines return the moment it goes. RE-TUNE the ordering (closest-to-done first) if user corrections show the top pick consistently overridden. |
| `/spec:sketch` ratification (`data-status="ratified"` set only at exit readout) + `/spec:plan` mock-intake warn | advisory | v6.3 (2026-07): roadmap-level mock iteration had no scoped home — the atlas sweep was all-or-nothing and per-brief brainstorms silently outran their briefs (a mock gained a capability the Scope never named), so plan read stale intent. Sketch scopes design to one brief, triages every change into its binding home in-round (brief first, mock second, architecture flags to Open questions/amendment ADRs), and the exit readout stamps mock↔brief agreement; plan reads the mocks and warns on unratified UI briefs. | Fable 5-era | PROMOTE the plan warn to a block if a review/escape row traces a shipped defect to a brief that skipped ratification. RETIRE the `ratified` tier if two projects' exit readouts never produce a brief edit (the readout would be theater — sketch alone kept homes coherent). |
| Amendment ADRs propagate at decision time (`Applies to` list + `Amended by ADR-NNNN` backlinks + letter-suffixed successor for consumed briefs; roadmap deltas/ retired) + doctor check 17 bidirectional link audit + plan Phase 0 unpropagated-amendment net | gate | PRAX-20260721-01 (2026-07-21): a delta binding 01-app-spine carried the security rules for a T3 auth change (OAuth account-linking hazard); the brief predated the delta so cited nothing, plan.md read only brief-cited deltas, and the delta was silently skipped — corroborated externally: OpenSpec's tracker documents silent overwrites/orphaned deltas in the same merge-later model, and ADR practice documents "decision drift" (recorded, never applied) with pointer-only tooling. Root cause is the artifact class: work living outside the numbered sequence the user walks. | Fable 5-era | RETIRE the plan-time net if two quarters of host runs show doctor check 17 catching every miss first (redundant net). RETIRE never for propagate-at-decision-time itself while the roadmap sequence is the only work queue — reintroducing a pending-amendment artifact recreates the incident class by construction; revisit only if multi-author parallel roadmap editing (the one documented delta advantage) becomes real. |
| Verdict derivation (`verdict.js`: fresh per-iteration evidence manifest + workflow return + disposition counts → one word, first-match-wins order; `ci-query.js` normalizes the CI leg via `--commit`/`--branch` keyed modes, shared with spec 03's `observe-ci.js`) | gate | 2026-08 refuter-demonstrated hole: a fix-delta CLEAN could ride pre-fix gate rows (stale evidence), and a zero-findings panel could return CLEAN with nothing executed — the CLEAN definition was prose a model applied, not a value a script computed. Subsumes and re-homes the enforcement of three prior rows in this ledger: fail-closed reviewer, boot smoke leg, skipped-test reconciliation — each stays its own row above (its own incident/justification), but its mechanical teeth are now `verdict.js` required-leg membership. **2026-08-10 re-tune (spec 20260810/07, Prax stale-CI incident):** the ci leg blocked a review on evidence about a *different* commit (branch-latest keying caught a stale pre-spec red run) — `ci-query.js` gained a `--commit` mode and review/release now key the ci leg to the exact reviewed/released commit; `ci` stays in `REVIEW_BLOCKING` and joins `RELEASE_LEGS`, required-leg membership otherwise unchanged. | Fable 5-era | RETIRE never while any stage's verdict word must originate somewhere — reintroducing prose-asserted CLEAN recreates the incident class by construction. RE-TUNE the required-leg set (and the blocking vs findings-producing split) if a quarter of review-row data shows a leg never deciding a verdict, or a findings-producing leg should have blocked. |
| Post-verdict observation window (`observe-ci.js` checks the default branch's latest completed CI run once per invocation, no daemon; a red run attributes by ancestry to the latest-closing done spec whose close commit it contains and appends one `ci:"red"` row, a later green run's ancestry containing that close commit appends a clearing `ci:"green"` row, everything else is silent; `spec-status.js` derives `observation: n/a|ok|red` — no certification sub-state, nothing renders for `ok` — and turns a red observation into the dashboard headline plus the `--next` top pick, suppressing the plan-next-brief fallback; `/spec:escape` D8 derives its evidence from the row and refuses to record against an unimplicated spec) | gate | 2026-08 confirmed escape: the window between `/spec:review` flipping `done` and the first CI run on the landed code was unowned — a real defect lived undetected in it for days, and a green dashboard coexisted with a silently red run of the landed code the whole time. 2026-08-07: the owner retired the certification half (`pending`/done-unobserved) directly — merge-back never pushes, so historical specs' closes can never be validated by ancestry against a run, making `pending` a permanently unsatisfiable state whose only visible effect was pushing 🎯 Next off-screen (specs/20260807/01-observation-red-alarm.md); the red-alarm half, which has the confirmed incident behind it, survives whole. | Fable 5-era | RETIRE the headline/–-next escalation (keep plain ledger recording) if two quarters of ledger data show zero red observations ever routed to a recorded escape (the escalation would be firing with no one acting on it). PROMOTE the ancestry gate (D4) to also require a second independent CI provider's confirmation if two quarters show `git merge-base --is-ancestor` false-accepting a run against a since-force-pushed branch (not yet observed as a failure mode; the gate's current bar is single-provider ancestry only). |
| Intake class discipline (`tests/intake/intake-discipline.test.js`: accepted-findings `Fix` column required on every row; a row whose Category repeats an earlier row may only close `prose(<reason>)` with a ≥ 20-char impossibility statement, else it must land `mechanism(<path>)`; closed vocabulary list lives in the test) | gate | 2026-08 measurement: 5 prose patches in 3 days on the dashboard seam and 3 versions in 3 days on deltas/fidelity-check — recurring incident classes were getting re-patched in prose instead of gaining a mechanism, with no record forcing the second occurrence to escalate. | Fable 5-era | RETIRE the reason-length floor (accept any non-empty prose) if two quarters of intake data show zero `prose(...)` closes gaming the 20-char bar with vacuous text (review would already be catching those, making the floor redundant). PROMOTE the vocabulary list to require a linked scaffold-ledger row (not just a path) if two quarters show `mechanism(<path>)` citations pointing at test files with no ledger entry, i.e. the mechanism itself was never registered as a guard. |
| Claims registry (`claims-lint.js`: every blocking-consequence claim in `spec/commands/*.md`+`spec/doctrine/*.md`+`spec/agents/*.md` carries an inline `enforcedBy:`/`unenforced:` marker; a dual line-count/orphan ratchet against `claims-baseline.json`, exact-match either direction) | gate | 2026-08 measurement: 5 prose patches in 3 days on the dashboard seam — mechanism work kept getting re-patched in prose with no floor forcing a claim to name its real carrier, and the corpus grew 317→404 lines across two specs with no visible ratchet catching it. | Fable 5-era | PROMOTE-equivalent already gate on land. RETIRE the marker requirement if two quarters of `tests/claims/` + doctor check 18 data show zero orphan-claim findings ever correspond to a real missing carrier (the bar would be firing on claims that were never actually unenforced). RETIRE the line-count half of the ratchet if two quarters show zero baseline-drift findings catch anything but the sanctioned `--update-baseline` remedy commit (the ratchet would be pure ceremony). |
| Ratchet-mode architecture enforcers (`duplication`, `cycle` categories: per-host baseline snapshot at `/spec:enforce` Phase 4 wiring; gate wired in no-new-violations form; manifest entries carry `baseline: {path, establishCmd}`) | advisory | 2026-08-10 research session (four-agent fan-out over 2025–26 practice): ratchet/no-new-violations baselines are the mainstream debt gate (SonarQube Clean-as-You-Code, Notion's lint ratchet, Betterer); duplication is the #1 AI-signature smell (GitClear: copy-paste ~5× refactoring, 8× duplicated blocks), dependency cycles/boundary erosion next; periodic-only audits demonstrably fail at AI velocity (CMU compounding data; Devin's daily sweep) — the continuous gate layer must carry the load; no host measurement yet, research-earned only. | Fable 5-era | PROMOTE to gate when any host's ratchet check blocks a real new violation (evidence: feedback brief or run ledger). RETIRE if two consecutive releases show only baseline noise and zero catches. |
| Doctor check 19: CI-gate parity (gateCommand literal segments — split on `{…}`, trimmed, ≥10-char floor with a whole-command fallback — required as a substring of concatenated `.github/workflows/*.yml`+`*.yaml`) | advisory | 2026-08-10 (spec 20260810/07, Prax stale-CI incident): root-cause fix for local-green/CI-red drift — one authored copy of "what checks run" (the Makefile-wrapper pattern); no host measurement yet, incident-earned only. | Fable 5-era | PROMOTE to gate if a host's ledger records a CI/gate-command drift incident this check would have caught. RETIRE if two quarters of doctor runs across hosts show zero findings AND no host reports respelled-but-equivalent CI as a false negative (the check cannot see equivalent-but-differently-worded CI steps, so persistent silence needs that corroboration before it counts as clean). |
| Doctor check 20: section-citation scan (`citations-check.js` — two-line window, two-word file-reference lookback, loud SKIP accounting) | advisory | 2026-08-10 (spec 20260810/09, command-surface audit slice 2): six broken section citations found in one review cycle, then the checker's own first naive run found 25 more against the live corpus (71 percent of citations use the two-word "shared invariants" section idiom the naive grammar missed) — text pointing at a retired/renamed heading misdirects a reader as authority, and the class regrows silently without a complete enumerator. | Fable 5-era | PROMOTE to blocking after two consecutive releases report `MISS=0`. RETIRE if `shared-for` section filtering is ever replaced by a mechanism that structurally cannot produce a mismatched citation. |
| Terminal-observable ACs (produced fixtures) — every Decision promising a user-observable surface owes an AC asserting on the observable itself through the spec's own producer chain, never a hand-authored props object (invented-fixture liveness); enforced via the existing AC↔test matrix, Phase 3 refuter clause, and widened Phase 4 lock audit — no new script or review leg | gate | 2026-08-10 measurement (UpWell): 4 dead surfaces in one build, all with green terminals fed hand-typed props the author invented from the same mental model that wrote the wrong code — the pure-UI TDD exemption handed the terminal to the catalog, which cannot fail. | Fable 5-era | RETIRE the fixture-provenance clause if two consecutive quarters of escape data show zero invented-fixture-liveness findings. RETIRE the whole rule only if the dead-surface class stops appearing in escape data for two consecutive quarters. |

## Adding a row

A new mechanism enters this ledger the same run it enters the pipeline — never
retroactively. Every row requires all five fields:

- **Mechanism** — the guard by name, matching the string/marker it manifests as in generated
  files (so the doctor's grep can find it).
- **Kind** — `gate` (blocks), `advisory` (reports, never blocks), `structural` (zero-token
  shape, not a runtime check), or a named variant when the mechanism doesn't block/report but
  tunes another gate's intensity (e.g. `gate-sizing`).
- **Justification** — the incident or measurement that earned the mechanism, dated. "Seemed
  prudent" is not a justification; an incident (what broke, when) or a measurement (a ledger
  query, its result, its date range) is.
- **Earned under** — the model generation the justification was measured against (family +
  version, e.g. `Sonnet 4.5-era`), or `structural`/`harness-level, model-independent` when no
  model generation applies.
- **Promote/retire condition** — the specific, checkable condition that would move this row
  (advisory → gate, gate → retired, or an explicit "retire never" with its reason). Must name
  a measurement or a ledger query where one is possible — not "if it stops being useful."

**A row without a promote/retire condition is invalid** — do not land it; the mechanism ships
with no way for a future model generation, or a future `/spec:doctor` run, to know when to
stop trusting it or start trusting it more.
