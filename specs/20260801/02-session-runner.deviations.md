- [tests/autopilot/session.test.js] Spec's AC-20260801-02-6 and Contracts section don't lock the exact error shape/message that counts as an "unavailable-model error" (D9 only names the class) → test fixture throws `Error("model 'fable' not available")` as the fake's first-call error; implementation must classify by a reasonable substring/shape match, not this exact string.
- [autopilot/daemon/session.js] Contracts/Behavior name streaming-input mode (D8) but don't lock the SDK message-envelope literal an async generator must yield → chose a conservative `{type:'user', message:{role:'user', content:text}, parent_tool_use_id:null}` frame (standard SDKUserMessage shape); unverified against the live SDK since no test exercises the generator's yielded content (fakes ignore `prompt` entirely) — flag for the live smoke alongside A2.
- [orchestrator/.claude/spec.config.json] The gate '{testDirs}' placeholder invites a directory, but `node --test tests/autopilot` fails on Node 26 in this repo ("test at tests/autopilot:1:1 ✖"); only the glob form `node --test 'tests/autopilot/*.test.js'` runs. Build resolved the gate to the glob form to proceed — every future scoped build hits this. Out of this spec's File Plan; flagged for /spec:doctor or INTAKE, not silently fixed here.
