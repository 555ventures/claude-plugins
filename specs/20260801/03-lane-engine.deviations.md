- [tests-batch] Contracts block shows only `startSurfaces` for checkpoint.js, but Behavior D12 requires an optional-screenshot-then-sendPhoto flow owned by the module that spawns commands → tests/autopilot/checkpoint.test.js pins a derived `screenshotIfConfigured({screenshotCommand, url, project, adapter, cwd, log})` export (substitutes `{url}`/`{out}`, reads the produced file into a buffer, calls `adapter.sendPhoto(project, {buffer,...})` on success, no-ops on non-zero exit) as the conservative, Behavior-grounded shape.
- [lane.js] AC-20260801-03-2 asserts the checkpoint ask (with tunnel URL) is posted after a single `flush()` (20 `setImmediate` ticks — measured ~2ms real wall time), but checkpoint.js's real `startSurfaces`/`captureTunnelUrl` must spawn a new `node` process for the test's `tunnelCommand` and wait on its stdout, which measured ~40-80ms of real OS process-spawn time regardless of implementation → `runCheckpoint` fully awaits `startSurfaces` before posting the ask, matching Behavior/D4 verbatim (the only correct order for a real tunnel URL); this AC cannot pass against real child-process timing without the test itself doing a bounded real wait (the pattern checkpoint.test.js's own AC-9 already uses) instead of `flush()` alone — flagging for the next test/review pass, not fixable from lane.js. Separately, the "bypass pollSeconds until idle" wake chain (D4/D5, see AC-12) needed an explicit `setImmediate` yield per bypassed cycle (`yieldTick()`) — without it a fake oracle/session returning the same re-admissible pick after "Next spec" recurses on microtasks only and OOMs the test run (the repo's injected-transport Gotcha), confirmed by reproducing the OOM with `--max-old-space-size=200` before the fix and a clean pass after.
- [orchestrator, phase 4] Resolved the two entries above. (a) AC-20260801-03-2: fixed in the TEST, not the implementation — `lane.test.js` gained a `waitFor(predicate, 2000)` bounded real-time poll (the pattern `checkpoint.test.js`'s AC-9 already uses) replacing `flush()` for the checkpoint-ask assertion; `runCheckpoint` still fully awaits `startSurfaces` per D4. No assertion was weakened. (b) AC-2 vs AC-12 then exposed a genuine cadence conflict in `lane.js`: the "bypass pollSeconds until idle" wake chain was armed by BOTH the brief checkpoint and the halt ask, so AC-2's static fake oracle re-ran the same pick 20x after "▶ Start". Narrowed the chain to the halt path only — "➡ Next spec" (D5) means the operator asked the lane to move on, so completions chain until idle (AC-12); "▶ Start" (D4) releases only the stage it gates, and ordinary `pollSeconds` cadence resumes when that stage finishes (AC-1/AC-2). `yieldTick()` retained for the halt chain per the injected-transport Gotcha.
