- [tests] AC-9 needs a throwaway repo path for `runStage`, but the spec's Contracts/Decisions gate live.test.js only on AUTOPILOT_LIVE + the four credential vars (D6) and never name a repo-path variable → added `AUTOPILOT_LIVE_REPO` (operator-prepared, `/spec:init`-grounded throwaway repo) as a fifth required env var for AC-9 only; AC-7/AC-8 are unaffected. Assert its presence explicitly rather than silently deriving/creating a repo.
- [doctrine] D8 directs recording the class-level falsifier "in spec/INTAKE.md, not implemented here," but INTAKE.md's own authoring contract requires every row's `Pinned by` to name a failing test or a `pre-contract` doc/ledger artifact, and this batch's File Plan carries no new test file for the class-level check (only the instance fix's own ACs, tests/autopilot/*.test.js) → landed the row with a `pre-contract` citation to this spec's Rationale + D2–D4 (the instance fix) rather than inventing an out-of-batch test file or leaving the pin empty; the row text says explicitly the class-level check itself has no repro test yet, matching D8's "fix the class on its own evidence" framing.
