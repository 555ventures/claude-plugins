- [doctrine batch] File Plan row for spec/commands/review.md called for net doctrine lines to go down; the locked D6/D7/D8 mechanical-finding and reconcilePath prose (a new Phase 0 step, a new mechanical-findings step, and the excluded/renamed presentation line) is new required content with no equivalent old prose in review.md itself to delete (the old File-Plan-scoped-diff prose the Rationale cites lives in wf-review.body.js, a different File Plan row) → kept all locked-Decision content verbatim and trimmed wording elsewhere for minimum growth; net review.md line count rose from 318 to 336 lines.
- [gate-scripts] plain `git status --porcelain` reports a wholly-untracked directory as one `?? dir/` line, not its files (AC-20260805-01-4 fixture creates only `src/new.js` inside a previously-nonexistent dir) → scope-reconcile.js uses `git status --porcelain --untracked-files=all`
