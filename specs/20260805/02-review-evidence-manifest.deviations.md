- [tests-batch] D6 says the two existing pins (`boot smoke leg green`, `never write CLEAN on a row whose survived is non-zero`) get "updated to the new wording" alongside review.md's rewrite, but the spec names no exact replacement text → kept both regexes verbatim in the new `AC-20260805-02-6` test (verdict-doctrine.test.js) as positive pins on the underlying invariant surviving in some form, instead of inventing wording a later implementer would then be forced to match; the genuinely new AC-8 schema assertions (D5 enum + `legs` field + SURVIVORS retirement) were added fresh to tests/run-ledger.test.js so they demonstrably fail on current code.
- [doctrine-batch] File Plan's review.md row says "DELETE replaced CLEAN/verdict prose (net lines down)", but AC-20260805-02-6/7's pinned regexes (fresh-manifest lifecycle, per-leg row-append instructions for the new `ci` leg, the pre-panel hard-stop step with its own `verdict.js --ledger` GATE_RED append, and the two-pass verdict derivation replacing the one-line CLEAN ⇔ sentence) require materially more doctrine than the old prose held, even after trimming per-leg JSON schemas down to name+`observed`-format only → took the conservative option (satisfy every pinned AC over the line-count prediction): review.md net +55 lines, release.md net +28 lines. No prose was left un-trimmed that duplicated the intro's one-time manifest-row-shape sentence.
