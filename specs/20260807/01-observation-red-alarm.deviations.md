- [spec/scripts/spec-status.js,observe-ci.js] D1 moves the ⚠️ Anomalies section above 🎯 Next (previously below); the folded-anomaly summary line's trailing clause "tagged ⚠️ above" (referring to a spec's own Next line, which used to render before the anomalies section) became directionally wrong once Next moved below Anomalies → changed the word to "below" to match the new order; no AC or test pins the exact word, so this is a plain accuracy fix, not a behavior change.
