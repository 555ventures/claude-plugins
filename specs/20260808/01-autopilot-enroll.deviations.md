- [repair-round-1] tests/autopilot/enroll.test.js's `runAutopilot` used `spawnSync` against an in-process `node:http` stub server; spawnSync blocks the calling process's event loop for the child's entire lifetime, so the parent could never service the child's request — every AC reaching a live stub hung to the 5s timeout with status=null/SIGTERM (AC-1..6, 8, 10, 13). → Switched `runAutopilot` to async `spawn` (Promise resolving on `close`, same 5000ms kill-timer, same `{status, signal, stdout, stderr, home}` shape), awaited it at every call site, and made AC-9's test body async — no assertions changed.
