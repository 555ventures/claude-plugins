- [daemon-lib] D6's "lock/daemon liveness (pid in lockfile alive -> 'daemon running (pid N)')" line does not state whether "no daemon running" counts as a doctor failure; the pinned test tests/autopilot/doctor.test.js AC-20260810-05-6 requires `ok:true` overall on a box with everything else passing and no daemon running (the linux D3 service checks, run separately, are what assert install/active state) → made the "daemon running" line always `ok:true` (informational detail only, never gates doctor's overall `ok`).
- [daemon-lib] D5's stale-lock recovery ("unlink then a fresh wx write... EEXIST on that second write means another starter won the race") doesn't cover the case where the racing starter's own write is already gone by the time this process re-reads the lockfile to name its pid (tests/autopilot/lock.test.js AC-20260810-05-11's race simulation forces exactly this: the second EEXIST is thrown without ever landing real content) → made the post-EEXIST pid read best-effort (tolerates ENOENT), still always throwing LockError.
