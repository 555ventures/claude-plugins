- [repair-round-1-plugin-tests] run-ledger.test.js's `/exactly ONE line/` pin broke on release.md's existing line-wrap (the phrase spans a markdown line break at "exactly\nONE line") — release.md is outside this batch's assigned files (tests/*.test.js only), so the doctrine wrap was not touched → widened the regex to `/exactly\s+ONE line/`, which still asserts the invariant (release.md documents an exactly-one-line append) without depending on incidental line-wrap formatting.
