- [wf-panel/dispatch batch] D2 flips wf-panel's aggregate seat to `model: 'fable'` but the File Plan row only names the model/effort/text edits, not the call mechanism; a bare `agent()` call has no fallback path (D1 lives inside `dispatch()`), so pinning `fable` there without also routing the call through `dispatch()` would make D1's fallback machinery dead code for the one seat it was built for and leave the seat unable to recover from a fable outage — spliced `// @fragment:dispatch` into wf-panel.body.js and changed the aggregate call from `agent(...)` to `dispatch(...)`; research/propose calls stay on plain `agent()` since MODEL_FALLBACK only maps `fable`.
