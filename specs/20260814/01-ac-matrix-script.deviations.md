- [01/doctrine] D6's colliding-pin retag list didn't enumerate `tests/consistency/drift-reconcile.test.js`'s literal-phrase pin on "the Phase 0 grep matrix IS the drift gate" in review.md's Drift gate section — reworked step 5's invocation text initially dropped that literal while adding the script-executor citation D5 requires. → kept the literal phrase intact ("the Phase 0 grep matrix IS the drift gate, now executed by `ac-matrix.js` …") rather than retagging the out-of-batch test file, per the locked-retired-literal Gotcha's in-place-update-without-weakening remedy; both the pin and D5's script-citation requirement hold.
