- [tests-batch] AC-20260815-02-7 and AC-20260815-02-8's manifest-only constructions already pass on current (pre-D4) verdict.js: an unrecognized `at-risk` row is simply ignored by today's REVIEW_LEGS/REVIEW_BLOCKING regardless of presence or exit code, so no manifest input can distinguish "ignored" from "correctly required-but-non-blocking" until `at-risk` actually enters REVIEW_LEGS. Kept both as written (they remain valid regression guards once D4 lands) → tests/review/verdict.test.js. Only AC-20260815-02-6 (presence/absence under the fail-closed required-leg check) is genuinely red-first today.
- [doctrine-batch] D8's literal 6.78.0 target was already superseded at HEAD (6.80.0 shipped first, from the prior spec's build). Bumped to the next free version, 6.81.0, per the version-bump-discipline Gotcha → spec/.claude-plugin/plugin.json.
- [orchestrator/Phase 4] Assumption A4 fired: the pre-image suite check found SIX pins reddened by D4's REVIEW_LEGS extension in three suites the adversarial pass did not enumerate — tests/clean-row-survivor-consistency.test.js (2), tests/verdict-gatered-no-workflow.test.js (2), tests/suite-baseline/doctrine.test.js (2, one of them a review.md step-8 prose regex broken by D3, not D4). Per A4 all three became in-flight File Plan rows; fixtures gained the at-risk row and the two byte-exact regexes were retargeted, none weakened. D3's new AC-20260815-02-14 was added in the same edit for the step-8 findings-producing-legs promise, which had no AC. Corroborating evidence for D3's run-not-list design: this spec's own atRisk derivation listed all three surprise suites.
- [orchestrator/Phase 4] `.claude/suite-baseline.json` was edited surgically (one row removed) instead of via the printed `--update` remedy: a wholesale rewrite would have absorbed the unrelated pre-existing red in tests/feedback-loop.test.js (a stale INTAKE pin naming a nonexistent tests/tdd-waiver-provenance.test.js) into the sanctioned set, which Phase 4's own disposition says to WARN on, never repair or absorb.
