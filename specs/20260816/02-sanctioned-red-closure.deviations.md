- [doctrine-batch] D8's target version 6.86.0 was already taken at batch time (plugin.json read 6.90.0, from concurrent/prior landings) → INTAKE.md's JJ-20260816-03 `Fixed in` cell records 6.91.0 (next free after the observed current version) per the standing Gotcha ("a spec Decision naming a literal version-bump target can be stale by build time... the build bumps to the next free version"); the plugin.json bump itself is a separate File Plan row/batch this worker does not own — if that batch lands a different next-free number, reconcile this cell to match at review.
- [orchestrator] D1's rewrite of the at-risk hard-finding sentence dropped the finding's quoted headline ("at-risk pins red: pins that live outside the scoped gate failed on this diff"), which is the literal carrying tests/scoped-gate-behavior-collision.test.js (JJ-20260815-03 / AC-20260815-02-10) — a doctrine pin outside the File Plan, green pre-build, red after the doctrine batch. → Restored the headline inside the rewritten sentence, keeping D1's new "cites exactly the NEW-FAILING residual lines" requirement. Repair landed in spec/commands/review.md (already a File Plan row), so no scope widening; the pin was never weakened. This is the colliding-pin class the host Gotcha names, in its behavior-change (not retired-literal) form.
