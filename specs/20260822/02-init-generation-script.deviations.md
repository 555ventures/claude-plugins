- D11 version target: the spec names 7.17.0, but HEAD already carried 7.18.0 (the recorded semver-race gotcha — the literal number is a target, not a pin). Bumped to 7.19.0 with the same changelog obligation.
- `build_base: main` corrected at build close to the true pre-image sha `909bd30`: a sibling session committed spec 20260822/01 (`b4cf091`) onto `main` after this worktree branched, so the moving-branch base diffed that spec's files (review.md, spec-review-driver.js, verdict.js, tests/review/*) into this spec's panel as out-of-plan. Recorded per the host Gotchas' stale-base entry; review inherits the corrected value with no special handling.
- `tests/run-ledger.test.js` (D12, an edit-only File Plan row carrying no AC) was authored with the token `AC-20260822-02-3` inside a comment naming AC-3's new behavioral home. red-check derives carriage from AC-ID occurrence, so the token forced a false red expectation onto a file whose only change is a deletion (`unsanctioned-green`). Reworded the comment to name `tests/init-gen/generate.test.js` without the ID — the removal fix the host Gotchas prescribe for this class, never an invented ID.
