# Deviations — specs/20260823/04-review-close-hardening.md

- AC-20260823-04-4's exec pin (tests/frontmatter/frontmatter.test.js) is **pre-green at test-authoring time**, not red. Executed spike (2026-08-23): spec-review-driver.js already routes `build_base:` through `lib/frontmatter.js`'s existing `fmVal` (landed by specs/20260823/03 D4, which D8 records as an orchestrator-reconciliation fact), and that implementation already strips a whitespace-preceded inline comment correctly — a synthetic host with `build_base: <sha>   # set by enter-worktree` already resolves to bare `<sha>` and reaches state REVIEWER with no `fatal: invalid object name`. The spec's own AC-4 text carries no `[pre-green: ...]` marker (unlike AC-9), but D8's narrative makes this outcome expected, not a stale assumption: "Both drivers already wrap the import as a local `fmVal(k)` arrow, so each is a one-line import rename." Per this repo's guidance on vacuous-but-correct pins (a sibling spec landing the underlying fix first is a legitimate, not a false, red), the test is kept as authored and tagged `[pre-green: fixed-by-sibling-spec-20260823-03]` in its own name rather than forced into a false-red assertion. No spec text was changed to accommodate this; only this bullet records the departure.
