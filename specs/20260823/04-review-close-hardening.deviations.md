# Deviations — specs/20260823/04-review-close-hardening.md

- AC-20260823-04-4's exec pin (tests/frontmatter/frontmatter.test.js) is **pre-green at test-authoring time**, not red. Executed spike (2026-08-23): spec-review-driver.js already routes `build_base:` through `lib/frontmatter.js`'s existing `fmVal` (landed by specs/20260823/03 D4, which D8 records as an orchestrator-reconciliation fact), and that implementation already strips a whitespace-preceded inline comment correctly — a synthetic host with `build_base: <sha>   # set by enter-worktree` already resolves to bare `<sha>` and reaches state REVIEWER with no `fatal: invalid object name`. The spec's own AC-4 text carries no `[pre-green: ...]` marker (unlike AC-9), but D8's narrative makes this outcome expected, not a stale assumption: "Both drivers already wrap the import as a local `fmVal(k)` arrow, so each is a one-line import rename." Per this repo's guidance on vacuous-but-correct pins (a sibling spec landing the underlying fix first is a legitimate, not a false, red), the test is kept as authored rather than forced into a false-red assertion. (Correction, build close: the test author had also written a `[pre-green: fixed-by-sibling-spec-20260823-03]` tag into the test's own name; the orchestrator removed it, because that enum is closed (`fallback-rejection` | `absence-invariant` | `predicate-in-test`) and a sibling-landed fix is none of the three — an out-of-enum tag in a test name is a hard `invalid-pre-green` finding, and this sidecar is the correct home for the departure.) No spec text was changed to accommodate this; only this bullet records the departure.

- AC-20260823-04-4's pre-green status is now **falsified-once, not merely reasoned**. Executed 2026-08-23 at build close: the strip in `lib/frontmatter.js`'s `stripFrontmatterValue` was deliberately removed (`return trimmed` in place of `return trimmed.replace(/\s+#.*$/, '')`), and `node --test 'tests/frontmatter/*.test.js'` went from 10 pass / 0 fail to 6 pass / **4 fail**, AC-4's driver exec pin among the four (alongside AC-1 and both raw-block/full-text entry pins). The strip was restored immediately and the file re-verified byte-clean against the checkpoint commit, suite back to 10/10. This distinguishes the pin from the vacuous-pre-implementation-pin class `red-check.js` guards: a vacuous pin cannot fail by construction, whereas AC-4 fails the moment the behavior it names regresses. It is green on arrival only because specs/20260823/03 landed the underlying fix first — a coverage-provenance fact, not a coverage gap.
