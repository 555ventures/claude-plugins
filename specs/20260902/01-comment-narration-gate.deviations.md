# Deviations — 01-comment-narration-gate

- D11's Applied-to parenthetical quotes `.claude/agents/gate-scripts.md`'s line-count
  guidance as "5–12 lines is normal"; the file on disk reads "15–35 lines is normal". Kept
  the on-disk phrasing unchanged (only swapped the "why (dated incident)" content), per the
  task's own instruction to preserve that phrasing and D11's "no other sentence in those
  files changes" constraint — the line-count figure is not part of the header-comment
  sentence D11 rewrites.
