# Deviations — 06-plugin-owned-capture-at-approval

- tests/mocks/mocks-driver-fixtures.js (not in the tests-layer File Plan) gained `writeFixtureCapture`/`writeCaptureConfig` and a `writeCaptureConfig` call inside `advanceToJourneyApproved`, per the Rationale's "Executes leg" instruction to repair fixture hosts before every `journey-approved`/`approved` mark hits the new `--which` fallback. This is the sole shared location `advanceToJourneyApproved`/`advanceToApproved` live in, so the repair also keeps tests/mocks/mocks-driver-look-stops.test.js green (same fixtures, not in my file list) as an unavoidable side effect; tests/mocks/mocks-notes.test.js keeps its own private, independent fixture set and was left untouched (out of my batch, not named in the Rationale's enumeration).
- AC-20260905-06-5's fixture "browser" (tests/render/render-gate.test.js, `FAKE_CDP_BROWSER_SRC`) hand-rolls a minimal DevTools-over-WebSocket handshake (Target.createTarget/attachToTarget{flatten}/Page.enable/Runtime.enable/Emulation.setDeviceMetricsOverride/Page.navigate/Page.loadEventFired/Runtime.evaluate/Browser.close) matching Assumption A1's own executed-spike sequence, since the real render-capture.js does not exist yet to verify the exact protocol against. If the shipped implementation's CDP sequence differs from A1's narrative in some particular the fixture does not anticipate, this one test may need a follow-up adjustment at build time — not a weakening, a protocol-shape correction.
- tests/render/render-gate.test.js's `FAKE_CAPTURE_SRC`/`FAKE_CAPTURE_ENTRIES_SRC` fixtures gained a `page` block (the former unconditionally from `--width`, the latter opt-in via `FAKE_CAPTURE_PAGE`) per the File Plan's own AC-20260824-04-10 fixture-repair note; this changed no existing assertion in the file (no pre-existing test reads `doc.page`).
- render-gate.js's D5-triggered `--mocks` call now runs at `journey-approved`, which in the real
  `/spec:mocks` timeline precedes `theme-picked` (the mark that first copies a chosen
  `design/theme/<kebab>/tokens.css` into `design/tokens.css`) — so `design/tokens.css` may not
  exist yet at the point render-gate.js's own `runRules()` unconditionally passes it to
  render-rules.js's `--tokens` flag. None of the three adaptation rules
  (no-mock-overflow/desktop-fill/line-length) read a token color, so render-gate.js now
  substitutes a scratch empty tokens file when `design/tokens.css` is absent, `--mocks` mode
  only (`--spec` mode is untouched — its own precondition already requires the file). This is
  not named by any Decision or Contract in the spec; it is a forced, unblocking fix surfaced by
  wiring D5's call into the real mocks-driver.js test fixtures (tests/mocks/mocks-driver.test.js
  AC-20260905-06-7/-8/-9 and every pre-existing journey-approved/approved test reached through
  `advanceToJourneyApproved`), all of which exercise a host tree that has no `design/tokens.css`
  at that stage.
- spec/doctrine/design.md's D9 sentence (§ Design Canon) initially wrapped to +2 lines, pushing /spec:design's and /spec:init's read-load budgets 2 lines over cap (both budgets sum the whole file for `init`, and the four design-scoped sections for `design`). Fixed by tightening the new sentence's wording (net +1 line) and merging the adjacent "Local mock canon" intro's two short lines into one (net -1 line, no content lost) — design.md holds at 156 lines, both budgets pass. The § Design Render Gate cross-file citation from mocks.md's new sentence was reworded (`§ Design Render Gate, \`render-gate --mocks\`` instead of the reverse order) after citations-check.js's own resolver treated the literal word "mocks" immediately before `§` as a same-basename match against mocks.md itself, producing a false MISS; reordering leaves it a SKIP (an unresolvable cross-file bare citation), the same as the file's existing `§ Design Authoring Contracts` citation.
- docs/adr/0007-plugin-owned-capture.md (layer `other`, its own wave) was authored by the doctrine-layer worker inside the doctrine+scripts wave alongside the ADR-0002 backlink the spec assigns as an orchestrator duty — one worker, one session of ADR context; the `other` wave was marked done with that same worker counted once, no second agent spawned.
