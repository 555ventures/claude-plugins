# Deviations — 03-questions-on-the-wireframe

- Test author note (tests/mocks/mocks-notes.test.js AC-20260906-03-5): a pinned question's underlying ledger row is necessarily `open`+`inferred` (or `invented`) — exactly the shape `requireGateOpen()` (mocks-driver.js:353, called before `requireNotesResolved` on every advancing mark including journey-approved) already blocks on, independently of notes. D4's worked example ("first stderr line `unanswered question(s) on onboarding: W7 (signin)`") is only reachable if the question-aware note gate is checked (or its message wins) ahead of/instead of the generic ledger-gate message for this case. The AC-5 test pins the literal AC wording; satisfying it is a build-time ordering/wording decision (e.g. checking notes before the generic ledger gate, or folding the two messages), not a test-authoring choice.
- Build ruling (orchestrator, resolved against D4 + § Behavior "Gate", no user fork): `journey-approved` and `approved` now call `requireNotesResolved` BEFORE `requireGateOpen`, so an unanswered question's line is the first stderr line as AC-20260906-03-5 promises; the generic ledger gate runs unchanged afterwards. A pinned question's row is by construction open+inferred, so the previous order could never reach the promised wording.
