- `scripts/dup-windows.js`'s `--update` scopes D3's `new-dup` refusal to an EXISTING baseline: when no `dup-baseline.json` file is present yet, `--update` is the seeding command (every tracked file's actual score is written as-is, `new-dup` does not refuse it) because "new" only means something relative to a baseline that already exists — the same seed carve-out `scripts/size-ratchet.js`'s `doUpdate` (`isSeed`) already carries, which D2/D3 incorporate by reference via "same tightness rule as spec 01 D2" / "identical operator model to the size ratchet".
- D9 (added mid-build) brought `scripts/size-ratchet.js` into this spec's File Plan as a MODIFY: both it and `scripts/dup-windows.js` had their local `writeFd`/`writeOut`/`writeErr` writer deleted in favor of importing `writeOut` from `spec/scripts/lib/driver-io.js`, resolved via `path.join(__dirname, ...)`, per D9's verbatim instruction.
