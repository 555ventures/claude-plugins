# Deviations — 20260909/04-review-soft-floor

- Test-author pass (tests layer): `escalate-row.fixtures.js`'s shared `reviewerReturn()`/`driveToCapEdge()` and `review-driver.fixtures.js`'s shared `SURVIVOR_RETURN` carry `severity:"soft"`. Under D1/D5/D8, a soft survivor never enters the hard pool, so once this spec's `spec-review-driver.js`/`verdict.js` changes land, every consumer of these two shared fixtures that dispatches a fix against the single survivor will break at setup (the disposer return naming `s0` will be refused as matching nothing in the hard-only pool, or the driver will auto-close instead of landing FIX). This batch's own files (`escalate-row-step.test.js`, `review-driver-fix-cycle.test.js`) work around it with a local hard-severity return and do not edit the shared fixture files, since those are also consumed by `escalate-row.test.js`, `escalate-cap-durable.test.js`, `review-driver.test.js`, `review-driver-close-refusals.test.js`, `review-driver-close-row.test.js`, `review-driver-replay-entry.test.js`, `review-driver-replay-record.test.js`, and `review-driver-close-drift-line.test.js` — none of which were in this batch's file list. The build layer should change `reviewerReturn()`'s and `SURVIVOR_RETURN`'s survivor `severity` from `'soft'` to `'hard'` (a one-line change in each file) as part of implementing D1/D5/D8, and re-run those out-of-batch files to confirm they still pass; `review-driver.test.js`'s own `AC-20260820-07-5` was checked and stays correct either way (its assertion only requires exit 2, which the contradiction guard still produces against an empty hard pool).
- Test-author pass: growing `tests/review/verdict.test.js`, `disposer-gate.test.js`, `disposer-gate.fixtures.js`, `disposer-gate-refusals.test.js`, `escalate-row-step.test.js`, `review-driver-fix-cycle.test.js`, and the new `soft-floor.test.js` trips `tests/consistency/size-ratchet-live.test.js` (`AC-20260908-01-9`) against the tracked `size-baseline.json`, which is outside this batch's file list. The build layer should run the raises `node scripts/size-ratchet.js --root . --raise tests/review/disposer-gate-refusals.test.js --to 17838 --cite specs/20260909/04-review-soft-floor.md`, `--raise tests/review/disposer-gate.fixtures.js --to 8794`, `--raise tests/review/disposer-gate.test.js --to 14840`, `--raise tests/review/escalate-row-step.test.js --to 18359`, `--raise tests/review/review-driver-fix-cycle.test.js --to 20382`, `--raise tests/review/verdict.test.js --to 129678`, and `--raise tests --to 3609707` (the `tests/` tree total), all citing this spec — exact byte counts may drift slightly once the corresponding implementation files are also in place and the suite is re-run.
- Orchestrator (red-check repair): the test author placed a genuinely-red "file-only, no count flags" test inside `tests/review/disposer-gate-refusals.test.js`, whose only new AC (AC-20260909-04-12) is a SHALL CONTINUE TO pin — red-check classifies the whole file `sanctioned-green`, so the red test broke the pin. Removed it; the same promise is already pinned red by AC-20260909-04-8 in `tests/review/disposer-gate.test.js`, which is where D7's new behavior belongs.
- Orchestrator (File Plan widening, forced collateral of D1/D5/D8): added four `other`-layer rows — `tests/review/disposer-gate.fixtures.js`, `tests/review/escalate-row.fixtures.js`, `tests/review/review-driver.fixtures.js` (shared fixtures whose survivor severity must move `soft` → `hard` or their out-of-batch consumers stop landing FIX), and `size-baseline.json` (ratchet raises cited to this spec, per .claude/rules/spec-pipeline.md § Review Checks). The lock-time collision sweep swept the literal `medium`, not the literal `soft`, so these were invisible at plan time.
- Scripts-layer worker (D6 collateral, wider than the previously-logged severity-soft symptom): D6's literal text ("when the hard pool is empty after `--mark reviewer-returned`") auto-dispositions and auto-closes on ANY empty hard pool, not only a soft-survivor one — so it also fires for the plain zero-survivor `CLEAN_RETURN` fixture (`disposer-gate.fixtures.js`/`review-driver.fixtures.js`) that many out-of-batch tests use as their "nothing found" setup, landing state CLOSE where those tests' own setup assertions still expect state DISPOSITIONS (the pre-image behavior, where an empty pool still needed an explicit `--mark dispositions --waived 0 --rejected 0 --fix-dispatched 0`). Confirmed via `node --test 'tests/review/*.test.js'`: 226/251 pass; all 25 failures trace into the three already-flagged forced-collateral fixture files (disposer-gate.fixtures.js, review-driver.fixtures.js, escalate-row.fixtures.js), split between the already-logged severity-soft symptom and this CLEAN_RETURN/empty-pool symptom — `disposer-gate.test.js:AC-20260901-09-1`, `disposer-gate-refusals.test.js:AC-20260901-09-8`, `review-driver-fix-cycle.test.js:AC-20260902-05-8`, `review-driver.test.js:AC-20260820-07-5`/`AC-20260901-02-4`, `review-driver-close-row.test.js` (4 tests), `escalate-row-step.test.js:AC-20260822-01-8/-12/-13`, `escalate-row.test.js` (4 tests), `escalate-cap-durable.test.js` (3 tests). None of these files are in this batch's file list (`spec/scripts/verdict.js`, `spec/scripts/spec-review-driver.js` only) and none were edited. The build layer's `severity: soft → hard` fix to the shared fixtures (already logged above) resolves the severity-soft half; the CLEAN_RETURN/empty-pool half needs each listed test's own setup assertion (`stateOf(...) === 'DISPOSITIONS'` right after a zero-survivor `reviewer-returned`) retagged to expect `CLOSE` directly, or an explicit `--mark dispositions --waived 0 --rejected 0 --fix-dispatched 0` inserted before it where the test's real subject is a LATER state transition.
- Scripts-layer worker: `spec-review-driver.js`'s header comment for D5-D10 originally read "a soft-only reviewer return used to demand the same ... ceremony" — `comment-narration.js --root .` flagged "used to" as prior-behavior narration (Worker Rules: header comments never narrate prior behavior). Reworded in place to a present-tense statement of the mechanism; `comment-narration.js --root .` now reports 0 findings in 301 files.
- Other-layer worker (fixture retag, per the already-logged Scripts-layer worker note above): `tests/review/disposer-gate.fixtures.js`'s `ONE_SURVIVOR_RETURN`/`TWO_SURVIVOR_RETURN`, `tests/review/escalate-row.fixtures.js`'s `reviewerReturn()`, and `tests/review/review-driver.fixtures.js`'s `SURVIVOR_RETURN` all had their sole survivor's `severity` retagged `'soft'` -> `'hard'` (D1/D8). `node --test 'tests/review/*.test.js'` dropped from 226/251 to 242/251; the remaining 9 failures were all the CLEAN_RETURN/empty-pool half of the same collateral (D6 auto-dispositions any empty hard pool straight to CLOSE, so a setup step built on a zero-survivor return no longer lands DISPOSITIONS). Retagged in place, never weakening an assertion:
  - `tests/review/disposer-gate-refusals.test.js` — `AC-20260901-09-8`: the two `stateOf(...) === 'DISPOSITIONS'` assertions (direct and --via loop hosts) retagged to `'CLOSE'`, citing AC-20260909-04-7/D6; the flag-refusal assertions (exit 2, stderr naming `--skip-independence-check-because` and `ADR-0005`, byte-identical state) are unchanged and still hold against the self-dispositioned CLOSE state.
  - `tests/review/disposer-gate.test.js` — `AC-20260901-09-1` (both the --via loop and no-flag hosts) and `AC-20260901-09-9`: swapped the reviewer-returned fixture from `CLEAN_RETURN` to `ONE_SURVIVOR_RETURN` (already retagged hard) instead of downgrading the assertion, since these three ACs' own point is proving the DISPOSITIONS-routing/CHECKPOINT-retirement mechanism, which needs a genuinely non-empty hard pool to reach DISPOSITIONS at all — the `'DISPOSITIONS'` assertions are unchanged.
  - `tests/review/review-driver-close-row.test.js` — `AC-20260820-07-6`, `AC-20260824-06-6`, `AC-20260823-03-11`, `AC-20260823-05-7`/`AC-20260824-06-11`: each test's separate `--mark reviewer-returned` (setup, asserted DISPOSITIONS) + `--mark dispositions --waived 0 --rejected 0 --fix-dispatched 0` (the actual close pass) pair collapsed into the single `reviewer-returned` call, since D6 now runs the close pass and appends the ledger row there; `AC-20260824-06-6`'s dirty-tracking fixture edits (the tracked `src/foo.js` write, the untracked `scratch.txt` write) moved to BEFORE that one mark so the diff snapshot still captures them, citing AC-20260909-04-7/D6.
  - `tests/review/review-driver-fix-cycle.test.js` — `AC-20260902-05-8`: swapped the reviewer-returned fixture from `CLEAN_RETURN` to `SURVIVOR_RETURN` (already retagged hard, newly imported) instead of downgrading the assertion, since this AC's manifest-disagreement refusal needs the run genuinely parked at DISPOSITIONS before the hand-edit runs — with `CLEAN_RETURN` the self-dispositioning at D6 would close the run before the hand-edit ever executes. Verified empirically that `SURVIVOR_RETURN`'s UNVERIFIED manifest-disagreement refusal still fires identically (verdict.js's first-match-wins order checks UNVERIFIED before any disposition arithmetic, per the script's own D5 comment).
  - `tests/review/review-driver.test.js` — `AC-20260901-02-4` (also `AC-20260901-09-6`): both the --via loop and no-via/no-stamp hosts collapsed their `reviewer-returned` + `dispositions` call pair into the one `reviewer-returned` call for the same D6 reason as the close-row file above; the ledger `before`-snapshot capture moved ahead of that one call.
- Fix-worker pass (post-review disposition `fix`, finding at spec-review-driver.js ~line 1328): D10's Contract text says the dispositions-time cap refusal keys on "the same count `handleFixApplied` uses" (`manifests.length - 1 + priorEscalations.length * FIX_CAP`), but mirroring that count in full at `handleDispositions()` — even restricted to just the durable, cross-session half (`unclearedEscalationsFor().length > 0`) — breaks TWO independent already-hardened pins, not one: (1) `AC-20260820-07-8`/`AC-20260909-04-13` in `tests/review/review-driver-fix-cycle.test.js` (the THIRD `--mark dispositions --fix-dispatched 1`, immediately ahead of the refused THIRD `fix-applied`, is pinned accepted — "the cap applies to fix-applied, not to entering FIX" — and at that call `manifests.length - 1` alone is already `2 >= FIX_CAP`); and (2), discovered while verifying the fix, `tests/review/escalate-cap-durable.test.js`'s first test (core § Incident Policy: "the fix-iteration cap survives a cold restart"), which is NOT in this spec's File Plan and pins the *identical* scenario the finding names — a cold restart with one uncleared escalate row for this spec — as ACCEPTED at `--mark dispositions --fix-dispatched 1` with a stderr warning (`cap .* is already spent`), refused only at the FOLLOWING `fix-applied`, which is deliberately where the durable escalate row for the repeat offense gets appended. Widening the dispositions-time guard to cover this case would refuse before that write and silently drop the second escalate row the durable-cap mechanism depends on. Both pins force the SAME exact case (an uncleared prior escalate row, `marks.escalated` unset) to opposite outcomes than the finding's literal fix demands, with no narrower condition able to satisfy both — so the guard is left unchanged (`marks.escalated && tally.fix > 0`, the pre-existing condition), and the over-cap dispositions-with-fix case this finding names is still caught one step later, unchanged, by `handleFixApplied`'s own refusal (which is where the durable-cap test's assertions already exercise it).
- Fix-worker pass (iteration 2, both dispositioned `fix` findings): the new behavioral test in `tests/review/verdict.test.js` (pinning `spec/scripts/verdict.js:613`'s sum-based contradiction-guard lead) and the corrected D6 refusal comment in `spec/scripts/spec-review-driver.js` (~line 1178) together pushed both files, and the `spec/scripts` and `tests` trees, over `size-baseline.json`. Ran `node scripts/size-ratchet.js --root . --reconcile --cite specs/20260909/04-review-soft-floor.md`, which raised `spec/scripts/spec-review-driver.js` 136636 -> 136859, `tests/review/verdict.test.js` 129678 -> 131060, `spec/scripts` 1293114 -> 1293337, and `tests` 3624190 -> 3625572.
