# Deviations — 06-every-mock-has-a-page-you-can-mark

- `docs/adr/` already has a `0026-a-note-is-a-conversation.md` from spec 05: `docs/adr/0026-every-mock-has-a-page-you-can-mark.md` is created exactly as the File Plan names it (A5's "if false" remedy), a second `0026`-numbered file distinguished only by its slug — never renamed here, per the orchestrator's instruction.
- D6's citation of `specs/20260905/01-picks-on-the-atlas-page.md D3` for "the lightbox bar's Pick this copy" is imprecise against that file's own D-numbering: the lightbox-opened-from-a-compare-card `Pick this` bar button is actually described in that spec's D4, while D3 covers the compare table's structure (including the candidate card's own `open ↗` link). Applied verbatim per the Decisions table (D3 named, not amended); the ADR's own `Applies to` and `Decision` sections describe both D3's `open ↗` link and D4's lightbox-bar copy so the record is accurate regardless of which D-number the citation meant.
- `lib/stop-block.js`'s pre-existing `PICKS_SCRIPT` (`__renderPick`) already queried `[data-decide="pick"]` — unrelated to D4's lightbox deletion. AC-20260913-06-5 (`tests/mocks/screen-page.test.js`) counts the literal `data-decide="pick"` across the WHOLE built atlas document (a plain regex, not a DOM query), so the two real button elements plus this one script-source occurrence always summed to 3, never the AC's expected 2 — a pre-existing false-positive collision the new AC exposes, not something this batch's edits introduced. Changed the selector to the equivalent unquoted CSS attribute selector `[data-decide=pick]` (`pick` needs no quoting; identical runtime behavior) so the script's own source no longer spells the literal the AC counts.
